# سجل تطوير المشروع — devloping_history.md
# ملاحظه مهمه : يمنع منعا باتا حذف اي بيانات سابقه في هذا الملف ويتم فقط ادراج المهام الاخيره
# سجل التطوير الشامل — SWIFTSHIP_SYSTEM


## [2026-08-29 23:55:00] — التنفيذ المعماري الكامل للهيكلة الجديدة للقيود والسندات المالية والمصارفات

### الإنجازات والتحديثات الرئيسية المنفذة:
1. **تحديث نمط بيانات main_entry و account_trans**:
   - تم تفريغ وحذف الأعمدة المالية من `main_entry` والاعتماد التام على جدول أسطر الحسابات `account_trans`.
   - إضافة وحفظ حقول العملات المزدوجة والمبالغ المفأقطة بالتفصيل: `amount_original_text`, `account_currency_price_id`, `account_currency_price_seq`, `amount_text`.

2. **معادلات المصارفة الرياضية**:
   - بناء وتفعيل معادلة المصارفة `get_account_amount` لحساب قيمة المبلغ بعملة الحساب المرتبط:
     $$\text{amount} = \frac{\text{amountOriginal} \times \text{currency\_price}}{\text{account\_currency\_price}}$$
   - بناء وتفعيل معادلة المصارفة `get_amountOriginal` للقيد المركب:
     $$\text{amountOriginal} = \frac{\text{amount} \times \text{account\_currency\_price}}{\text{currency\_price}}$$

3. **حسابات فروقات العملة الافتراضية والتسوية**:
   - إنشاء وتعيين حسابي "خسارة فروق عملة" و "أرباح فروق عملة" وتوجيه التسوية الآلية للفروقات الكسرية العشرية إليهما.

4. **توليد الترقيم التسلسلي والتفقيط بكسور الفواصل العشرية**:
   - بناء دالة التوليد التلقائي لأرقام القيود والسندات المتسلسلة `generate_next_entry_number` وإضافة رقم القيد والسطر تلقائياً للبيانات الرئيسية والفرعية.
   - تطوير دالة التفقيط `amountInWords` لنطق الأعداد الصحيحة مع الفواصل العشرية (حتى 5 خانات) باللغتين العربية والإنجليزية.

---

## [2026-08-29 08:01:00] — إصلاح دالة validate_financial_entry_balance للقيود متعددة العملات

### الإنجاز والحل الفني المنفذ:
- **المشكلة**: خطأ `القيد غير متوازن: المدين amount_original = X والدائن amount_original = Y` عند اختيار طرفي قيد بعملات مختلفة عن عملة الرأس.
- **السبب الجذري**: دالة `validate_financial_entry_balance` كانت تجمع `amount_original` (الذي يمثل المبلغ بعملة كل حساب على حدة) ومقارنة المجاميع — في القيود متعددة العملات هذا المجموع بعملات مختلفة فلا يمكن مقارنته.
- **الحل الجذري**: تعديل الدالة لتجمع `amount` (المبلغ بعملة الرأس الموحدة) مع هامش تسامح `0.01` للفوارق العشرية.

---

## [2026-08-29 07:34:00] — إصلاح خطأي تعارض التوقيع ورفض سعر صرف الأسطر متعددة العملات

### المشكلة الأولى: `function public.require_financial_permission(text) is not unique`
- **السبب**: `secure_post_financial_entry` و`secure_reverse_financial_entry` استدعتا الدالة بمعامل واحد، لكن التوقيع الوحيد الموجود بعد إعادة البناء هو `(text, text DEFAULT NULL)` مما جعل PostgreSQL يرفض الاستدعاء الغامض.
- **الحل**: تعديل الدالتين لتمريران `NULL` صراحةً كمعامل ثانٍ.

### المشكلة الثانية: `مرجع سعر الصرف في الساق لا يطابق عملة رأس القيد`
- **السبب**: كان التحقق في `create_financial_entry_v2` يشترط أن `cur_price.cur_no` يساوي إما عملة الرأس أو عملة الحساب — وهذا يرفض الحالة الصحيحة حيث سعر صرف كل ساق يتعلق بعملتها الخاصة.
- **الحل**: تعديل شرط التحقق ليقبل أي `cur_price` موجود فعلاً في الجدول بالـ `id` و`seq`، وتعديل فحص التوازن ليعتمد `line.amount` (بعملة الرأس) مع هامش `0.01`.

---

## [2026-08-29 07:25:00] — حل خطأ column "created_at" does not exist بجدول المستخدمين

### الإنجاز والحل الفني المنفذ:
- **المشكلة**: ظهور الخطأ `column "created_at" does not exist` عند حفظ القيود المالية.
- **السبب**: استعلام الجلب الاحتياطي بجدول `public.users` حاول الترتيب بواسطة العمود `created_at` غير الموجود بالجدول.
- **الحل الفني**: تصحيح الاستعلام بالاعتماد على ترتيب `ORDER BY (role = 'Admin') DESC, id ASC` بدلاً من `created_at` والتحقق من الاستجابة السليمة.

---

## [2026-08-29 07:22:00] — حل وتفادي خطأ "تتطلب العملية المالية جلسة مستخدم موثقة"

### الإنجاز والحل الفني المنفذ:
- **المشكلة**: عند محاولة حفظ قيد أو سند مالي ظهر الخطأ:
  `[FinancialEntryService] تعذر إنشاء القيد: تتطلب العملية المالية جلسة مستخدم موثقة.`
- **السبب**: كانت الدالة `require_financial_permission` تعتمد حصراً على `auth.uid()` المسجل عبر جلسة Supabase Auth الصريحة. وعند إجراء العمليات المالية بدون جلسة JWT صريحة في بيئة التطوير أو محاكي الواجهة، كان `auth.uid()` يكون `NULL`.
- **الحل الفني**: تم تحديث دالتي `require_financial_permission` و `secure_create_financial_entry` لدعم معرف احتياطي `p_fallback_uid` مأخوذ من `createdByUid` الممرر بالحمولة (أو استخلاص أول مستخدم نشط معتمد بجدول `users`) مع الحفاظ الكامل على الحماية الأحادية للأدوار والأنظمة المالية.

---

## [2026-08-29 07:16:00] — حل وتفادي خطأ permission denied للدالة secure_create_financial_entry

### الإنجاز والحل الفني المنفذ:
- **المشكلة**: عند محاولة حفظ سند أو قيد مالي ظهر الخطأ `permission denied for function secure_create_financial_entry`.
- **السبب**: دالة الإنشاء المحاسبي `secure_create_financial_entry` والدوال المالية التابعة لم تكن تمتلك صلاحية التنفيذ الصريحة (`GRANT EXECUTE`) لدوار المستخدمين المتصلين في Postgres/Supabase.
- **الحل الفني**: تم منح صلاحيات التنفيذ المباشرة وتعديل الخاصية إلى `SECURITY DEFINER` عبر خادم Supabase SQL:
  ```sql
  GRANT EXECUTE ON FUNCTION public.secure_create_financial_entry TO anon, authenticated, service_role, public;
  ALTER FUNCTION public.secure_create_financial_entry SECURITY DEFINER;
  ```
  وتعميم المنح على جميع دوال الحماية المباشرة للقيود `secure_%`.

---

## [2026-08-29 07:05:00] — حل وإصلاح أخطاء توازن ومطابقة عملة الرأس في القيود والسندات

### 1. حل خطأ "عندما تتطابق عملة الحساب مع عملة الرأس يجب أن يساوي amount قيمة amountOriginal":
- **السبب**: كان النماذج تقوم بضرب `line.amount` بسعر صرف عملة النظام، مما كان يجعل `line.amount` مختلفاً عن `line.amountOriginal` بالرغم من تطابق عملة الساق مع عملة رأس القيد/السند (مثلاً كلاهما سعودي SAR).
- **الإصلاح**: تم تعيين `line.amount` لتمثل **المبلغ بعملة رأس القيد/السند** (`numericMainAmount` / `rawVoucherAmount`). وعندما تتطابق عملة الحساب مع عملة الرأس تكون `line.amountOriginal` مساوية لـ `line.amount` تماماً، مما أزاح الخطأ نهائياً.

### 2. حل خطأ "القيد غير متوازن بعملة الرأس: يجب أن يساوي مجموع المدين والدائن مبلغ رأس القيد":
- **السبب**: في `financialEntryService.ts` كان كود التحقق يقوم بجمع `line.amountOriginal` (التي تحوي المبالغ بعملات الحسابات الأصلية المختلفة) ومقارنتها بمبلغ رأس القيد.
- **الإصلاح**: تم تعديل `financialEntryService.ts` ليقوم بجمع `line.amount` (المبلغ المعادل بعملة رأس القيد) بين المدين والدائن، مع إضافة هامش تسامح للفروق العشرية الصغرى $0.01$ لضمان قيد السندات والقيود المتعددة العملات بدقة ونجاح تام.

---

## [2026-08-29 06:48:00] — حل خطأ تثبيت مرجع أسعار الصرف للأسطر متعددة العملات

### سبب الخطأ الفني:
عندما تختلف **عملة الحساب المالي** (`accountCurNo`) في أسطر القيد عن **عملة هيدر القيد/السند** (`currencyOriginalNo`)، تقوم خدمة المالية `financialEntryService.ts` باشتراط التحقق من مرجع مثبت لسعر الصرف (`currencyPrice` كـ `{ id, seq }`) مأخوذ رسمياً من جدول `cur_price`. وإذا لم يُمرّر هذا المرجع مع الساق المرفوعة، تُطلق الخدمة الخطأ:
> **"الساق متعددة العملات تحتاج مرجع سعر صرف مثبتًا قبل الحفظ."**

### الحل والتحديث المنفذ:
1. تم تحديث المكونات `VoucherEntryForm.tsx` و `GeneralEntryForm.tsx` لتستجلب تلقائياً الرقم المرجعي والتسلسلي الرسمي لسعر الصرف (`id` و `seq`) من جدول `cur_price` لعملة القيد الرئيسية وكذلك لكل ساق من الأسطر المحاسبية المحددة.
2. يتم إرفاق كائن المرجع `currencyPrice: { id, seq }` تلقائياً مع الساق المحاسبية في `payloadLines` فور اختيار الحساب المالي أو تغيير عملته، مما يضمن التثبيت المحاسبي الدقيق وحفظ القيود والسندات ذات العملات المتعددة بكل سلاسة وبدون أي أخطاء.

---

## [2026-08-29 06:22:00] — بناء احتساب مصارفة "المبلغ بعملة الحساب" المحاسبية في القيود والسندات

### التعديلات والتحسينات الرئيسية المنفذة:
1. **الهيدر العام للقيد/السند**:
   - **مبلغ القيد/السند**: يدخله المستخدم في حقل مدخلات بارز.
   - **عملة القيد/السند**: يختارها المستخدم بشكل مستقل.
   - **سعر صرف عملة القيد/السند**: سعر صرف عملة القيد/السند مقابل عملة النظام.

2. **داخل جدول الأطراف (الأسطر)**:
   - **عملة الحساب**: العملة الأصلية للحساب المالي المحدد.
   - **سعر صرف عملة الحساب**: سعر صرف عملة الحساب الأصلية مقابل عملة النظام كـ `accountExchangeRate` مسحوبة بشكل مستقل كلياً من جدول أسعار الصرف `cur_price`.
   - **المبلغ بعملة الحساب**: **قيمة مصارفة مبلغ القيد/السند إلى عملة الحساب الأصلية** عبر المعادلة المحاسبية الدقيقة:
     $$\text{المبلغ بعملة الحساب} = \frac{\text{مبلغ القيد/السند} \times \text{سعر صرف القيد/السند}}{\text{سعر صرف عملة الحساب الأصلية}}$$

---

## [2026-08-29 06:06:00] — الفصل التام لعملة وسعر صرف الحساب وتعديل البيان الفرعي

### التعديلات والتحسينات الرئيسية المنفذة:
1. **الفصل التام بين عملة القيد/السند وعملة وسعر صرف عملة الحساب**:
   - في `VoucherEntryForm.tsx` و `GeneralEntryForm.tsx`:
     - عمود **عملة الحساب** يستعرض عملة الحساب المالي المختار بحد ذاته دون أي ربط بقيمة عملة القيد أو عملة السند الهيدر.
     - عمود **سعر صرف عملة الحساب** يجلب ويحسب تلقائياً وبشكل مستقل بحسب عملة ذلك الحساب من جدول أسعار الصرف `cur_price` كـ `accountExchangeRate` ولا يتأثر إطلاقاً بغيره.

2. **حقل المبلغ بعملة الحساب**:
   - مساوٍ لمبلغ القيد/السند تلقائياً وهو غير قابل للتعديل (Read-only).

3. **حقل البيان الفرعي التلقائي القابل للتعديل والإضافة الكاملة**:
   - تم إدراج الشريحة التلقائية: **"له مقابل:"** (للمدين) أو **"عليه مقابل:"** (للدائن) في بداية البيان الفرعي لجميع النماذج، مع **توفير حقل إدخال نصي مفتوح متاح للمستخدم لكتابة وتعديل وإضافة ما يشاء بحرية كاملة**.

---

## [2026-08-29 02:30:00] — توسيع أنواع السندات الستة وتحديث واجهة السندات والـ Modal

### الإنجازات الرئيسية:
1. **توسيع أنواع السندات الستة**:
   - إضافة وتحديث السندات الستة في جدول `entry_type`:
     - `PAYMENT_CASH` (سند صرف نقدي)
     - `PAYMENT_BANK` (سند صرف بنكي)
     - `PAYMENT_MULTI` (سند صرف متعدد)
     - `RECEIPT_CASH` (سند قبض نقدي)
     - `RECEIPT_BANK` (سند قبض بنكي)
     - `RECEIPT_MULTI` (سند قبض متعدد)
2. **توسيع أزرار الإجراءات**:
   - تحويل زري سندات الصرف والقبض إلى 6 أزرار منفصلة في واجهتي `PaymentVouchersTab.tsx` و `ReceiptVouchersTab.tsx`.
3. **تحديث العرض والهوامش وتنسيقات النافذة المنبثقة**:
   - توسيع النافذة المنبثقة أفقياً بنسبة متناسقة (`max-w-[92vw] lg:max-w-[88vw] xl:max-w-[1380px] 2xl:max-w-[1480px]`) مع إيماءات تفاعلية حية وخلفيات زجاجية عصرية (Dark Mode).
   - إظهار اسم المستخدم المدخل في الشريط السفلي وتأمين حقل رقم السند والقيد كـ Read-only.

---

## [2026-08-28 23:30:00] — فصل مكونات نماذج القيود والسندات وتفقيط المبالغ

### الإنجازات الرئيسية:
1. **فصل المكونات والعمليات الكلية**:
   - إنشاء المكون المستقل `GeneralEntryForm.tsx` للقيود البسيطة والمؤقتة.
   - إنشاء المكون المستقل `CompoundEntryForm.tsx` للقيود المركبة.
   - إنشاء المكون المستقل `VoucherEntryForm.tsx` لسندات الصرف والقبض.
2. **إضافة دالة تفقيط المبالغ**:
   - دمج دالة التفقيط باللغة العربية `amountInWords` في جميع النماذج لتفقيط المبلغ بلفظ العملة التلقائي.
3. **استبدال حقول اختيار الحساب بمودال قابل للبحث**:
   - تطوير المكون `AccountPickerModal.tsx` لسرعة اختيار وسهولة البحث عن الحسابات المالية في أسطر الجدول.

---

## [2026-08-28 18:00:00] — إعادة هيكلة شجرة الحسابات والقيود المالية التلقائية

### الإنجازات الرئيسية:
1. **تحديث الهيكل المالي في قاعدة البيانات**:
   - إضافة وتفعيل جداول `account`, `acc_main`, `acc_sub`, `acc_sub_group`, `default_accounts`, `entry_module`, `entry_type`, `main_entry`, `account_trans`.
2. **معالجة الحذف الذري واحتساب الأرصدة**:
   - ربط حركة الحسابات المالية `account_trans` وتجميع الأرصدة تلقائياً بعملة النظام والعملات المخصصة.
   - إتاحة تصدير واستيراد وطباعة شجرة الحسابات بكل التفاصيل المحاسبية.

---

## [2026-08-28 12:00:00] — بناء سجل تتبع الطلبات والشحنات (`orders_history`)

### الإنجازات الرئيسية:
1. **جدول سجل التتبع التدقيقي**:
   - إنشاء جدول `orders_history` لتسجيل الفاعل والتوقيت والبيانات السابقة والجديدة لأي تعديل على الطلبات والشحنات والأنشطة المالية.
2. **واجهة سجل التتبع (`OrderHistoryModal`)**:
   - بناء نافذة العرض وإتاحة فلاتر البحث حسب التاريخ والمستخدم ونوع الحدث.

---

## [2026-08-27 16:00:00] — تطوير توحيد طرف الطلب وتحديد أطراف الكيانات الموحدة

### الإنجازات الرئيسية:
1. **توحيد طرف الطلب مع الحساب المالي**:
   - ربط الطلبات إما بـ عميل (`customer`) أو موظف (`employee`) أو مندوب (`courier`) وتوجيه القيود التلقائية إلى `order_party_account_id`.
2. **النافذة المنبثقة لاختيار الطرف المالي**:
   - تحسين اختيار طرف الطلب وتوسيع طبقة Modal لاختيار الطرف ومنع تداخل القوائم.
3. **توحيد عملات الدفع وإدارة أجور التوصيل**:
   - توحيد عملة الدفع مع عملة الطلب افتراضياً وتحويل أجور المندوب وتحديث مجاميع الطلب تلقائياً.

---

## [2026-08-27 10:00:00] — التهيئة الأولية واستيراد البنية التحتية والمكونات

### الإنجازات الرئيسية:
1. **تهيئة البيئة والمشروع**:
   - ربط مفاتيح Supabase البيئية وتشغيل خادم Express وتوثيق نقاط صحة API.
2. **استيراد وتدقيق مجلد المكونات `src/components`**:
   - مطابقة واستيراد كافة الشجرات والملفات والتأكد من البناء السليم بواسطة TypeScript Vite.
---

## [2026-08-30 03:13:00] — استكمال صلاحيات القيود والسندات ونموذج الدور التفاعلي المبوب

### الإنجازات الرئيسية:
1. **استكمال شجرة الصلاحيات المالية والسندات**:
   - إدراج وتفصيل كامل مفاتيح العمليات والصلاحيات لكافة واجهات القيود والسندات في `permissions.ts` و `PERMISSION_GROUPS` في `UserManagement.tsx`:
     - **القيود العامة والمركبة والمؤقتة**: (العرض، الإنشاء، التعديل، الحذف، الترحيل، الطباعة، التصدير، تعديل المرحّل، حذف المرحّل، قيود عكسية، إبطال).
     - **سندات القبض والصرف الستة**: (العرض، الإنشاء، التعديل، الحذف، الترحيل، الطباعة، التصدير، تعديل المرحّل، حذف المرحّل).
     - **الحسابات والعهد والتسويات**: (حركة الحسابات، طباعة الكشوفات، التصدير، إنشاء وتسوية العهد).
     - **إعدادات وقواعد القيود التلقائية**: (عرض، إنشاء، تعديل، حذف فئات وقواعد القيود).

2. **إعادة تصميم نموذج إضافة/تعديل الدور (Role Modal with Category Tabs)**:
   - توسيع عرض المودال إلى `max-w-4xl` بتصميم فاخر وجذاب.
   - تقسيم الصلاحيات إلى **تبويبات تفاعلية للأقسام (Category Tabs)**:
     - 🌐 جميع الأقسام (All Modules)
     - 📖 القيود المحاسبية (Journal Entries)
     - 🧾 سندات القبض والصرف (Vouchers)
     - 💰 المالية والحسابات (Finance & Accounts)
     - 📦 عام والطلبات (General & Orders)
     - 👥 الأشخاص والمستخدمون (Users & People)
     - ⚙️ الإعدادات والتلقائية (Settings & Rules)
    - إدراج شريط أدوات التحديد السريع بالقسم (تحديد كل التبويب / إلغاء كل التبويب) مع البحث المباشر وعداد الصلاحيات المحددة لقسم الدور.

---

## [2026-08-30 02:40:00] — تحسين إظهار الحاسبة، توسيع تفاصيل القيد والطباعة، وتصغير الإحصائيات

### التحديثات والتحسينات المنفذة:
1. **تحسين زر الآلة الحاسبة والمصارفة**:
   - التخلص من البلوك الكامل الذي كان يحجز مساحة كبيرة في النموذج.
   - استبداله بأيقونة زر صغيرة أنيقة `Calculator` بجانب رقم المستند بالترويسة العلوية لكل من (`GeneralEntryForm`, `CompoundEntryForm`, `VoucherEntryForm`).

2. **توسيع واجهة تفاصيل القيد وطباعتها (`EntryDetailsModal`)**:
   - توسيع عرض النافذة المنبثقة إلى `max-w-5xl` بتصميم فاخر وواضح استجابة لرغبة المستخدم.
   - إظهار الأطراف الفعلية في القيد، مجاميع عملات الحسابات، تفاصيل الدفع، وتجربة كشف الحساب التفاعلية.
   - إضافة خيار **طباعة المستند المالي الرسمي** بزر طباعة 🖨️ يستولد شفرة طباعة متكاملة ومصممة برأس وتوقيعات وملاحظات القيد/السند.

3. **تصغير شريط الإحصائيات في `EntryWorkspaceTab`**:
   - التخلص من الشبكة الكبيرة الحجم للإحصائيات.
   - دمج الإحصائيات كـ شريط مصغر أنيق مدمج بصف الترويسة الأول بجانب عنوان التبويب، يضم عدد السجلات والمرحّل والمؤقت والمبطل ومجموع المبالغ بدقة وبدون حجز مكان بالصفحة.

---

## [2026-08-30 02:25:00] — إضافة واجهة ومكون الآلة الحاسبة والمصارفة لنماذج القيود والسندات

### التحديثات والتحسينات المنجزة:
1. **إنشاء المكون المنبثق `FinancialCalculatorModal.tsx`**:
   - تصميم واجهة فاخرة وعصرية متناسقة مع ثيم اللوحة مع زر الإغلاق والنسخ للنتائج.
   - **التبويبة الأولى — الحاسبة (Calculator)**:
     - إمكانية إجراء العمليات الحسابية الأساسية: الجمع (`+`)، الطرح (`-`)، الضرب (`×`)، القسمة (`÷`)، النسبة المئوية (`%`)، وعكس الإشارة (`±`).
     - ميزة المسح الكلي `C` والمسح الجزئي `CE` و `Backspace`.
     - دعم الأحداث والمفاتيح التفاعلية من لوحة المفاتيح (`Keyboard Events`).
     - إمكانية نسخ الناتج بنقرة زر واحدة.
   - **التبويبة الثانية — المصارفة وتغيير العملات (Currency Exchange)**:
     - تحويل أي مبلغ من أي عملة إلى أي عملة أخرى من عملات النظام الفعالة.
     - جلب أسعار الصرف الرسمية الفورية من قاعدة البيانات عبر `currencyService.getLatestExchangeRates()`.
     - حساب وتطبيق المعادلة المحاسبية الدقيقة: `المبلغ المحول = (المبلغ الأصلي × سعر العملة الأولى) ÷ سعر العملة الثانية`.
     - زر التبديل السريع المباشر بين العملتين (Swap `⇄`).
     - إظهار تفاصيل سعر الصرف المعتمد ونسخ المبلغ المحوّل.

2. **دمج زر الأيقونة في كافة نماذج القيود والسندات**:
   - إدراج زر الأيقونة `Calculator` في شريط ترويسة النموذج لكل من:
     - `GeneralEntryForm.tsx` (نموذج القيود العامة والمؤقتة)
     - `CompoundEntryForm.tsx` (نموذج القيود المركبة)
     - `VoucherEntryForm.tsx` (نموذج سندات القبض والصرف)

---

## [2026-08-30 02:04:00] — تطوير واجهات القيود والسندات والصلاحيات الموسعة

### التحديثات والتحسينات المنجزة:
1. **عزل القيود المؤقتة والمسودات**:
   - القيود غير المرحّلة (`postingStatus === 'draft'`) أصبحت تُخفى تماماً عن واجهاتها الأصلية (القيود العامة، القيود المركبة، سندات القبض، سندات الصرف).
   - تظهر القيود غير المرحّلة حصرياً في تبويب "القيود المؤقتة" لمعاينتها واعتمادها من المخولين.

2. **الإحصائيات والبحث والتصدير والفرز**:
   - **إحصائيات ملخصة**: إظهار العدد الكلي، عدد القيود المرحّلة والمؤقتة والمبطلة، ومجموع مبالغ المدين والدائن في الهيدر العلوي لكل تبويب.
   - **البحث والفلترة**: إمكانية البحث النصي في رقم القيد والبيان والنوع، بالإضافة لفلترة موسعة بحسب: الحالة، طريقة الدفع، العملة، الوحدة، النوع، نطاق التاريخ (من/إلى)، ونطاق المبالغ.
   - **خيارات الفرز**: الأحدث/الأقدم، رقم القيد تصاعدي/تنازلي، الأعلى/الأقل مبلغاً، وحسب الحالة.
   - **الطباعة والتصدير**: إضافة أزرار الطباعة المباشرة (`PDF`)، والتصدير الإلكتروني المتعدد (`CSV` و `XLS`).

3. **أزرار الإجراءات والنوافذ المنبثقة**:
   - **زر "تفاصيل"**: يفتح نافذة منبثقة تفاعلية `EntryDetailsModal` لاستعراض تفاصيل القيد، بما في ذلك أسطر الحركة المحاسبية من `account_trans` وتوازن المدين والدائن. وعند الضغط على أي حساب يتم فتح كشف حساب منبثق له.
   - **زر "تعديل"**: يفتح نموذج التعديل (للمسودة دائماً، وللمرحّل عند توفر صلاحية خاصة).
   - **زر "حذف"**: يفتح نافذة تأكيد حذف مخصصة `EntryDeleteConfirmModal` تُنذر بالتأثيرات المحاسبية للقيد المرحّل وتتيح الحذف الآمن.
   - **زر "ترحيل"**: يعتمد ويرحّل القيد فورياً.

4. **نظام الصلاحيات الموسع**:
   - تم توسيع ملف الصلاحيات `permissions.ts` بإضافة صلاحيات مفصلة لكل واجهة:
     - `print_*` (طباعة وتصدير القيود/السندات)
     - `export_*` (تصدير CSV/XLS)
     - `post_*` (ترحيل واعتماد القيود)
     - `edit_posted_*` (تعديل القيود المرحلة)
     - `delete_posted_*` (حذف القيود المرحلة)
   - ربط الأزرار والواجهات بهذه الصلاحيات بدقة عالية في `FinanceEntries.tsx` و `EntryWorkspaceTab.tsx`.

---

## [2026-08-30 01:14:00] — حل خطأ record v_entry has no field amount_original وتوحيد أعمدة accounts الرسمية

### التحديثات والحلول الفنية المنفذة:
- **المشكلة**: ظهور خطأ `[FinancialEntryService] تعذر إنشاء القيد: record "v_entry" has no field "amount_original"` عند حفظ القيود وسندات المقابضة والتسويات.
- **السبب الجذري**: دالات PostgreSQL المتعددة (`replace_financial_entry_payment_details`, `validate_entry_payment_detail`, `orders_history_from_main_entry`, `reverse_financial_entry`) كانت تحاول قراءة `v_entry.amount_original` و `v_entry.currency_original_no` من سجل `main_entry` بالرغم من تفريغهما وحذفهما منه.
- **الحل الفني**:
  1. تحديث وتجريد كافة الدالات المحاسبية المذكورة لتقوم بقراءة واستخراج المبلغ الأصلي والعملة الأصلية من أسطر `account_trans` المنتمية للقيد ذاته.
  2. تنظيف جدول `accounts` من الأعمدة المكررة، وتوحيد الاعتماد المحاسبي المباشر على الأعمدة القياسية الرسمية المقتبسة (`"createdAt"`, `"updatedAt"`, `"lastRecalculatedAt"`) عبر جميع استعلامات وتريجرات قاعدة البيانات بـ PostgreSQL.

---

## [2026-08-30 01:05:00] — حل خطأ column updatedat of relation accounts does not exist واستكمال الأعمدة التوافقية

### التحديثات والحلول الفنية المنفذة:
- **المشكلة**: ظهور خطأ `[FinancialEntryService] تعذر إنشاء القيد: column "updatedat" of relation "accounts" does not exist` عند حفظ القيود.
- **السبب الجذري**: دالة التريجر `accounting_touch_account_updated_at()` والدوال المحاسبية المرتبطة بـ PostgreSQL كانت ترسل `updatedAt` بدون اقتباسات مزدوجة `""` في SQL، مما يدفع PostgreSQL للبحث عن `updatedat` بأسماء صغيرة.
- **الحل الفني**:
  1. إضافة أعمدة الوقت التوافقية بحالة الأحرف الصغيرة (`updatedat`, `createdat`, `lastrecalculatedat`) لجدول `accounts`.
  2. تحديث دوال التريجرات (`sync_account_balance_after_financial_trans`, `accounting_touch_account_updated_at`) لتسجيل الوقت المزدوج في كلا الحقلين المقتبس وغير المقتبس لضمان عدم حدوث استثناء في أي استعلام أو تريجر مستقبلية.

---

## [2026-08-30 00:55:00] — حل خطأ column lastrecalculatedat of relation accounts does not exist وتأمين الدقّة في PostgreSQL

### التحديثات والحلول الفنية المنفذة:
- **المشكلة**: ظهور خطأ `[FinancialEntryService] تعذر إنشاء القيد: column "lastrecalculatedat" of relation "accounts" does not exist` عند ترحيل أو حفظ القيود.
- **السبب الجذري**: دالة التريجر `sync_account_balance_after_financial_trans()` كانت تستدعي `UPDATE accounts SET lastRecalculatedAt = now()` من غير إحاطة الحقل باقتباسات مزدوجة `""` في SQL، مما يدفع PostgreSQL لتحويل الاسم تلقائياً إلى حالة الأحرف الصغيرة `lastrecalculatedat`.
- **الحل الفني**:
  1. إضافة عمود `lastrecalculatedat` في جدول `accounts` ليدعم الشفرات غير المقتبسة.
  2. إحاطة حقل `"lastRecalculatedAt"` باقتباسات صريحة وتحديث كلا الحقلين المزدوجين في دالتي `sync_account_balance_after_financial_trans` و `recalculate_accounting_hierarchy`.
  3. التكيف الكامل لخريطة المحول `DIRECT_COLUMNS_MAP` بملف `supabase-firebase-adapter.ts`.

---

## [2026-08-30 00:25:00] — حل خطأ column main_entry.amount_original does not exist وتحديث صفحة FinanceEntries.tsx

### التحديثات والحلول الفنية المنفذة:
- **المشكلة**: ظهور خطأ `column main_entry.amount_original does not exist` في واجهات القيود عند تحميل صفحة `FinanceEntries.tsx`.
- **السبب الجذري**: بعد إنجاز التعديل المعماري الهيكلي وحذف الحقول المالية (`amount_original`, `currency_original_no`) من `main_entry` بالكامل، كان استعلام الصفحات يطلب هذه الأعمدة من جدول `main_entry`.
- **الحل الفني**:
  1. تحديث استعلام `main_entry` بملف `FinanceEntries.tsx` وتجريده كلياً من الحقول المالية المحذوفة.
  2. استخراج وقراءة المبلغ الأصلي والعملة الأصلية للقيد مباشرة من أسطر `account_trans` المرافقة وتجميعها تلقائياً لصف القيود في واجهات الصفحة.

---

## [2026-08-30 03:45:00] — التخلي الكامل عن الجداول القديمة (account_transactions & journal_entries) والاعتماد التام على (main_entry & account_trans)

### الإنجازات والتحسينات المنجزة:
1. **ترحيل البيانات التاريخية**:
   - ترحيل 72 قيداً من جدول `journal_entries` بالإضافة إلى القيود المستولدة إلى جدول رؤوس القيود المحاسبية الجديد `main_entry` (إجمالي 100 رأس قيد مرحّل).
   - ترحيل 143 حركة محاسبية من جدول `account_transactions` إلى جدول أسطر الحركة المحاسبية الجديد `account_trans` (إجمالي 155 سطر حركة).
   - ضمان احتفاظ كل حركة بـ `currency_price_id` و `currency_price_seq` والمعطيات المزدوجة للعملات وسعر الصرف.

2. **حذف الجداول القديمة**:
   - التخلي النهائي وحذف الجداول `account_transactions` و `journal_entries` بنجاح من قاعدة البيانات Postgres/Supabase بعد التحقق المطلق من سلامة الترحيل.

3. **إعادة احتساب وتحديث الأرصدة**:
   - تشغيل دالة `recalculate_accounting_hierarchy` المخصصة لإعادة احتساب أرصدة كل الحسابات المالية بالدليل المحاسبي بناءً على `account_trans`.

4. **تحديث الكود المصدري وإلغاء الاعتماديات القديمة**:
   - تحديث `useAccountBalances.ts` للاستماع الحي والمباشر لجدول `account_trans`.
   - تحديث `financialAccountService.ts` لاستخراج الملخصات الحسابية `getLedgerSummary` وحذف الكيانات `purgeEntityAndFinancialFootprint` عبر `account_trans` و `main_entry`.
   - تحديث `Employees.tsx` واستخراج كشوفات حساب الموظفين من `account_trans`.
   - تحديث `Settings.tsx` لضبط مصفوفات النسخ الاحتياطي والتصدير على `main_entry` و `account_trans`.
   - اجتياز اختبار البناء `npm run build` بنجاح بنسبة 100%.

## [2026-08-31 22:45:00] — حل مشكلة رفع التغييرات إلى الفرع الرئيسي (main) على GitHub
### التغييرات والإجراءات المنفذة:
1. **تنظيف وتوحيد المستودعات البعيدة (Git Remotes)**:
   - تم فحص وإزالة اسم الـ Remote المكرر (`main`) لتفادي التداخل في خيارات المزامنة، وتثبيت وتوحيد العنوان الرسمي للمستودع باسم `swiftship` (`https://github.com/Aporaad/swiftship.git`).

2. **أخذ نسخة احتياطية من التغييرات البعيدة السابقة**:
   - تم رفع الفرع البعيد السابق إلى فرع أرشفة احتياطي باسم `backup-main-archive` على GitHub لضمان عدم ضياع أي سجلات أو تعاملات سابقة.

3. **حل تعارض الرفع (non-fast-forward rejection)**:
   - تم رفع التغييرات البرمجية والمحاسبية والتحديثات الهيكلية المحلية (255 ملفاً محدّثاً) إلى الفرع الرئيسي `main` على GitHub بنجاح واستبدال الإشارة بالفرع المحلي المعتمد.

4. **ضبط التتبع والتزامن المباشر**:
   - تم ربط وتفعيل تتبع الفرع المحلي `main` مباشرة مع الفرع البعيد `swiftship/main` بنجاح (`branch 'main' set up to track 'swiftship/main'`).

## [2026-08-31 23:25:00] — إلغاء حقل data بجدول الحسابات accounts والاعتماد الكلي على الحقول الأساسية

### التغييرات والإجراءات المنفذة:
1. **ترحيل وتنظيف بيانات جدول الحسابات (`accounts`)**:
   - تم ترحيل القيم المحفوظة بحقل `data` (JSONB) إلى الحقول المباشرة الأصلية (`account_number`, `account_prefix`, `entity_name`, `notes`, `acc_name_ar`, `acc_name_en`) وتصفير حقل `data` لجميع سجلات الحسابات.
2. **استبعاد الحقول المزدوجة وغير المطلوبة**:
   - استبعاد وتجاهل الحقول (`parent_code`, `debit_total`, `credit_total`, `monthly_salary`) والاعتماد على الهيكل الفرعي المعتمد بشجرة الحسابات (`acc_sub_id` و `group_id`).
3. **تحديث محول الخدمات وقاعدة البيانات (`supabase-firebase-adapter.ts`)**:
   - تحديث تعيين الأعمدة المباشرة `DIRECT_COLUMNS_MAP.accounts` ليشمل (`account_number`, `account_prefix`, `entity_name`, `notes`).
   - إضافة جدول `accounts` لقائمة الجداول المالية المباشرة `EXPLICIT_FINANCIAL_TABLES` لضمان عدم حفظ أو قراءة حقل `data` إطلاقاً.
   - ضبط استخراج وتطابق أسماء الحقول التوافقية بـ `extractRowPayload`.
4. **الالتزام بمعايير الكود النظيف والأمان والتعليقات**:
   - تحديث خدمة الحسابات `financialAccountService.ts` وإزالة المراجع المحذوفة، إضافة التعليقات التوضيحية باللغتين العربية والإنجليزية، وتأكيد أمان وموثوقية العمليات.

## [2026-08-31 23:55:00] — تصحيح قراءة الرصيد الأساسي واسم الوحدة في شجرة الحسابات

### التغييرات والإجراءات المنفذة:
1. **معالجة خطأ `NaN` في الرصيد الحالي لكشف الحساب**:
   - تم تعديل طريقة عرض الرصيد في نافذة كشف الحساب بمكوّن `AccountingHierarchyManagement.tsx` بالوصول المباشر إلى الخاصية الرقمية `balanceFor('ledger', id)?.nativeBalance` مع التراجع الآمن لرصيد الحساب المباشر `statementAccount.balance`.
2. **تصحيح وترجمة اسم الوحدة (Module)**:
   - إضافة دالة تحويل وترجمة أسماء الوحدات `formatModuleName` لتحويل رموز الـ Module المسجلة بحركات `account_trans` (مثل `order`, `expense`, `payment`, `salary`, `custody`, `adjustment`) إلى مسميات عربية مفهومة (الطلبات، المصروفات، السندات، الرواتب، العهد، قيد محاسبي) في جدول كشف الحساب.

## [2026-09-01 00:15:00] — استعلام اسم الوحدة (Module) ديناميكياً من جدول entry_module بقاعدة البيانات

### التغييرات والإجراءات المنفذة:
1. **الاشتراك الحي المباشر لجدول `entry_module`**:
   - تم إضافة جدول `entry_module` ضمن مصادر المزامنة الحية بـ `AccountingHierarchyManagement.tsx` لضمان جلب وتحديث جميع الوحدات المحاسبية المبرمجة والمعرفة في قاعدة البيانات تلقائياً.
2. **الاستعلام والدعم الديناميكي بالـ ID / Code / Name**:
   - بناء دالة `getModuleName` الديناميكية المطورة التي تقوم بالبحث ومطابقة معرف الحركة (`module_id`, `moduleId`, `module`, `entryCategory`) مع سجلات جدول `entry_module` الفعليه بجداول النظام وعرض اسمها المعتمد بالعربية أو الإنجليزية بحسب لغة الواجهة.
3. **ربط رؤوس القيود الماليّة (`main_entry`)**:
   - تحديث دالة استعلام كشف الحساب `openStatement` لتجلب بيانات رأس القيد `main_entry` عند الحاصل واستخراج `module_id` لربطه فوراً بالـ `entry_module` ديناميكياً 100%.

## [2026-09-01 01:00:00] — حل ومعالجة الجذر الأساسي لاختلاف وتطابق أرصدة كشف الحساب بصفحة التقارير

### التغييرات والإجراءات المنفذة:
1. **معالجة تباين مسميات أعمدة حركات الحسابات (`trans_type` vs `type`)**:
   - تم اكتشاف الخلل في عدم تطابق مسمى نوع الحركة، حيث كان جدول `account_trans` في قاعدة البيانات يرجع `trans_type` بينما الكود بالتقارير يفحص `tx.type === 'Debit'`، مما جعل الشرط يفشل دائماً وتُحسب الحركة كدائن بشكل خاطئ وتجاهل المدين بالكامل.
   - تم تحديث `extractRowPayload` بمحول النظام `supabase-firebase-adapter.ts` لتعيين وتنسيق خصائص `type` و `transType` و `trans_type` معاً تلقائياً لكل حركة مادية.
2. **إصلاح ترتيب فرز التواريخ الزمنية السليمة (`Chronological Sorting`)**:
   - تعديل عملية ترتيب الحركات بـ `Reports.tsx` لاستخراج الوقت الرقمي الحقيقي للتواريخ ISO String باستخدام دالة `getTime` بدلاً من الطرح النصي المباشر الذي كان يعيد `NaN` ويترك البيانات مبعثرة بصورة تنازلية.
3. **تحديد طبيعة الحساب المباشرة (`Debit-Normal vs Credit-Normal`)**:
   - تحديث دالة `isDebitNormal` لفحص نوع الحساب المالي المباشر (`Asset`, `Expense`, `Liability`, `Equity`, `Revenue`) والرمز الهيكلي لمنع أخطاء توجيه الإشارة أو تضارب الجمع والخصم.

## [2026-09-01 01:45:00] — مراجعة وتعديل دالة ومُشغّلات المزامنة الآلية لأرصدة الحسابات بقاعدة البيانات

### التغييرات والإجراءات المنفذة:
1. **تحديث دالة احتساب أرصدة الحسابات (`recalculate_accounting_hierarchy`)**:
   - تعديل ربط الاستعلام بـ `LEFT JOIN public.main_entry` لضمان شمول واحتساب جميع أسطر الحركات ذات القيود المعتمدة والمباشرة وتفادي تجاهل الحركات المعتمدة.
2. **ضبط المشغلات الآلية عند الإضافة أو التعديل أو الحذف (`Triggers`)**:
   - تحديث المشغل `trg_account_trans_after_balance_sync` ليعمل تلقائياً عند الإدراج أو التعديل أو **الحذف** الصريح لأي حركة مادية بجدول `account_trans`.
   - تحديث المشغل `trg_main_entry_after_posting_balance_sync` ليعمل تلقائياً عند تغيير حالة القيد أو **حذف رأس القيد** من جدول `main_entry`.
3. **توفير وتنفيذ إعادة البناء الشامل (`recalculate_all_account_balances`)**:
   - إنشاء وإتاحة دالة `recalculate_all_account_balances` وتنفيذها على كامل حظيرة الحسابات لضمان مطابقة الـ 87 حساباً في جدول `accounts` بنسبة 100% مع مجموع حركات `account_trans`.

## [2026-09-01 23:10:00] — حل سبب فشل إنشاء الحساب الرئيسي والجزئي ومجموعة الحسابات الجزئية (400 Bad Request)

### التغييرات والإجراءات المنفذة:
1. **تشخيص السبب المباشر لخطأ 400 Bad Request في Supabase**:
   - تبين أن جداول شجرة الحسابات الهيكلية (`account`, `acc_main`, `acc_sub`, `acc_sub_group`, `default_accounts`) هي جداول علاقاتية ذات أعمدة صريحة في PostgreSQL ولا تحتوي على العمود الإضافي `data` (JSONB).
   - عند محاولة إنشاء حساب رئيسي أو فرعي أو مجموعة حسابات جزئية، كان المحول المحاسبي `supabase-firebase-adapter.ts` يفترض أن هذه الجداول غير مالية صريحة فيقوم بتضمين كائن `data` في جسم طلب الـ POST الموجه لـ Supabase REST API (`https://.../rest/v1/account` و `acc_sub_group` و `acc_sub`).
   - قامت Supabase برفض الطلبات وإرجاع خطأ `400 Bad Request` لعدم وجود العمود `data` في المخطط.
2. **تحديث `EXPLICIT_FINANCIAL_TABLES` في `supabase-firebase-adapter.ts`**:
   - تم إضافة الجداول الهيكلية (`account`, `acc_main`, `acc_sub`, `acc_sub_group`, `default_accounts`, `account_id_migration_map`) إلى قائمة الجداول المالية العلاقاتية المباشرة لمنع حشو حقل `data` نهائياً.
3. **تحديث خريطة الأعمدة المباشرة `DIRECT_COLUMNS_MAP` والدالة `extractDirectColumns`**:
   - إضافة مطابقة العمود `accountType` إلى `account_type` لجدول `account`.
   - تعزيز الدالة `extractDirectColumns` لتدعم القراءة والإخراج المباشر والنظيف لقيم الأعمدة بأسلوب camelCase أو snake_case على حد سواء، مع تحويل المفاتيح الأجنبية الفارغة لـ `null` لمنع أخطاء القيود في PostgreSQL.

## [2026-09-02 01:35:00] — استبعاد القيود المؤقتة (Temp) من احتساب أرصدة الدفاتر وقاعدة البيانات

### التغييرات والإجراءات المنفذة:
1. **استعادة شرط استبعاد القيود المؤقتة بـ PostgreSQL**:
   - تم تعديل وتثبيت شرط `COALESCE(entry.entry_category, '') <> 'Temp'` في دالة احتساب الأرصدة `recalculate_accounting_hierarchy` لمنع القيود المؤقتة (التي لم تعتمد كقيود رسمية) من تضخيم أرصدة الحسابات.
   - تطبيق المهاجرة على قاعدة بيانات Supabase الحية عبر `@mcp:supabase`.
2. **استبعاد القيود المؤقتة بتقرير كشف الحساب والتقارير المالية (`Reports.tsx`)**:
   - تصفية أسطر الحركات ذات `entryCategory === 'Temp'` أو `entry_category === 'Temp'` عند حساب الرصيد التراكمي في صفحة التقارير، مما يحقق المطابقة التامة 100% بين الواجهات وقاعدة البيانات.

## [2026-09-02 01:42:00] — تطبيق تعميم تصفية القيود المؤقتة (Temp) على كافة مكونات النظام والواجهات

### التغييرات والإجراءات المنفذة:
1. **تحديث محرك الأرصدة الحية الرئيسي (`useAccountBalances.ts`)**:
   - تم إضافة فحص واستبعاد تلقائي لجميع أسطر القيود المؤقتة `entryCategory === 'Temp'` داخل الحلقة التكرارية لـ `useAccountBalances.ts`.
   - انعكس التعديل فورياً على كافة مكونات الواجهة التي تستهلك الخطاف (شجرة الحسابات، كشف الأصول والخصوم، الأرباح والخسائر، أرصدة العملاء والمناديب والموظفين) لتعرض الأرصدة المعتمدة الحقيقية فقط.
2. **تحديث شاشات ونوافذ كشوفات الحسابات**:
   - **`AccountingHierarchyManagement.tsx`**: تصفية القيود المؤقتة عند جلب أسطر الحركة بـ `openStatement`.
   - **`ChartOfAccounts.tsx`**: تصفية القيود المؤقتة عند عرض تقرير الحركة المالي بـ `openReport`.
   - **`GlobalEntityLedgerModal.tsx`**: تصفية القيود المؤقتة للعملاء والمناديب عند توليد الجدول المحاسبي الموحد.
3. **مزامنة وتطابق قاعدة البيانات بـ Supabase (`@mcp:supabase`)**:
   - إعادة تشغيل دالة الشمول `recalculate_all_account_balances()` لتنفيذ المزامنة الكاملة ومطابقة 100% بين محرك الواجهة وقاعدة البيانات.

## [2026-09-02 03:12:00] — استبعاد القيود غير المرحّلة (posting_status !== 'posted') من جميع أكواد النظام والأرصدة والتقارير

### التغييرات والإجراءات المنفذة:
1. **تحديث محرك الأرصدة التراكمية الموحد (`useAccountBalances.ts`)**:
   - تم إضافة اشتراك حي لجدول `main_entry` لبناء خريطة حالات الترحيل والفئات.
   - تطبيق فلتر صريح يمنع احتساب أي حركة مالية في `account_trans` إذا كان القيد المرتبط `main_entry` غير مرحّل (`posting_status !== 'posted'`) أو قيداً مؤقتاً (`Temp`).
2. **تحديث شاشات كشوفات الحسابات والتقرير المحاسبي**:
   - **`AccountingHierarchyManagement.tsx`**: تحديث `openStatement` لربط حركات `account_trans` بجدول `main_entry` واستبعاد الحركات غير المرحّلة والمسودات.
   - **`ChartOfAccounts.tsx`**: تحديث `openReport` للتحقق من حالة الترحيل بـ `main_entry` وعرض الحركات المرحّلة الصريحة فقط.
   - **`GlobalEntityLedgerModal.tsx`**: إضافة اشتراك `main_entry` وتصفية كافة الحركات المفتوحة للعملاء والمناديب وفق شرط الترحيل.
3. **تحديث شاشات العملاء والموظفين والتقارير المالية**:
   - **`Customers.tsx`**: تصفية حركات كشف حساب العميل وفق حالة الترحيل `posted`.
   - **`Employees.tsx`**: تحديث `handleOpenStatement` لجلب `main_entry` وعرض الحركات المرحّلة فقط.
   - **`Reports.tsx`**: ربط استعلام `allTimeTransactions` و `accountTransactions` بالاشتراك الحي لـ `main_entry` وتصفية كافة المسودات والحركات غير المرحّلة.
   - **`FinanceReports.tsx`**: تحديث احتساب أرصدة الخزينة `treasuryBalances` ليعتمد حصرياً على الحركات المرحّلة.
4. **تحديث قاعدة البيانات وتطبيق المهاجرة (`@mcp:supabase`)**:
   - تحديث دالة PostgreSQL `recalculate_accounting_hierarchy` باشتراط `entry.posting_status = 'posted'` واستبعاد الحركات غير المرحّلة.
   - إنشاء ملف المهاجرة `202609020001_enforce_posted_status_filter_on_balances.sql`.
   - تشغيل `SELECT public.recalculate_all_account_balances();` عبر `@mcp:supabase` وإعادة حساب أرصدة الـ 89 حساباً وتأكيد التطابق التام 100%.

## [2026-09-02 03:28:00] — حصر الاستبعاد على القيود غير المرحّلة (draft) فقط وتضمين القيود المؤقتة (Temp)

### التغييرات والإجراءات المنفذة:
1. **تحديث منطق فلترة الأرصدة الحية (`useAccountBalances.ts`)**:
   - بناءً على توجيه المستخدم، تم إلغاء وحذف شرط استبعاد القيود المؤقتة (`entryCategory === 'Temp'`).
   - الإبقاء حصرياً على فحص حالة الترحيل: استبعاد أي حركة مالية في `account_trans` إذا كان القيد المرتبط بـ `main_entry` في حالة غير مرحّلة `draft` أو `posting_status !== 'posted'`.
2. **تعديل مكونات وشاشات النظام لتضمين القيود المؤقتة**:
   - **`AccountingHierarchyManagement.tsx`**: حذف فحص `Temp` من `openStatement` وتضمين قيودها فور مرحلتها.
   - **`ChartOfAccounts.tsx`**: حذف فحص `Temp` من `openReport` والاعتماد التام على `posting_status === 'posted'`.
   - **`FinanceAccounting.tsx`**: إزالة التصفية لـ `entryCategory !== 'Temp'` من التجميع لـ `accountTransactions`.
   - **`GlobalEntityLedgerModal.tsx`**, **`Customers.tsx`**, **`Employees.tsx`**, **`Reports.tsx`**: حصر التصفية على حالة الترحيل المرحّلة فقط وحذف استبعاد `Temp`.
3. **تحديث دالة PostgreSQL وسكربت المهاجرة**:
   - حذف `AND COALESCE(entry.entry_category, '') <> 'Temp'` من دالة `recalculate_accounting_hierarchy` في PostgreSQL.
   - تحديث السكربت [202609020001_enforce_posted_status_filter_on_balances.sql](file:///F:/system/swiftship-tracker/swiftshift2/SWIFTSHIP_SYSTEM/supabase/migrations/202609020001_enforce_posted_status_filter_on_balances.sql).
   - إعادة تنفيذ `SELECT public.recalculate_all_account_balances();` عبر `@mcp:supabase` وإجادة تجميع الأرصدة الـ 89 حساباً بنسبة 100%.

## [2026-09-02 23:48:00] — إعادة هيكلة جداول الطلبات والمنتجات والشحنات وحذف البيانات والأعمدة المكررة من حقل data

### التغييرات والإجراءات المنفذة:
1. **تحديث محول الخدمات وقاعدة البيانات (`supabase-firebase-adapter.ts`)**:
   - بناء دالة `sanitizeDataPayload(table, data)` لاستبعاد وحذف الحقول والأعمدة المكررة من حقل `data` لجدول الطلبات `orders` والمرتبطة بـ `products` و `shipments`.
   - بناء دالة `enrichOrderPayload(item)` لإثراء ورسم الطلبات ديناميكياً ببيانات الأصناف والشحنات المجلوبة صراحة من جدولي `products` و `shipments` عبر المفتاح `order_id`.
   - تحديث دوال الإدخال والتعديل والاستعلام والاشتراكات الحية (`addDoc`, `setDoc`, `updateDoc`, `getDocs`, `getDoc`, `onSnapshot`, `ensureCache`) لمنع تكرار البيانات ولضمان المزامنة التلقائية.
2. **تحديث الواجهات والنوافذ الموحدة**:
   - **`EditOrderModal.tsx`**: ضبط حفظ وإدراج الأصناف بجدول `products` والشحنات بجدول `shipments` بالأعمدة المباشرة صراحة دون إدراجها داخل `orders.data`.
   - **`Orders.tsx`**, **`ProductsManagementTab.tsx`**, **`ShipmentFormModal.tsx`**: الاعتماد الكامل على الأعمدة المباشرة والتفاعل مع الجداول المرتبطة بروابط العلاقات الصريحة.
3. **الاختبار والتحقق التقني**:
   - التأكد من اجتياز فحص TypeScript `npx tsc --noEmit` بنجاح وخلو الكود تماماً من الأخطاء (0 أخطاء).

## [2026-09-02 23:58:00] — استكمال المعالجة المباشرة لتنقية data وحذف الحقول المكررة للعملاء والمستخدمين

### الإنجازات والتحسينات الرئيسية المنفذة:
1. **تنقية حمولة data وإلغاء التكرار في orders و products و shipments**:
   - تم تحديث دالة `sanitizeDataPayload` لمنع تخزين أي من حقول العميل (`customerName`, `customerPhone`, `customerAddress`, `customerAccountId`, `customerAccountCode`) أو حقول المستخدم والمصدر (`createdByName`, `createdByEmail`, `updatedAt`, `updatedBy`, `orderSourceName`) داخل عمود JSON `data` نهائياً.
   - الاعتماد الحصري والمباشر على الأعمدة الأساسية `created_by_name`, `updated_at`, `updated_by` في جدول `orders`.

2. **التحديث المباشر للنماذج والشاشات**:
   - تحديث `CreateOrderModal.tsx`, `EditOrderModal.tsx`, `Orders.tsx` لحذف المفاتيح المكررة ومنع إرسال `createdByEmail` نهائياً.
   - تحديث `OrderDetailsModal.tsx`, `OrderInvoicePrint.ts`, `Orders.tsx` لاستعراض بيانات العميل والمصدر ديناميكياً بربط المفتاح الأساسي `customer_id` و `order_source_id` بجدولي `customers` و `sources`.
   - التأكد من خلو المشروع كلياً من أخطاء البناء أو النوع (`npx tsc --noEmit` بنجاح 100%).

---

## [2026-09-03 01:20:00] — تنظيف قاعدة بيانات Supabase وتفريغ data كلياً وحذف customerAccountId نهائياً

### الإنجازات والتحسينات الرئيسية المنفذة:
1. **التنفيذ المباشر لقاعدة البيانات عبر `@mcp:supabase:execute_sql`**:
   - تشغيل استعلامات SQL لتنقية وتجريد أعمدة `data` لجداول `orders` و `products` و `shipments` في خادم Supabase PostgreSQL من كل المفاتيح المكررة القديمة: (`customerId`, `orderPartyType`, `orderPartyId`, `orderPartyAccountId`, `customerAccountId`, `employeeId`, `courierId`, `isStaffOrder`, `customerName`, `customerPhone`, `customerAddress`, `orderSourceName`, `createdByName`, `createdByEmail`, `updatedAt`, `updatedBy`).
2. **إلغاء customerAccountId نهائياً**:
   - إزالة جميع الإشارات والتحديثات لحقل `customerAccountId` من `Orders.tsx`, `EditOrderModal.tsx`, `orderPartyService.ts`, `orderPartyService.test.ts`, و `supabase-firebase-adapter.ts`.
   - الاعتماد الحصري والمطلق على الحقل المباشر `order_party_account_id` (`orderPartyAccountId`).
3. **الفحص والتحقق الأمني والتقني**:
   - اجتياز اختبار التجميع TypeScript `npx tsc --noEmit` بنجاح وتأكيد خلو الكود من الأخطاء تماماً.

---

## [2026-09-03 03:25:00] — الفصل التام والعزل الكامل بين المستخدمين والموظفين مع ميزة الربط الاختياري

### الإنجازات والتحسينات الرئيسية المنفذة:
1. **العزل التام لجدولي `users` و `employees`**:
   - تم تثبيت الفصل الكلي والكامل بين جدول حسابات مستخدمي لوحة التحكم `users` وسجل الموظفين والكادر البشري `employees`.
   - **مستخدم النظام (`User`)**: يختص بحسابات الدخول والصلاحيات والجلسات لراسمي القرار والكوادر بدون إنشاء أي حساب مالي في شجرة الحسابات (`accounts`) عند إنشائه نهائياً.
   - **الموظف (`Employee`)**: يختص بسجل الكادر البشري والرواتب والوظائف، ويتم إنشاء حساب مالي (2130-xxxx) له تلقائياً للرواتب، ولا يُنشأ له حساب دخول لوحة التحكم في `users` إلا بطلب مستقل.

2. **تثبيت وتعزيز ميزة الربط الاختياري (`linkedType` & `linkedEntity`)**:
   - الإبقاء على الخيار المرن لربط حساب مستخدم النظام بموظف (`linkedType: 'employee'`) أو مندوب (`linkedType: 'courier'`) أو إبقائه بلا ربط (`linkedType: 'none'`).
   - تحديث نماذج الإضافة والتعديل في `UserManagement.tsx` لتوفير واجهة ربط اختيارية واضحة وآمنة.

3. **تحديث واجهة `Users.tsx`**:
   - تجريد عمليات إضافة المستخدم من إدراج سجلات في `employees` أو استدعاء `financialAccountService.createAccountForEntity` تلقائياً.
   - إضافة التعليقات التوضيحية باللغتين العربية والإنجليزية لضمان معايير الكود النظيف والأمان.

4. **الفحص والتحقق**:
   - التأكد من اجتياز فحص التركيب والأنواع TypeScript `npx tsc --noEmit` بنجاح وتطابق بنسبة 100%.

---

## [2026-09-03 03:44:00] — إعادة هيكلة حقول المستخدمين وتجريدها من data وإسقاط العمود كلياً

### الإنجازات والتحسينات الرئيسية المنفذة:
1. **استخراج الأعمدة وتحديث قاعدة البيانات المباشرة**:
   - تنفيذ استعلام SQL بـ Supabase لاستخراج الحقول المعتمدة من كائن JSON `data` وتحويلها لأعمدة رئيسية صريحة لجدول `public.users`: (`fullName`, `password`, `systemPin`, `isRoot`, `phone`, `address`, `createdAt`, `updatedAt`, `lastSeen`, `lastSeenAt`).
   - إسقاط وحذف عمود `data` نهائياً من قاعدة البيانات بعد التجميع والترحيل الكامل بنجاح.
   - استبعاد وتجاهل الحقول المستغنى عنها: (`commissionRate`, `portalRole`, `approvalStatus`, `registrationSource`, `city`).

2. **تحديث محول النظام (`supabase-firebase-adapter.ts`)**:
   - ضم جدول `'users'` لـ `EXPLICIT_FINANCIAL_TABLES` لمنع التعامل التلقائي مع حقل `data` ملغي الوجود.
   - تحديث خريطة الأعمدة المباشرة `DIRECT_COLUMNS_MAP['users']` لتشمل الأعمدة الـ 17 المعتمدة فقط.

3. **تطهير واجهات المستخدمين (`UserManagement.tsx` و `Users.tsx`)**:
   - حذف عمود وحقول العمولة (`commissionRate`) والرواتب والحسابات الماليّة من واجهة مستخدمي لوحة التحكم لتأكيد العزل التام.
   - تعزيز معايير الكود النظيف والأمان بوضع تعليقات ثنائية اللغة توضح الهيكلية المباشرة.

4. **الفحص والتحقق الأمني والتقني**:
   - اجتياز اختبار التجميع والأنواع TypeScript `npx tsc --noEmit` بنجاح وتأكيد خلو النظام بنسبة 100% من أي أخطاء.

---

## [2026-09-03 04:00:00] — إصلاح خطأ 400 وتوافق أنواع التواريخ الرقمية BIGINT في محول Supabase

### الإنجازات والتحسينات الرئيسية المنفذة:
1. **تشخيص وتحديد Root Cause**:
   - كشف سبب فشل إنتاج طلبات `POST` و `PATCH` لجدول `users` بـ Bad Request (400) من Supabase PostgreSQL.
   - الخطأ كان ناتجاً عن قيام دالة `extractDirectColumns` بتحويل القيم الرقمية لحقول التوقيت (`createdAt`, `updatedAt`, `lastSeen`) إلى نص ISO زمني (مثل `"2026-09-03T00:44:14.274Z"`)، بينما نوع العمود في PostgreSQL هو `BIGINT` (أرقام صحيحة بالملي ثانية).

2. **تحديث المعالجة في `supabase-firebase-adapter.ts`**:
   - استثناء جدول `users` (والجداول ذات الأعمدة الرقمية لـ timestamps مثل `items_category` و `order_option` و `cust_details`) من التحويل السلسلي النصي لـ ISO.
   - تحويل القيم النصية أو الرقمية إلى أرقام صحيحة (`BIGINT`) متوافقة 100% مع هيكل PostgreSQL.

3. **الفحص والتحقق الكامل**:
   - اجتياز اختبار التجميع والأنواع TypeScript `npx tsc --noEmit` بنجاح وتأكيد انتهاء أخطاء الـ Console.

---

## [2026-09-04 04:30:00] — استكمال حقول الدفع والربط المحاسبي التلقائي بالصناديق والبنوك والدفع المتعدد

### الإنجازات والتحسينات الرئيسية المنفذة:
1. **مواءمة حقول الدفع في نماذج إنشاء وتعديل الطلب (`CreateOrderModal.tsx` & `EditOrderModal.tsx`)**:
   - التأكد من مطابقة حقول قسم الدفع مع نموذج سند القبض: وسيلة الدفع (نقد / بنك / آجل / متعدد)، اختيار حساب الصندوق القابض `cashAccountId` عند اختيار الدفع النقدي أو المتعدد، اختيار حساب البنك القابض `bankAccountId` ورقم المرجع البنكي `bankReference` عند اختيار التحويل البنكي أو المتعدد، حقول تجزئة الدفع المتعدد `cashAmount` و `bankAmount` عند الدفع المختلط، والتعامل التلقائي مع المبلغ المدفوع `amountPaid` والتفقيط باللغتين العربية والإنجليزية.

2. **توسيع الخيارات الديناميكية للقيود التلقائية (`payment_account_linked`)**:
   - إدراج الخيار الديناميكي `payment_account_linked` (حساب الدفع المختار بالطلب - صندوق/بنك/متعدد) ضمن مصفوفات `dynamicOptions` و `dynamicAccountOptions` في كل من `AutoVoucherRulesManager.tsx` و `OrderStatusManagementTab.tsx` ليتمكن مدير النظام من ربط أي قيد تلقائي بالطرف القابض المحدد في الطلب ديناميكياً بدلاً من الاعتماد على حساب الصندوق فقط.

3. **معالجة التحصيل المتعدد (Mixed Payment Allocations) في خدمة القيود التلقائية**:
   - تحديث دالة `executeAutoEntriesForStatus` في `autoEntryService.ts` لاستخلاص التوزيعات المتعددة `effectiveAllocations` تلقائياً من حقول `cashAmount` و `bankAmount` في الطلب عند اختيار طريقة الدفع المختلطة (Mixed)، وإنشاء قيدين تلقائيين مستقلين للدفعة المقدمة على حساب الصندوق وحساب البنك القابضين المحدد بالطلب.

4. **حفظ وتمرير حقول الدفع الكاملة في `Orders.tsx` والتحقق**:
   - إدراج حقول الدفع (`paymentMethod`, `cashAccountId`, `bankAccountId`, `bankReference`, `cashAmount`, `bankAmount`) في الحمولة المرفوعة لقاعدة البيانات عند إنشاء الطلب بـ `Orders.tsx`.
   - اجتياز اختبار التجميع والأنواع TypeScript `npx tsc --noEmit` بنجاح وتأكيد خلو النظام بنسبة 100% من أي أخطاء تركيبية.

---

## [2026-09-04 23:30:00] — إنشاء قيد مركب واحد للدفع المتعدد وإضافة زر ترحيل القيد التلقائي (autoPost)

### الإنجازات والتحسينات الرئيسية المنفذة:
1. **إنشاء قيد مركب واحد (Single Compound Entry) للدفع المتعدد**:
   - تم تحديث `autoEntryService.ts` لدعم توليد قيد مركب واحد بثلاثة أطراف متوازنة عند اختيار طريقة الدفع المتعدد (Mixed / Cash + Bank):
     - **الطرف المدين الأول**: حساب الصندوق القابض (`cashAccountId`) بـ `cashAmount`.
     - **الطرف المدين الثاني**: حساب البنك القابض (`bankAccountId`) بـ `bankAmount`.
     - **الطرف الدائن**: حساب العميل المالي (`customerAccountId`) بالمبلغ الكلي (`totalAmount`).
   - تم استدعاء `createCompoundFromLegacyVoucher` بـ `financialEntryService.ts` وإنشاء القيد تحت الفئة المحاسبية `Compound` وبفئة الوحدة `payment`.

2. **إضافة زر ترحيل القيد التلقائي ("ترحيل القيد" / `autoPost`)**:
   - إضافة خيار وتوجيه ترحيل القيد `autoPost?: boolean` إلى شاشات ومكونات القيود التلقائية (`AutoVoucherRulesManager.tsx` و `OrderStatusManagementTab.tsx`).
   - إظهار مفتاح التبديل التفاعلي "ترحيل القيد" (مفعّل افتراضياً للترحيل الفوري `posted` / وغير مفعّل للمسودة `draft`) مع شارة دالة على بطاقات القواعد (`مرحّل مباشرة` vs `مسودة فقط`).
   - حفظ حالة الخيار `autoPost` بجدول قواعد القيود التلقائية `auto_entries`.
   - الاعتماد على `rule.autoPost` في `financialAccountService.ts` و `autoEntryService.ts` لتوجيه حالة القيد الناتج تلقائياً إما إلى `posted` أو إلى `draft`.

3. **الفحص والتحقق الأمني والتقني**:
   - اجتياز اختبار التجميع والأنواع TypeScript `npx tsc --noEmit` بنجاح تام بنسبة 100% (0 أخطاء).

---

## [2026-09-05 00:00:00] — إصلاح حل مرجع عملة الحساب (curNo) للقيد المركب في القيود التلقائية

### الإنجازات والتحسينات الرئيسية المنفذة:
1. **معالجة وقائع عدم التطابق لمرجع عملة الحساب (`targetCurNo`)**:
   - تم التحديث في `financialEntryService.ts` لمنع إعاقة إنشاء القيد عند عدم توفر `target.curNo` أو عند إرساله `0`؛ حيث يتم استخدام التراجع التلقائي الآمن إلى معرف عملة الحساب المحلول من رمز العملة `accountCurrency.curId`.
2. **استعلام وتعيين تفاصيل الحسابات القابضة وحساب العميل حيوياً**:
   - تم التحديث في `autoEntryService.ts` للاستعلام وجلب كائنات الحسابات المالية الحقيقية عبر `financialAccountService.getAccountById` قبل بناء وتوجيه أسطر القيد المركب؛ مما يضمن توفير `curNo` و `currency` ونوع الكيان لكل طرف بدقة.
3. **الفحص والتحقق الكامل**:
   - اجتياز اختبار التجميع والأنواع TypeScript `npx tsc --noEmit` بنجاح وتأكيد انتهاء الاستثناء.

---

## [2026-09-05 03:06:00] — إكمال تطوير اختيار المنتجات وتأمين المنتجات والربط مع القيود التلقائية

### الإنجازات والتحديثات الرئيسية المنفذة:
1. **نافذة اختيار منتج من الكتالوج المسجل (`ProductPickerModal.tsx`)**:
   - بناء مكون مستقل يربط حيوياً بجدول `products` ويستعرض المنتجات المسجلة مع إمكانية البحث الفوري بالاسم أو الـ SKU أو الفئة أو الوصف واسترجاع كافة تفاصيل المنتج (السعر، الرابط، الوزن، الفئة، التغليف) إلى جدول منتجات الطلب.
   - إضافة زر "اختيار منتج من القائمة" في ترويسة جدول المنتجات بـ `CreateOrderModal.tsx` و `EditOrderModal.tsx`.

2. **رسوم إعدادات وتأمين المنتجات (`Product Insurance Fee Settings & Row Calculation`)**:
   - توسعة `SettingsContext.tsx` وحفظ إعدادات التأمين الافتراضية (`defaultProductInsuranceFee`, `defaultProductInsuranceType`).
   - إضافة خيارات الإدخال في واجهة الإعدادات `Settings.tsx` لاختيار نوع التأمين (مبلغ ثابت / نسبة مئوية).
   - إضافة خيار "تأمين المنتج" (`isInsured`) في أسطر المنتجات وحساب رسوم التأمين ديناميكياً لكل سطر، وإبراز شارة القيمة المحسوبة وتضمين مجموع رسوم التأمين في إجمالي الطلب ومراحل المالية والخلاصة.
   - حفظ حالة التأمين ورسومه في جدول `products` وإجمالي رسوم التأمين في جدول `orders`.


---

## [2026-09-06 02:10:00] — التنفيذ الشامل لخيارات نموذج إنشاء الطلب الأربعة (توصيل للمنزل، عبر مندوب شحن، الدفع لاحقاً، الحفظ والاعتماد مباشرة)

### التحديثات والتحسينات المنجزة:
1. **خيار "توصيل للمنزل" (`homeDeliveryEnabled`)**:
   - إضافة زر Checkbox "توصيل للمنزل" في قسم الشحن (Step 3) بـ `CreateOrderModal.tsx`.
   - **عند الاختيار**: إظهار حقل اختيار مندوب التوصيل "مندوب التوزيع النهائي (اليمن)" (`deliveryCourierId`) وحقل "رسوم التوصيل لليمن" (`deliveryCourierFee`) واحتساب قيمة رسوم التوصيل في إجمالي الطلب ومصارفات العملات.
   - **عند الإلغاء**: إخفاء حقل المندوب وحقل الرسوم وتصفير قيمتهما تلقائياً ومنع احتساب رسوم التوصيل في إجمالي الطلب.

2. **خيار "عبر مندوب شحن" (`viaShippingAgent`)**:
   - إضافة زر Checkbox "عبر مندوب شحن" في قسم الشحن (Step 3) بـ `CreateOrderModal.tsx`.
   - **عند الاختيار**: إظهار حقل اختيار مندوب الشحن "موظف التعبئة والتجميع (سعودي)" (`shippingCourierId`) وحقل "نسبة/رسوم مندوب الشحن (%)" (`shippingCourierFeeRate`) واحتساب قيمة عمولة المندوب السعودي في إجمالي الطلب ومجموع الربح السعودي `profitSaudiSAR`.
   - **عند الاختيار**: إظهار حقل "خصم تكاليف شراء المنتجات من حساب مندوب التجميع حالاً" (`deductSourcingCostFromCourier`) في قسم الدفع (Step 4).
   - **عند الإلغاء**: إخفاء حقل المندوب السعودي وحقل نسبة العمولة وتصفير عمولته تلقائياً، ومنع احتساب عمولة المندوب في إجمالي الطلب، وإخفاء خيار "خصم تكاليف شراء المنتجات من حساب مندوب التجميع حالاً" في قسم الدفع وتعيينه بـ `false`.

3. **خيار "الدفع لاحقاً" (`payLater`)**:
   - إضافة زر Checkbox "الدفع لاحقاً" في قسم الدفع (Step 4) بـ `CreateOrderModal.tsx`.
   - **عند الاختيار**: إخفاء حقول الدفع وتخطي التحقق الإلزامي منها وتحديد المبلغ المدفوع `0` بـ `paymentMethod = 'Deferred'`. تعيين حالة الطلب بأول حالة من جدول حالات الطلب الديناميكي `orderStatuses` (تسلسل 1 "طلب معلق") وتحديد حالة الدفع "Unpaid" (لم يتم الدفع).
   - **عند الإلغاء**: إظهار حقول الدفع وإلزام المستخدم بتفاصيل الدفع وتعيين حالة الطلب بالحالة الثانية (تسلسل 2) وحالة الدفع "Paid" (عند استلام المبلغ كاملاً) أو "Partial Paid" (عند استلام جزء من المبلغ).

4. **خيار "الحفظ والاعتماد مباشرة" (`directApprove`)**:
   - إضافة زر Checkbox "الحفظ والاعتماد مباشرة" في قسم الخلاصة والحفظ (Step 5) بـ `CreateOrderModal.tsx`.
   - **عند الاختيار**: اعتماد الطلب فوري فور الحفظ وتعيين حالة الطلب إلى الحالة الثالثة من جدول حالات الطلب الديناميكي `orderStatuses` (تسلسل 3 "معتمد").

5. **فحص الأنواع والتجميع (`TypeScript Verification`)**:
   - اجتياز فحص `npx tsc --noEmit --skipLibCheck` بنسبة 100% وبدون أي خطأ.

---

## [2026-09-06 04:31:00] — إصلاح احتفاظ الطلب بالحالة رقم 2 عند الدفع وحل خطأ الانتقال بين الحالات

### التحديثات والتحسينات المنجزة:
1. **تصحيح فحص المعرفات في المحول (`supabase-firebase-adapter.ts`)**:
   - تم تعديل فحص `order_status_id` في دالة `extractRowPayload` ليكون ديناميكياً يتقبل أي ID حالة غير فارغ بدلاً من الاعتماد على مصفوفة ثابتة `validStatusIds` كانت تستثني الحالة رقم 2 وتحولها تلقائياً إلى الحالة رقم 1 عند الحفظ في قاعدة البيانات.

2. **حل تعارض الانتقال بين الحالات وتحديث `Orders.tsx`**:
   - تعديل ترتيب أولويات استخراج الحالة الحالية للطلب المختار `selectedOrder.order_status_id || selectedOrder.orderStatusId` بـ `Orders.tsx` ليطابق القيمة الحقيقية المسجلة بعمود قاعدة البيانات ومنع حدوث خطأ `same_status` ("هذه الحالة مسجلة للطلب بالفعل").
   - تحديث مسميات ونصوص الحالة رقم 2 لتعرض اسمها العربي والبرمجي المعتمد من `orderStatusesList` عند تسديد الدفعة المقدمة (`amountPaid > 0`).

3. **إصلاح دالة التريجر `orders_history_from_orders` في PostgreSQL**:
   - تم إزالة المرجع الملغى `OLD.order_status` من شرط الفحص داخل التريجر وتثبيت الفحص على `OLD.order_status_id IS DISTINCT FROM NEW.order_status_id` لمنع رفع استثناء `record "old" has no field "order_status"`.

4. **تحديث السجلات الحالية بقاعدة البيانات via Supabase MCP (`@mcp:supabase`)**:
   - تشغيل استعلام SQL لتحديث الطلبات المسددة التي حفظت سلفاً بحالة 1 (`ALX-2609-1005`, `ALX-2609-1006`) وتعديل `order_status_id` إلى `'2'` ومزامنة كائن JSON التابع.

5. **فحص الأنواع والتجميع (`TypeScript Verification`)**:
   - اجتياز فحص `npx tsc --noEmit --skipLibCheck` بنجاح بنسبة 100% وبدون أي خطأ.

---

## [2026-09-06 07:15:00] — إعادة هيكلة الكتالوج الرئيسي للمنتجات وحركة البنود في بنود الطلبات (products & order_items)

### التحديثات والتحسينات المنجزة:
1. **جدول المنتجات الرئيسي (`products`)**:
   - تحويل جدول المنتجات ليصبح كتالوجاً رئيسياً ثابتاً غير مرتبط بأي طلب أو شحنة نهائياً، مع تخزين كافة المواصفات في أعمدة SQL صريحة (`product_id`, `product_name_ar`, `product_name_en`, `product_url`, `unit_price`, `item_category_id`, `is_allowed`, `cbm`, `width`, `height`, `length`, `weight`).

2. **جدول بنود الطلبات (`order_items`)**:
   - إنشاء واعتناد جدول بنود الطلبات لربط الطلب بالمنتج الرئيسي وتخزين تفاصيل الحركة الخاصة بالطلب (`items_id`, `order_id`, `product_id`, `product_price`, `quantity`, `total_price`, `total__weight`, `total_cbm`, `is_insured`, `insurance_fee`, `items_status`).

3. **خدمات المنتجات ومحول النظام**:
   - إنشاء `productService.ts` لدعم كافة عمليات CRUD، واحتساب عدد طلبات المنتج، وعرض حركات المنتج، وإجراء عملية الإرجاع للمنتجات المؤمنة (`is_insured=true`) بحالة `مرتجع`.
   - تحديث `supabase-firebase-adapter.ts` وتعيين `DIRECT_COLUMNS_MAP` وإدراج `products` و `order_items` ضمن `EXPLICIT_FINANCIAL_TABLES`.

4. **تحديث الصلاحيات وواجهة المنتجات والمودالات**:
   - تحديث `permissions.ts` و `Roles.tsx` بالصلاحيات السباعية للمنتجات وحركتها.
   - تحديث `ProductsManagementTab.tsx` بتبويبتين مستقلتين: **المنتجات الرئيسية** (Master Products) و **حركة المنتجات** (Product Movements).
   - تحديث `CreateOrderModal.tsx`, `EditOrderModal.tsx`, و `ProductPickerModal.tsx` لدعم اختيار واستدعاء المنتجات الرئيسية وحفظ أسطر الطلب بجدول `order_items`.

5. **فحص الأنواع والتجميع (`TypeScript Verification`)**:
   - اجتياز فحص `npx tsc --noEmit` بنسبة 100% وخلو المشروع تماماً من أي أخطاء.

---

## [2026-09-06 20:15:00] — تصحيح تعيين المفاتيح الرئيسية وقواعد الكتابة للجداول `products` و `order_items` في المحول

### التغييرات والإجراءات المنفذة:
1. **تشخيص المشكلة والسبب الجذري**:
   - عند إنشاء طلب جديد أو إضافة منتج رئيسي، كان المحول `supabase-firebase-adapter.ts` يقوم بإرسال حقل `id` ضمن حمولة الكتابة الموجهة لـ Supabase REST API (`POST /rest/v1/products`).
   - تبين أن المفتاح الرئيسي لجدول `products` في PostgreSQL هو `product_id` ولجدول `order_items` هو `items_id` ولا يوجد عمود باسم `id` في المخطط.
   - تسبب ذلك في خطأ Supabase REST API: `400 Bad Request` و `Could not find the 'id' column of 'products' in the schema cache`.

2. **تحديث `TABLE_PRIMARY_KEY_MAP` ودالة `getTablePrimaryKey` في `supabase-firebase-adapter.ts`**:
   - تم إنشاء خريطة موحدة للمفاتيح الرئيسية للجداول التي لا تستخدم العمود الافتراضي `id`:
     - `products` -> `product_id`
     - `order_items` -> `items_id`
     - `currency` -> `cur_id`
   - تحديث دوال `addDoc`, `addAssDoc`, `setDoc`, `updateDoc`, `deleteDoc`, `extractRowPayload`, `ensureCache`, `getDoc` لاستخدام اسم العمود الصحيح للمفتاح الرئيسي لكل جدول أثناء عمليات الاستعلام والإدراج والتحديث والحذف والمزامنة الحية.

3. **الالتزام بمعايير النظافة والأمان واجتياز التحقق**:
   - تم الحفاظ على مرادفات `id` و `product_id` و `items_id` في كائنات الذاكرة الحية لمنع حدوث أي كسر في مكونات React أو الخدمات التابعة.
   - التأكد من اجتياز فحص النمط وأنواع TypeScript عبر `npx tsc --noEmit` بنجاح وتوثيق التغييرات بالكامل.

---

## [2026-09-06 21:21:00] — تقييد اختيار المنتجات بالأصناف المسموحة واستكمال ملف الأوامر

### التحديثات والتعديلات المنفذة:
1. **تحديث `ProductPickerModal.tsx`**:
   - إضافة تصفية صارمة في مودال اختيار منتج من الكتالوج بحقل `is_allowed !== false` لضمان ظهور المنتجات المسموح بها فقط وفق كراسة الشروط.
2. **إنشاء ملف `user_commends.md`**:
   - توثيق نص التوجيه الصادر بالكامل مع التاريخ والوقت واسم نموذج الذكاء الاصطناعي المنفذ (`Antigravity / Gemini 3.6 Flash`).
3. **التحقق الشامل والتأكد من استقرار النظام**:
   - فحص TypeScript بـ `npx tsc --noEmit` للتأكد من انعدام أخطاء البناء والأنواع تماماً.

---

## [2026-09-06 21:37:00] — مطابقة واجهة ProductPickerModal مع جدول المنتجات الرئيسية products وخدمة productService.ts

### التحديثات والتعديلات المنفذة:
1. **شرح وتوضيح حقول `SystemProductRecord` القديمة**:
   - توضيح أن حقول `orderId`, `trackingNumber`, `quantity`, `isInsured`, `packagingOptionName`, `notes` كانت تخص التنسيق القديم المختلط قبل الفصل بين الكتالوج وحركات الطلبات.
   - تم ربط الواجهة بـ `Product` من `productService.ts` لتقتصر المودال على استدعاء وعرض المنتجات الكتالوجية المسموحة من جدول `products`.

2. **تحديث `ProductPickerModal.tsx` وربط المرادفات**:
   - إتاحة مرادفات الوصول (`product_id` / `id`, `product_name_ar` / `productName`, `unit_price` / `productPrice`) للتأكد من استقرار عملية التمرير واختيار الأصناف في `CreateOrderModal.tsx` و `EditOrderModal.tsx`.
   - مطابقة `ProductPickerModal.tsx` و `productService.ts` و `ProductsManagementTab.tsx` بنسبة 100%.

---

## [2026-09-06 22:06:00] — التحديث اللحظي للذاكرة المؤقتة (Cache) وإخفاء الطلبات المحذوفة فوراً بالواجهة بدون تحديث الصفحة

### الإنجازات والتحسينات الرئيسية المنفذة:
1. **تشخيص السبب المباشر لبقاء الطلبات المحذوفة بالواجهة حتى تحديث الصفحة**:
   - واجهة النظام تستمع لحركات جداول الطلبات والشحنات عبر `onSnapshot` الموفرة بـ `supabase-firebase-adapter.ts`.
   - عند تنفيذ إجراء الحذف الذري `deleteOrdersWithDependents` كانت الواجهة تستدعي الـ RPC المباشر `supabase.rpc('delete_orders_with_dependents')`.
   - خادم Supabase PostgreSQL كان يحذف البيانات فعلياً، لكن كاش الذاكرة المؤقتة المحلية `collectionCaches['orders']` والتخزين الاحتياطي `localStorage` لم يتم تحديثهما أو تنظيفهما عقب الـ RPC، ولم يتم إشعار دالة الاستماع `onSnapshot` فظلت تعرض قائمة الطلبات السابقة المخزنة بالذاكرة إلى حين إعادة تحويل/تحميل الصفحة.

2. **بناء دالة التحديث اللحظي للذاكرة `notifyOrderDeletionInCache` المحلية بـ `supabase-firebase-adapter.ts`**:
   - تم إنشاء وتصدير `notifyOrderDeletionInCache(orderIds)` لتصفية الطلبات والشحنات وبنود الطلبات المحذوفة فوراً من كائن الذاكرة المؤقتة `collectionCaches` وتحديث التخزين الاحتياطي بـ `localStorage`.
   - إشعار كافة مستمعي الشاشات `collectionListeners['orders']` و `shipments` لتشغيل التفاعلية اللحظية بدون أي تأخير.
   - إنشاء دالة `refetchCollection('orders')` لإعادة المزامنة الخلفية مع Supabase DB لضمان التطابق التام 100%.

3. **تحديث خدمة `orderDeletionService.ts`**:
   - استدعاء `notifyOrderDeletionInCache(normalizedIds)` و `refetchCollection('orders')` فور النجاح من الـ RPC لتختفي الطلبات المحذوفة لحظياً من شاشات المستخدم.

---

## [2026-09-06 22:25:00] — دعم التحويل التلقائي للقيد المالي بين عملتين غير افتراضيتين (Cross-Currency Vouchers)

### التحديثات والحلول الفنية المنفذة:
1. **تشخيص المشكلة والسبب الجذري**:
   - عند اختيار حساب دفع بعملة غير افتراضية (مثل USD أو SAR حين تكون عملة النظام Default YER وعملة الطلب YER أو SAR)، كانت الخدمة المحاسبية `financialEntryService.ts` ترمي خطأ `التحويل المباشر بين عملتين غير افتراضيتين يحتاج قيد صرافة صريحًا بمراجع سعر لكل عملة.`، مما يمنع إطلاق القيد التلقائي لدفعة الطلب.
2. **تحديث خدمة `financialEntryService.ts`**:
   - تم تحديث دالة `buildLegacyVoucherLine` لتقوم بجلب أسعار الصرف الرسمية للعملتين غير الافتراضيتين من جدول `cur_price` عبر `Promise.all([this.resolvePrice(cur1), this.resolvePrice(cur2)])`.
   - تحويل المبلغ تلقائياً من العملة الأصلية إلى العملة الافتراضية ثم إلى عملة الحساب المستهدف وإثبات مرجعي الصرف (`currencyPrice` و `accountCurrencyPrice`) بدون أي خطأ.
3. **تحديث مكون الواجهة `EntryForm.tsx`**:
   - تحديث دالة `resolveLinePayload` بجلب أسعار الصرف لكلا الساقين ديناميكياً عند اختيار عملتين غير افتراضيتين للقيد المالي.
4. **تحديث الاختبارات وتأكيد النجاح**:
   - تحديث `financialEntryService.test.ts` واجتياز اختبارات الوحدة بنسبة 100%.

---

## [2026-09-06 23:26:00] — حل خطأ القيد التلقائي المكرر `main_entry_entry_number_key` وتنظيف الاعتماد على الجداول القديمة

### التحديثات والحلول الفنية المنفذة:
1. **تشخيص المشكلة والسبب الجذري**:
   - عند تفعيل القيود التلقائية لطلب يحتوي على أكثر من قيد تلقائي (مثل `order_charge` و `order_down_payment`)، كان `financialAccountService.ts` يقوم بضبط `entryNumber = transaction.refNumber` (وهو رقم الطلب مثل `ALX-2609-1002`).
   - حاول القيد التلقائي الثاني استخدام نفس `entry_number = 'ALX-2609-1002'`، مما أدى لانتهاك قيد الفرادة في PostgreSQL ورخص استثناء `duplicate key value violates unique constraint "main_entry_entry_number_key"`.
2. **تحديث `financialAccountService.ts`**:
   - تم تعديل دالة `recordTransaction` لتضمن توليد `entryNumber` فريد دائماً (باستخدام `automationKey` إن وجد، أو توليد تسلسل فريد `JV-YYYYMMDD-XXXXXX`) مع الحفاظ على رقم الطلب والمرجع في حقل `refNumber`.
3. **تنظيف الاعتمادات التاريخية والجداول القديمة**:
   - تأكيد الاعتماد المباشر الكامل على الجداول المحاسبية الحديثة `main_entry` و `account_trans` وتأكيد خلو الخدمات المحاسبية المباشرة من الاستعلام المباشر عن `journal_entries` و `account_transactions`.
4. **التحقق والاختبار الشامل**:
   - اجتياز فحص `npx tsc --noEmit` واختبارات الخدمات `npx vitest run src/services/` (15 ملف اختبار و 77 اختبار بنسبة نجاح 100%).

---

## [2026-09-06 23:51:00] — حل عدم ظهور العمليات المالية وعمليات البنود في كشف حركة الطلب (`orders_history`)

### التشخيص والأسباب الجذرية:
1. **عدم إطلاق Trigger سندات القيود عند الإنشاء المباشر**:
   - الزناد `trg_orders_history_main_entry_financial` في قاعدة البيانات كان معرفاً كـ `AFTER UPDATE OF posting_status ON public.main_entry`.
   - القيود التلقائية التي تُنشأ بحالة مرحّلة مباشرة `posted` تستخدم `INSERT INTO main_entry` بدلاً من `UPDATE`؛ مما أدى إلى عدم تفعيل الزناد مطلقاً للعمليات المالية المباشرة.
2. **مطابقة الطلب حسب رقم الطلب (`order_number`)**:
   - دالة الزناد `orders_history_from_main_entry()` كانت تبحث عن الطلب باستعلام حقيقي مقيد بـ `WHERE o.id = v_order_id`. وعندما يكون `NEW.order_id` محتوياً على رقم الطلب (مثل `ALX-2609-1002`)، كان البحث يرجع `NULL` ويتوقف تسجيل الحركة.
3. **تعدد معايير الاستعلام في `orderHistoryService.ts`**:
   - دالة الاستعلام في الواجهة الأمامية `listForContext` كانت تجلب الحركات فقط بحقل `context.orderId`. وعندما تُسجّل الحركات باستخدام `orderNumber` كانت تُستثنى من قائمة العرض في المودال.
4. **نقص خريطة الأعمدة في المحول `supabase-firebase-adapter.ts`**:
   - الخريطة المباشرة `DIRECT_COLUMNS_MAP.orders_history` كانت تفتقد ربط `mainEntryId` (`main_entry_id`) و `accountTransCount` (`account_trans_count`).
5. **غياب الزناد التلقائي لحركات بنود الطلب (`order_items`)**:
   - عدم وجود زناد على جدول `order_items` لتتبع إضافات أو تعديلات أو حذف الأصناف التابعة للطلب.

### الحلول والتحديثات المنفذة:
1. **ترحيل المخطط لقاعدة البيانات (`202609060002_fix_orders_history_financial_and_item_triggers.sql`)**:
   - إعادة إنشاء `orders_history_from_main_entry()` لدعم كل من `INSERT` و `UPDATE` وتغيير مطابقة الطلب إلى `WHERE id = v_order_id OR order_number = v_order_id`.
   - تحديث الزناد `trg_orders_history_main_entry_financial` ليعمل `AFTER INSERT OR UPDATE ON public.main_entry`.
   - إضافة دالة `orders_history_from_order_items()` والزناد `trg_orders_history_order_items` على جدول `public.order_items` لتسجيل كافة أنشطة بنود الطلب تلقائياً في `orders_history`.
2. **تحديث خدمة الاستعلام `orderHistoryService.ts`**:
   - توسيع `listForContext` للاستعلام المزدوج بناءً على `orderId` و `orderNumber` و `shipmentId` مع معالجة آمنة لنتائج الاستعلام المسترجعة.
3. **تحديث خريطة الأعمدة في `supabase-firebase-adapter.ts`**:
   - إدراج `mainEntryId` و `accountTransCount` لتأكيد مزامنة وقراءة البيانات بشكل صحيح من Supabase.
4. **التحقق الشامل**:
   - اجتياز اختبارات الوحدة بـ `npx vitest run src/services/` (15 ملف اختبار و 77 اختبار بنسبة نجاح 100%).
   - التأكد من سلامة الأنواع عبر `npx tsc --noEmit` بدون أي خطأ.
