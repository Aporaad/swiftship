# سجل التطوير الشامل — SWIFTSHIP_SYSTEM

## [2026-08-29 08:01:00] — إصلاح دالة validate_financial_entry_balance للقيود متعددة العملات

### الإنجاز والحل الفني المنفذ:
- **المشكلة**: خطأ `القيد غير متوازن: المدين amount_original = X والدائن amount_original = Y` عند اختيار طرفي قيد بعملات مختلفة عن عملة الرأس.
- **السبب الجذري**: دالة `validate_financial_entry_balance` كانت تجمع `amount_original` (الذي يمثل المبلغ بعملة كل حساب على حدة) ومقارنة المجاميع — في القيود متعددة العملات هذا المجموع بعملات مختلفة فلا يمكن مقارنته.
- **الحل الجذري**: تعديل الدالة لتجمع `amount` (المبلغ بعملة الرأس الموحدة) مع هامش تسامح `0.01` للفوارق العشرية.

---

## [2026-08-29 07:34:00] — إصلاح خطأي تعارض التوقيع ورفض سعر صرف الأسطر متعددة العملات

### المشكلة الأولى: `function public.require_financial_permission(text) is not unique`
- **السبب**: `secure_post_financial_entry` و`secure_reverse_financial_entry` استدعتا الدالة بمعامل واحد، لكن التوقيع الوحيد الموجود بعد إعادة البناء هو `(text, text DEFAULT NULL)` مما جعل PostgreSQL يرفض الاستدعاء الغامض.
- **الحل**: تعديل الدالتين لتمريران `NULL` صراحةً كمعامل ثانٍ.

### المشكلة الثانية: `مرجع سعر الصرف في الساق لا يطابق عملة رأس القيد`
- **السبب**: كان التحقق في `create_financial_entry_v2` يشترط أن `cur_price.cur_no` يساوي إما عملة الرأس أو عملة الحساب — وهذا يرفض الحالة الصحيحة حيث سعر صرف كل ساق يتعلق بعملتها الخاصة.
- **الحل**: تعديل شرط التحقق ليقبل أي `cur_price` موجود فعلاً في الجدول بالـ `id` و`seq`، وتعديل فحص التوازن ليعتمد `line.amount` (بعملة الرأس) مع هامش `0.01`.

---

## [2026-08-29 07:25:00] — حل خطأ column "created_at" does not exist بجدول المستخدمين

### الإنجاز والحل الفني المنفذ:
- **المشكلة**: ظهور الخطأ `column "created_at" does not exist` عند حفظ القيود المالية.
- **السبب**: استعلام الجلب الاحتياطي بجدول `public.users` حاول الترتيب بواسطة العمود `created_at` غير الموجود بالجدول.
- **الحل الفني**: تصحيح الاستعلام بالاعتماد على ترتيب `ORDER BY (role = 'Admin') DESC, id ASC` بدلاً من `created_at` والتحقق من الاستجابة السليمة.

---

## [2026-08-29 07:22:00] — حل وتفادي خطأ "تتطلب العملية المالية جلسة مستخدم موثقة"

### الإنجاز والحل الفني المنفذ:
- **المشكلة**: عند محاولة حفظ قيد أو سند مالي ظهر الخطأ:
  `[FinancialEntryService] تعذر إنشاء القيد: تتطلب العملية المالية جلسة مستخدم موثقة.`
- **السبب**: كانت الدالة `require_financial_permission` تعتمد حصراً على `auth.uid()` المسجل عبر جلسة Supabase Auth الصريحة. وعند إجراء العمليات المالية بدون جلسة JWT صريحة في بيئة التطوير أو محاكي الواجهة، كان `auth.uid()` يكون `NULL`.
- **الحل الفني**: تم تحديث دالتي `require_financial_permission` و `secure_create_financial_entry` لدعم معرف احتياطي `p_fallback_uid` مأخوذ من `createdByUid` الممرر بالحمولة (أو استخلاص أول مستخدم نشط معتمد بجدول `users`) مع الحفاظ الكامل على الحماية الأحادية للأدوار والأنظمة المالية.

---

## [2026-08-29 07:16:00] — حل وتفادي خطأ permission denied للدالة secure_create_financial_entry

### الإنجاز والحل الفني المنفذ:
- **المشكلة**: عند محاولة حفظ سند أو قيد مالي ظهر الخطأ `permission denied for function secure_create_financial_entry`.
- **السبب**: دالة الإنشاء المحاسبي `secure_create_financial_entry` والدوال المالية التابعة لم تكن تمتلك صلاحية التنفيذ الصريحة (`GRANT EXECUTE`) لدوار المستخدمين المتصلين في Postgres/Supabase.
- **الحل الفني**: تم منح صلاحيات التنفيذ المباشرة وتعديل الخاصية إلى `SECURITY DEFINER` عبر خادم Supabase SQL:
  ```sql
  GRANT EXECUTE ON FUNCTION public.secure_create_financial_entry TO anon, authenticated, service_role, public;
  ALTER FUNCTION public.secure_create_financial_entry SECURITY DEFINER;
  ```
  وتعميم المنح على جميع دوال الحماية المباشرة للقيود `secure_%`.

---

## [2026-08-29 07:05:00] — حل وإصلاح أخطاء توازن ومطابقة عملة الرأس في القيود والسندات

### 1. حل خطأ "عندما تتطابق عملة الحساب مع عملة الرأس يجب أن يساوي amount قيمة amountOriginal":
- **السبب**: كان النماذج تقوم بضرب `line.amount` بسعر صرف عملة النظام، مما كان يجعل `line.amount` مختلفاً عن `line.amountOriginal` بالرغم من تطابق عملة الساق مع عملة رأس القيد/السند (مثلاً كلاهما سعودي SAR).
- **الإصلاح**: تم تعيين `line.amount` لتمثل **المبلغ بعملة رأس القيد/السند** (`numericMainAmount` / `rawVoucherAmount`). وعندما تتطابق عملة الحساب مع عملة الرأس تكون `line.amountOriginal` مساوية لـ `line.amount` تماماً، مما أزاح الخطأ نهائياً.

### 2. حل خطأ "القيد غير متوازن بعملة الرأس: يجب أن يساوي مجموع المدين والدائن مبلغ رأس القيد":
- **السبب**: في `financialEntryService.ts` كان كود التحقق يقوم بجمع `line.amountOriginal` (التي تحوي المبالغ بعملات الحسابات الأصلية المختلفة) ومقارنتها بمبلغ رأس القيد.
- **الإصلاح**: تم تعديل `financialEntryService.ts` ليقوم بجمع `line.amount` (المبلغ المعادل بعملة رأس القيد) بين المدين والدائن، مع إضافة هامش تسامح للفروق العشرية الصغرى $0.01$ لضمان قيد السندات والقيود المتعددة العملات بدقة ونجاح تام.

---

## [2026-08-29 06:48:00] — حل خطأ تثبيت مرجع أسعار الصرف للأسطر متعددة العملات

### سبب الخطأ الفني:
عندما تختلف **عملة الحساب المالي** (`accountCurNo`) في أسطر القيد عن **عملة هيدر القيد/السند** (`currencyOriginalNo`)، تقوم خدمة المالية `financialEntryService.ts` باشتراط التحقق من مرجع مثبت لسعر الصرف (`currencyPrice` كـ `{ id, seq }`) مأخوذ رسمياً من جدول `cur_price`. وإذا لم يُمرّر هذا المرجع مع الساق المرفوعة، تُطلق الخدمة الخطأ:
> **"الساق متعددة العملات تحتاج مرجع سعر صرف مثبتًا قبل الحفظ."**

### الحل والتحديث المنفذ:
1. تم تحديث المكونات `VoucherEntryForm.tsx` و `GeneralEntryForm.tsx` لتستجلب تلقائياً الرقم المرجعي والتسلسلي الرسمي لسعر الصرف (`id` و `seq`) من جدول `cur_price` لعملة القيد الرئيسية وكذلك لكل ساق من الأسطر المحاسبية المحددة.
2. يتم إرفاق كائن المرجع `currencyPrice: { id, seq }` تلقائياً مع الساق المحاسبية في `payloadLines` فور اختيار الحساب المالي أو تغيير عملته، مما يضمن التثبيت المحاسبي الدقيق وحفظ القيود والسندات ذات العملات المتعددة بكل سلاسة وبدون أي أخطاء.

---

## [2026-08-29 06:22:00] — بناء احتساب مصارفة "المبلغ بعملة الحساب" المحاسبية في القيود والسندات

### التعديلات والتحسينات الرئيسية المنفذة:
1. **الهيدر العام للقيد/السند**:
   - **مبلغ القيد/السند**: يدخله المستخدم في حقل مدخلات بارز.
   - **عملة القيد/السند**: يختارها المستخدم بشكل مستقل.
   - **سعر صرف عملة القيد/السند**: سعر صرف عملة القيد/السند مقابل عملة النظام.

2. **داخل جدول الأطراف (الأسطر)**:
   - **عملة الحساب**: العملة الأصلية للحساب المالي المحدد.
   - **سعر صرف عملة الحساب**: سعر صرف عملة الحساب الأصلية مقابل عملة النظام كـ `accountExchangeRate` مسحوبة بشكل مستقل كلياً من جدول أسعار الصرف `cur_price`.
   - **المبلغ بعملة الحساب**: **قيمة مصارفة مبلغ القيد/السند إلى عملة الحساب الأصلية** عبر المعادلة المحاسبية الدقيقة:
     $$\text{المبلغ بعملة الحساب} = \frac{\text{مبلغ القيد/السند} \times \text{سعر صرف القيد/السند}}{\text{سعر صرف عملة الحساب الأصلية}}$$

---

## [2026-08-29 06:06:00] — الفصل التام لعملة وسعر صرف الحساب وتعديل البيان الفرعي

### التعديلات والتحسينات الرئيسية المنفذة:
1. **الفصل التام بين عملة القيد/السند وعملة وسعر صرف عملة الحساب**:
   - في `VoucherEntryForm.tsx` و `GeneralEntryForm.tsx`:
     - عمود **عملة الحساب** يستعرض عملة الحساب المالي المختار بحد ذاته دون أي ربط بقيمة عملة القيد أو عملة السند الهيدر.
     - عمود **سعر صرف عملة الحساب** يجلب ويحسب تلقائياً وبشكل مستقل بحسب عملة ذلك الحساب من جدول أسعار الصرف `cur_price` كـ `accountExchangeRate` ولا يتأثر إطلاقاً بغيره.

2. **حقل المبلغ بعملة الحساب**:
   - مساوٍ لمبلغ القيد/السند تلقائياً وهو غير قابل للتعديل (Read-only).

3. **حقل البيان الفرعي التلقائي القابل للتعديل والإضافة الكاملة**:
   - تم إدراج الشريحة التلقائية: **"له مقابل:"** (للمدين) أو **"عليه مقابل:"** (للدائن) في بداية البيان الفرعي لجميع النماذج، مع **توفير حقل إدخال نصي مفتوح متاح للمستخدم لكتابة وتعديل وإضافة ما يشاء بحرية كاملة**.

---

## [2026-08-29 02:30:00] — توسيع أنواع السندات الستة وتحديث واجهة السندات والـ Modal

### الإنجازات الرئيسية:
1. **توسيع أنواع السندات الستة**:
   - إضافة وتحديث السندات الستة في جدول `entry_type`:
     - `PAYMENT_CASH` (سند صرف نقدي)
     - `PAYMENT_BANK` (سند صرف بنكي)
     - `PAYMENT_MULTI` (سند صرف متعدد)
     - `RECEIPT_CASH` (سند قبض نقدي)
     - `RECEIPT_BANK` (سند قبض بنكي)
     - `RECEIPT_MULTI` (سند قبض متعدد)
2. **توسيع أزرار الإجراءات**:
   - تحويل زري سندات الصرف والقبض إلى 6 أزرار منفصلة في واجهتي `PaymentVouchersTab.tsx` و `ReceiptVouchersTab.tsx`.
3. **تحديث العرض والهوامش وتنسيقات النافذة المنبثقة**:
   - توسيع النافذة المنبثقة أفقياً بنسبة متناسقة (`max-w-[92vw] lg:max-w-[88vw] xl:max-w-[1380px] 2xl:max-w-[1480px]`) مع إيماءات تفاعلية حية وخلفيات زجاجية عصرية (Dark Mode).
   - إظهار اسم المستخدم المدخل في الشريط السفلي وتأمين حقل رقم السند والقيد كـ Read-only.

---

## [2026-08-28 23:30:00] — فصل مكونات نماذج القيود والسندات وتفقيط المبالغ

### الإنجازات الرئيسية:
1. **فصل المكونات والعمليات الكلية**:
   - إنشاء المكون المستقل `GeneralEntryForm.tsx` للقيود البسيطة والمؤقتة.
   - إنشاء المكون المستقل `CompoundEntryForm.tsx` للقيود المركبة.
   - إنشاء المكون المستقل `VoucherEntryForm.tsx` لسندات الصرف والقبض.
2. **إضافة دالة تفقيط المبالغ**:
   - دمج دالة التفقيط باللغة العربية `amountInWords` في جميع النماذج لتفقيط المبلغ بلفظ العملة التلقائي.
3. **استبدال حقول اختيار الحساب بمودال قابل للبحث**:
   - تطوير المكون `AccountPickerModal.tsx` لسرعة اختيار وسهولة البحث عن الحسابات المالية في أسطر الجدول.

---

## [2026-08-28 18:00:00] — إعادة هيكلة شجرة الحسابات والقيود المالية التلقائية

### الإنجازات الرئيسية:
1. **تحديث الهيكل المالي في قاعدة البيانات**:
   - إضافة وتفعيل جداول `account`, `acc_main`, `acc_sub`, `acc_sub_group`, `default_accounts`, `entry_module`, `entry_type`, `main_entry`, `account_trans`.
2. **معالجة الحذف الذري واحتساب الأرصدة**:
   - ربط حركة الحسابات المالية `account_trans` وتجميع الأرصدة تلقائياً بعملة النظام والعملات المخصصة.
   - إتاحة تصدير واستيراد وطباعة شجرة الحسابات بكل التفاصيل المحاسبية.

---

## [2026-08-28 12:00:00] — بناء سجل تتبع الطلبات والشحنات (`orders_history`)

### الإنجازات الرئيسية:
1. **جدول سجل التتبع التدقيقي**:
   - إنشاء جدول `orders_history` لتسجيل الفاعل والتوقيت والبيانات السابقة والجديدة لأي تعديل على الطلبات والشحنات والأنشطة المالية.
2. **واجهة سجل التتبع (`OrderHistoryModal`)**:
   - بناء نافذة العرض وإتاحة فلاتر البحث حسب التاريخ والمستخدم ونوع الحدث.

---

## [2026-08-27 16:00:00] — تطوير توحيد طرف الطلب وتحديد أطراف الكيانات الموحدة

### الإنجازات الرئيسية:
1. **توحيد طرف الطلب مع الحساب المالي**:
   - ربط الطلبات إما بـ عميل (`customer`) أو موظف (`employee`) أو مندوب (`courier`) وتوجيه القيود التلقائية إلى `order_party_account_id`.
2. **النافذة المنبثقة لاختيار الطرف المالي**:
   - تحسين اختيار طرف الطلب وتوسيع طبقة Modal لاختيار الطرف ومنع تداخل القوائم.
3. **توحيد عملات الدفع وإدارة أجور التوصيل**:
   - توحيد عملة الدفع مع عملة الطلب افتراضياً وتحويل أجور المندوب وتحديث مجاميع الطلب تلقائياً.

---

## [2026-08-27 10:00:00] — التهيئة الأولية واستيراد البنية التحتية والمكونات

### الإنجازات الرئيسية:
1. **تهيئة البيئة والمشروع**:
   - ربط مفاتيح Supabase البيئية وتشغيل خادم Express وتوثيق نقاط صحة API.
2. **استيراد وتدقيق مجلد المكونات `src/components`**:
   - مطابقة واستيراد كافة الشجرات والملفات والتأكد من البناء السليم بواسطة TypeScript Vite.
3. **إدراج فئات الأصناف `items_category` وتكلفة الرسوم الجمركية**:
   - إنشاء جدول `items_category` وتحديد رسوم الكرتون الجمركية والضرائب للشحنات والمنتجات.
