# وثيقة التحليل الشامل لنظام إدارة الطلبات واللوجستيات

## 1. مقدمة النظام (System Overview)
يعتبر هذا النظام منصة متكاملة (ERP / Logistics Management System) لإدارة الطلبات، العمليات اللوجستية، سلاسل الإمداد، وعمليات الشحن المتكاملة (محلياً ودولياً). يهدف النظام إلى أتمتة دورة حياة الطلب منذ لحظة إنشائه وحتى تسليمه للعميل النهائي، مع تتبع دقيق للحالة المالية واللوجستية في كل مرحلة.

## 2. التقنيات المستخدمة (Technology Stack)
- **واجهة المستخدم (Frontend):** React.js باستخدام بيئة Vite.
- **لغة البرمجة:** TypeScript لضمان أمان الأنواع (Type Safety) وقوة الهيكلة.
- **التصميم وتنسيق الواجهات:** Tailwind CSS لبناء واجهات عصرية ومتجاوبة (Responsive Design).
- **إدارة الأيقونات:** مكتبة Lucide-React.
- **التدويل (i18n):** دعم كامل للغتين العربية والإنجليزية (Bilingual Support).

## 3. الوحدات والنظام الفرعي (System Modules & Components)
يتكون النظام من عدة واجهات ووحدات مترابطة تعمل معاً لضمان سير العمل:

### 3.1. لوحة القيادة (Dashboard)
- عرض إحصائيات عامة حول عدد الطلبات الكلي.
- تحليل الوضع المالي لمعرفة (إجمالي المبيعات، المبالغ المقبوضة، الديون المتبقية).
- رسم بياني لحالة الطلبات ونسب التوصيل الناجحة مقارنة بالمرتجعة أو القيد الانتظار.

### 3.2. إدارة الطلبات (Orders Management) - *الوحدة الأساسية*
تعتبر هذه الوحدة القلب النابض للنظام وتحتوي على التفاصيل الآتية:
- **إنشاء وتعديل الطلبات:** إدخال بيانات العميل، مصدر الطلب، وتوليد رقم تعريف فريد (Smart Key / Barcode).
- **إدارة محتويات الطلب (Items):**
  - جدول فرعي لربط الطلب بعدة منتجات/طرود.
  - تفاصيل كل منتج: الاسم، الكمية، السعر، الوزن (KG)، الحجم (CBM)، ورابط المنتج الخارجي.
- **إدارة المسارات اللوجستية وتفاصيل الشحن (Shipping Manifests):**
  - يمكن للطلب الواحد أن يمتلك أكثر من مسار شحن (Multiple Shipping Segments).
  - إمكانية تحديد نوع الشحن لكل مسار: 
    - 🚛 شحن بري (Overland Cargo)
    - ✈️ شحن جوي (Air Freight)
    - 🚢 شحن بحري (Ocean Cargo)
  - تتبع اسم الشركة الناقلة (Carrier).
  - تحديد مكان الانطلاق (بلد التصدير / Source) ووجهة الوصول (Destination).
  - الجدولة الزمنية: تاريخ تسليم الشحنة للناقل، المدة التقديرية للترانزيت، التوصيل المتوقع، والتسليم الفعلي المطلق.
  - التكاليف المالية لكل مسار: تكلفة الشحن الأساسية، ورسوم التغليف (اختياري).
- **الوضع اللوجستي الإجمالي للطلب:**
  - تم تسجيل الطلب (Invoice Saved)
  - وصل مستودع السعودية للتعبئة
  - جاري الشحن لليمن (براً / جواً / بحراً)
  - في التخليص الجمركي
  - وصل مركز التوزيع في اليمن (Final Depot)
  - مع المندوب للتوصيل
  - تم التسليم (Delivered Successfully)
  - ملغي (Cancelled)
- **الحالة المالية الشاملة للطلب:**
  - حساب إجمالي الفاتورة آلياً (شامل التكاليف وقيمة المنتجات).
  - تسجيل المدفوعات / الدفعات المستلمة (Settled Balance).
  - حساب المديونية المتبقية (Remaining Arrears).
  - دعم العملات المتعددة وسعر الصرف (YER, AED, SAR, USD).
- **طباعة وثائق الطلب (QR & Barcodes):** توليد باركود أو QR Code لتسهيل التتبع والفرز في المستودعات.

### 3.3. إدارة العملاء (Customers)
- قاعدة بيانات لتخزين بيانات جميع العملاء (الاسم، رقم الهاتف / الواتساب، العنوان التفصيلي / المحافظة).
- إمكانية إنشاء عميل جديد بخطوة سريعة (Quick Add) أثناء إنشاء الطلب.

### 3.4. الموردين ومصادر الطلبات (Sources)
- تصنيف المصادر التي يأتي منها الطلب (مثل: Shein، مصانع في الصين، متاجر إلكترونية).
- إعداد تسعيرات خاصة (Red Price Overrides) بناءً على المصدر.

### 3.5. المندوبين وشركات التوصيل الداخلية (Couriers)
- إدارة المندوبين المسؤولين عن توصيل الميل الأخير (Last-Mile Delivery) داخل محافظات اليمن.
- تتبع العهد المستلمة لكل مندوب وتسجيل رسوم التوصيل الداخلي لكل طلب.

### 3.6. تتبع الشحنات (Tracking)
- واجهة مخصصة لعرض مسار الشحنة بالكامل بناءً على رقم التتبع (Tracking Number) أو رمز الطلب.
- عرض الحالة بأسلوب الخط الزمني (Timeline) لتعزيز شفافية العملية للعميل والموظف.

### 3.7. إدارة المصروفات (Expenses)
- توثيق وتسجيل المصروفات التشغيلية للمؤسسة.
- تصنيف المصروفات (نقل، جمارك، تغليف، رواتب، تشغيل).
- ربط المصروفات بحسابات الأرباح والخسائر.

### 3.8. المستخدمين والصلاحيات (Users & Roles)
- إنشاء حسابات للموظفين بنظام الأدوار والصلاحيات (Role-based access control).
- تقييد الوصول للواجهات الحساسة (مثل التقارير المالية) بناءً على الصلاحية.

### 3.9. الإعدادات (Settings)
- إعدادات النظام العامة (لغة العرض، العملة الافتراضية، إعدادات الطباعة).
- أرشفة النظام والنسخ الاحتياطي.

## 4. دورة حياة الطلب والعمليات (Order Lifecycle Workflow)
1. **الاستلام والتسجيل (Order Entry):** يقوم الموظف أو النظام باستقبال الطلب من المصدر، إدخال المنتجات (Items) المرتبطة وربطها ببيانات العميل.
2. **التسعير الأولي:** يتم بناءً على الوزن / الحجم للمنتجات والتكاليف المتوقعة.
3. **تخصيص مسارات الشحن اللوجستي (Logistics Segmentation):**
   - يمكن إضافة مسار (بحري) من الصين لجدة.
   - ثم إضافة مسار (بري) من جدة إلى صنعاء.
   - لكل مسار تكاليف وتواريخ مستلقة تسحب أوتماتيكياً لحالة التتبع الشامل.
4. **التحديثات الميدانية (Status Updates):** يتم تحديث حالة الطلب كلما مر بمرحلة (الجمارك، الوصول للمستودع).
5. **المحاسبة والتحصيل (Collection):** يقوم المودع أو محاسب التسليم بقبض المبالغ وتحديث (المقدار المقبوض) عبر نظام "تحديث الفواتير".
6. **الختم والتسليم (Fulfillment):** يتم تسليم الطلب وإغلاق دورته اللوجستية والمالية مع تحويل الحالة إلى "تم التسليم".

## 5. ميزات وخصائص متقدمة (Advanced Features)
- **التصميم الزجاجي الأنيق (Glassmorphism):** واجهات عصرية بألوان داكنة (Slate / Gray / Obsidian) مع لمسات من الألوان الذهبية (Gold/Amber) والمضيئة لتميز الحالات.
- **قوائم فرعية ديناميكية (Dynamic Sub-tables):** لا حاجة للانتقال بين صفحات متعددة، كل شيء يخص الطلب (المنتجات، وتفاصيل الشحن) متوفر داخل جدول منسدل احترافي.
- **مؤشرات الأداء الآلية (Animated Indicators):** استخدام تأثيرات بصرية مثل النبض (Pulse) للحزم التي لا زالت في "الترانزيت"، وألوان محددة لكل نوع شحن (السيان للجوي، العنبر للبري، والنيلي للبحري).

## 6. متطلبات النظام والأداء (System Requirements & Performance)
- **توافق المتصفح (Browser Compatibility):** يعمل بكفاءة على المتصفحات الحديثة (Chrome, Safari, Edge).
- **التجاوب (Responsiveness):** تم بناء الواجهات بنظام (Mobile-first) لتعمل على الجوالات والأجهزة اللوحية دون فقدان لتجربة المستخدم.
- **تحديثات الحالة (State Management):** استخدام نظام متطور وحزم مكونات React (Hooks) لمعالجة التغيرات اللحظية وفتح/إغلاق المودالز بدون إعادة تحميل الصفحة.

*تاريخ ومسؤولية التوثيق:*
تم إنشاء هذا المستند كمرجع فني وإداري يغطي كافة النطاقات الوظيفية للنظام ولتوجيه المبرمجين والمدراء المستقبليين أثناء التوسع.
