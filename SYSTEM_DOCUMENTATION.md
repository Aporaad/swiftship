# وثيقة التحليل الفني واللوجستي الشامل لنظام إدارة الطلبات والخدمات اللوجستية (ERP Logistics Platform)

---

## 1. مقدمة وهيكل النظام العام (System Core Architecture)

### 1.1 فلسفة النظام وأهدافه الاستراتيجية
صُمم هذا النظام ليكون منصة متكاملة وذكية لتخطيط موارد المؤسسات (ERP) المتخصصة في الاستيراد والتخليص والفرز وإدارة سلاسل الإمداد اللوجستية والتسليم المحلّي النهائي داخل الجمهورية اليمنية. يعزز النظام الكفاءة والسرعة لشركات الشحن والوساطة الاستيرادية التي تربط بين مصانع ومتاجر المنشأ العالمية (مثل مصانع الصين، دبي، الرياض ومواقع التسوق كـ Shein) والمشترين المحليين.

أبرز الأهداف الاستراتيجية للنظام:
- **تتبع متعدد المراحل (Multi-leg tracking):** كسر القيود القديمة وتوفير آلية تسمح للطلب الفردي بامتلاك أكثر من شحنة ومسار ترانزيت مستقل ومتتالٍ (بري، جوي، بحري).
- **أمان ومطابقة الحسابات المالية:** القضاء التام على أخطاء تقريب العملات واختلاف الصرف بفضل محرك حساب مالي يدعم العملات المتعددة (YER, SAR, USD, AED) بأسعار صرف حية قابلة للتحرير.
- **تخصيص الصلاحيات وأدوار الموظفين:** الفصل الكامل للوظائف والبيانات لحماية السجلات المالية وعمليات التسليم وحظر المندوبين الخارجيين من البيانات الحساسة.
- **سرعة التشغيل وسهولة الاستخدام:** دعم كامل ومستمر للغتين العربية والإنجليزية، وتصميم لوحات تحكم مرنة تخدم المشرف في المكتب اللوجستي، والمناديب أثناء تفريغ الحاويات في الميدان.

### 1.2 البنية التقنية (Architecture Tech-Stack)
- **محرك الواجهات وتفاعل المكونات:** React 18+ بالاعتماد التام على المكونات الوظيفية (Functional Components) والخطافات (Hooks).
- **محرك البناء والتطوير السريع:** بيئة Vite فائقة السرعة مع المزامنة الذكية للمتصفح.
- **قوة هيكلة الأكواد وسلامة الأنواع:** TypeScript (TSX) لتصنيف الكائنات وإلزام مطابقة الأنواع لمنع انهيارات الواجهة.
- **تصميم وتنسيق الواجهات:** Tailwind CSS استناداً إلى تصميم داكن مضيء وعالي الوضوح، يدمج تأثيرات البطاقات الزجاجية (Glassmorphism UI) والتدرجات الجذابة لربط الحالات اللوجستية.
- **حركات التغيير والولوج الفوري:** مكتبة المؤثرات الحركية (Transitions) لتنظيم تبديل التبويبات والمودالات.
- **الرموز التعبيرية للأيقونات:** حزمة `lucide-react` المتكاملة للأيقونات المتجاوبة.
- **قاعدة البيانات السحابية الحية:** Google Firebase Firestore لتحديث وتخزين البيانات لحظياً بأسلوب الاستماع للأحداث (onSnapshot).
- **نظام التوثيق والمصادقة:** Firebase Authentication لضبط الحماية والدخول الآمن بالبريد أو الدخول الموحد من جوجل (Google OAuth).
- **مولد رموز الاستجابة السريعة:** مكتبة `qrcode` المدمجة لرسم الباركود على لوحة HTML5 قماشية تفاعلية.
- **محرك التقارير الحيوية:** مكتبة `jsPDF` لتوليد جداول الكشوفات وتحميلها كلدجر محاسبي بصيغة PDF.

---

## 2. الهيكل التفصيلي لكائنات البيانات والتكامل (Database Schemas & Firestore collections)

يعتمد النظام على هيكلية وثائقية مرنة عبر مجموعات Firebase Firestore لتثبيت وتراكم العمليات التشغيلية والمالية.

### 2.1 مجموعة الطلبات الرئيسية (Collection: `orders`)
يمثل المستند الواحد طلباً متكاملاً بجميع تفاصيله المالية، ومنتجاته، وبياناته اللوجستية:
- `id` (`string`): معرف التوزيع العشوائي التلقائي للوثيقة في Firestore.
- `orderNumber` (`string`): الرمز الذكي المولد تسلسلياً حسب الصيغة الأوتوماتيكية: `ALX-YYMM-NNNN` (حيث YY هو آخر رقمين من السنة، MM الشهر، و NNNN هو تسلسل يبدأ من 1001 يتزايد بناءً على العمليات السابقة).
- `customerId` (`string`): مرجع يربط الطلب بوثيقة العميل في مجموعة العملاء.
- `customerName` (`string`): الاسم الكامل للعميل لتسريع قراءة البيانات المباشرة.
- `customerPhone` (`string`): رقم هاتف/واتساب العميل للمطابقة وعمليات الإشعار.
- `customerAddress` (`string`): العنوان الإقليمي والمديرية الجغرافية للزبون.
- `orderSourceType` (`string`): نوع المنشأ وقاعدة الاحتساب اللوجستي، وتأخذ أحد الخيارين:
  - `'Factory'`: يمثل المصانع الكبرى والموردين بالجملة في الصين (حيث يعتمد التسعير على الوزن التجاري والحجم CBM).
  - `'App'`: يمثل طلبات مواقع التجارة الإلكترونية الاستهلاكية (مثل Shein وTaobao الخاضعة لعمولات وخصومات مباشرة).
- `orderSourceId` (`string`): المعرّف الخاص بقناة الشراء والربط مع مجموعة المصادر.
- `orderSourceName` (`string`): اسم القناة الشريكة (مثل: شي إن الرياض، مصنع شنزن للزجاج).
- `externalOrderNumber` (`string`): رقم تتبع سلة التسوق الخارجية للمطابقة مع بوابات التوريد.
- `trackingNumber` (`string`): رقم البوليصة العام للشحنة الدولية (الافتراضي هو رقم OrderNumber في حال لم يحدد من المورد).
- `shippingCompany` (`string`): شركة الطيران الدولي والتجميع الأولي (مثل Aramex, DHL, SafePost).
- `shippingCourierId` (`string`): معرف المندوب الناقل الخارجي والوسيط الإقليمي (في السعودية مثلاً).
- `deliveryCourierId` (`string`): معرف مندوب التوصيل الداخلي المعين لتوزيع الميل الأخير باليمن.
- `deliveryCourierFee` (`number`): تكلفة التوصيل المتفق عليها للمندوب المحلي بـ (YER)، تضاف للفاتورة الإجمالية وتسجل كـ (عهد) على المندوب. الافتراضي هو `4000 YER`.
- `currency` (`string`): العملة الأساسية للمشتريات وفحص سلة البضائع (`SAR` أو `USD`).
- `exchangeRateYER` (`number`): سعر تحويل الريال السعودي إلى الريال اليمني عند لحظة تجميد وحفظ الفاتورة (الافتراضي: `390`).
- `exchangeRateUSD` (`number`): سعر تحويل الدولار الأمريكي إلى الريال اليمني للمطابقة والاحتساب (الافتراضي: `535`).
- `bankCommissionRate` (`number`): نسبة رسوم العمولات البنكية وتحويل الحسابات الإلكترونية (الافتراضي: `3%`).
- `companyProfitRate` (`number`): عمولة الشركة ونسب الربح المستقطعة من القيمة الأصلية للشحنات الشخصية (الافتراضي: `12%`).
- `packagingFee` (`number`): رسوم التغليف الإضافية وصناديق الحماية المدخلة اختيارياً بالريال السعودي.
- `sheinRedPrice` (`number`): القيمة الاستبدالية المكتوبة بـ (السعر باللون الأحمر) المخصوم من متجر Shein ليتجاوز محرك الحسابات مجموع الفواتير الفرعي ويطبقها مباشرة كقيمة إجمالية.
- `totalWeight` (`number`): الوزن الكلي التراكمي لجميع طرود الشحنة بالكيلوغرام (مستنتج آلياً).
- `totalCBM` (`number`): الحجم الكلي التراكمي لطرود المنتجات بالمتر المكعب (مستنتج آلياً).
- `totalCostSAR` (`number`): التكلفة الكلية للطلب المحتسبة بالعملة الإقليمية (SAR).
- `totalCostYER` (`number`): التكلفة الكلية النهائية المطلوب تحصيلها بالريال اليمني (YER).
- `amountPaid` (`number`): إجمالي المبالغ المستلمة فعلياً من العميل بالريال اليمني لغاية اللحظة.
- `amountRemaining` (`number`): مديونية العميل المتبقية والمطلوب سدادها بالريال اليمني (`totalCostYER` - `amountPaid`).
- `paymentStatus` (`string`): التصنيف المالي للطلب بناءً على السداد التراكمي ويأخذ قيمة من ثلاث:
  - `'Paid'`: الفاتورة مستوفاة بالكامل (المديونية المتبقية <= 0).
  - `'Partial Paid'`: سداد جزئي (المبلغ المدفوع > 0 ولكنه لم يستوف كامل الفاتورة).
  - `'Unpaid'`: لم يدفع أي مبلغ (المبلغ المدفوع = 0).
- `orderStatus` (`string`): الحالة اللوجستية التراكمية العامة للطلب (انظر 2.1.3 لرحلة الحالات اللوجستية).
- `deliveryStatus` (`string`): الحالة الموضعية وتكليف الموزع في اليمن وتمثل حركية التوزيع:
  - `'في الانتظار'`: بانتظار التخصيص والفرز في مركز التوزيع الرئيسي.
  - `'جاري النقل'`: تحت الترحيل البري بين المحافظات.
  - `'تم التسليم'`: تم التوصيل للمستهلك النهائي وتوقيع المانيفست.
  - `'مرتجع'`: تعذر التسليم وإعادة الطرود للمستودع.
- `locationYemen` (`string`): العنونة والمكان الدقيق للطرد في اليمن (مثل: مركز التوزيع الرئيسي، مستودع الأصبحي، جمارك البيضاء).
- `internalNotes` (`string`): توجيهات محاسبية أو مذكرات داخلية خاصة بمنسق الشحنات وقسم المحاسبة والمناديب.
- `items` (`Array<Item>`): مصفوفة طرود بداخل الطلب (انظر 2.1.2).
- `shippingDetails` (`Array<ShippingRoute>`): مصفوفة مسارات الترانزيت الميداني الفعلي للطلب (انظر 2.1.1).
- `createdByEmail` (`string`): البريد الإلكتروني للموظف منشئ الطلب.
- `createdByName` (`string`): الاسم الفعلي لمحرر المعاملة لتوثيق الأمان الرقابي والعملياتي.
- `createdAt` (`number`): تاريخ ووقت الإنشاء الدقيق بـ Epoch milliseconds.
- `updatedAt` (`number`): تاريخ آخر إرسال أو تحديث طرأ على الكود.

#### 2.1.1 تفاصيل مسارات الشحن والترانزيت (Class: `ShippingRoute`)
يمتلك الطلب مصفوفة مدمجة داخلية تسمى `shippingDetails` لتسجيل عدة مسارات نقل متتابعة للطرود:
- `id` (`string`): معرف مولد فريد لكل مسار عند الإنشاء على واجهة المستعرض لتسهيل التحديث والحذف الفردي.
- `shippingType` (`string`): النمط والوسيلة اللوجستية للمسار المعين وتأخذ خياراً بديهياً من:
  - `'بري'`: الشاحنات البرية الحوضية العابرة للمنافذ والجمارك.
  - `'جوي'`: الترانزيت الجوي السريع في مطارات كارجو.
  - `'بحري'`: الحاويات البحرية الاقتصادية عبر السفن والناقلات العملاقة.
- `shippingCompany` (`string`): الناقل المسؤول عن تسيير المرحلة اللوجستية مثل (Aramex، شركة الرواد للنقل، خطوط الهلال البرية).
- `shippingSource` (`string`): محطة البداية والانطلاق للمرحلة (مثل: مستودع شنزن، جمرك دبي، منفذ الوديعة).
- `shippingDestination` (`string`): وجهة التسليم والتفريغ للمرحلة (مثل: مستودع جدة، مركز تجميع صنعاء، مستودع عدن الفرعي).
- `shippingDate` (`string`): تاريخ تسليم الشحنات ومستندات الفرز إلى الناقل الميداني.
- `shippingDuration` (`string`): المدة الزمنية التقديرية لإتمام المسار بـالأيام أو الأسابيع (مثال: "5-7 أيام").
- `expectedArrival` (`string`): اليوم والوقت التقريبي لإنهاء التوريد والترانزيت.
- `deliveryDate` (`string`): تاريخ التسليم الفعلي المدقق والموثق من قبل مستلم المحطة (قيمة فارغة أو نصية).
- `shippingCost` (`number`): تكلفة نقل هذه الشحنة بـالعملة الإقليمية (الريال السعودي SAR).
- `packagingFees` (`number`): رسوم الصناديق والتغليف والعزل والتأمين المرتبط بهذا الجزء الميداني بـالعملة الإقليمية (SAR).

#### 2.1.2 تفاصيل المنتجات والمحتويات (Class: `Item`)
لتجنب تجميع شواهد السلة في وصف واحد غامض، توجد مصفوفة داخلية تسمى `items` تصف تفاصيل المشتريات والوزن:
- `productName` (`string`): بيان السلعة أو الرابط بشكل كامل وواضح (مادة إجبارية تمنع تركها فارغة).
- `productUrl` (`string`): رابط معاينة المنتج الخارجي لمراجعة اللون والقياس وضمان الجودة.
- `quantity` (`number`): كمية وعدد القطع المتشابهة في العلبة (الافتراضي `1`).
- `productPrice` (`number`): سعر القطعة الواحدة بالعملة الأساسية للطلب المحددة في الأعلى (USD أو SAR).
- `weight` (`number`): القيمة الفردية للوزن المادي بالكيلوغرام (مفيد لحساب شحنات المصانع والوزن الإجمالي).
- `cbm` (`number`): الحجم الفردي للطرد بالمتر المكعب (مفيد لحسابات الشحن البحري للسلع العريضة والمصاعد).
- `trackingNumber` (`string`): رقم بوليصة فرعي فريد للمنتج للتتبع الداخلي.

---

### 2.2 مجموعة العملاء (Collection: `customers`)
يتم تخزين بيانات وملفات جميع المستوردين والمشترين الداخليين بشكل مستقل لتسهيل الربط السريع:
- `fullName` (`string`): اسم العميل المتكامل والمستخدم في الفواتير الورقية والممانيفستات.
- `phone` (`string`): وسيلة تواصل العميل والواتساب لإمداد رسائل التأكيد والإشعارات.
- `address` (`string`): عنوان الزبون والمحافظة المرجعية التي تخدم عمليات مناديب الميل الأخير.
- `notes` (`string`): معلومات إرشادية وتنبيهات مالية وإذا ما كان العميل ذو حساب VIP أو يحظر التعامل معه.
- `createdAt` (`number`): تاريخ إدخال العميل إلى ملف الحسابات.

---

### 2.3 مجموعة مصادر الشراء (Collection: `sources`)
تحدد منصات وقنوات الطلبيات لدراسة كفاءة الشركاء والربحية:
- `name` (`string`): اسم شبكة التجارة أو المصنع (مثل: Shein Outlet, Yiwu Supplier).
- `type` (`string`): نوع القناة وتأخذ: `'App'` (متاجر وتطبيقات تسوق معولمة) أو `'Factory'` (مصنع أو خط إنتاج توريدي للمواد الخام).
- `notes` (`string`): سياسات العمولات وأرقام الاتصال بالمشرفين في الخارج.

---

### 2.4 مجموعة المناديب وتتبع العهد اللوجستية (Collection: `couriers`)
توثق شبكة الموزعين الداخليين في محافظات اليمن لمراقبة الميدان وحصر العهد والحسابات:
- `fullName` (`string`): الاسم الكامل للمندوب.
- `phone` (`string`): رقم مندوب التوزيع للتواصل والتسليم.
- `governorate` (`string`): نطاق النفوذ الجغرافي والمحافظة الموكل له إدارتها (مثل: صنعاء الدائري، تعز الحوبان، ذمار، عدن).
- `activeOrdersCount` (`number`): عدد الطرود وباقات المانيفست التي لا تزال بعهدة المندوب ويقوم بتوزيعها حالياً.
- `notes` (`string`): تقييم المندوب أو نسب العمولات المالية ومواعيد توريد الصندوق الدورية.

---

### 2.5 مجموعة المصروفات والعهد العامة (Collection: `expenses`)
سجل الصرف الشامل بالملاحة والمستودعات والرواتب وتفاصيل فواتير التشغيل لتثبيت حساب التدفق النقدي:
- `expenseNumber` (`string`): الرقم التعريفي للسند الفريد المولد ألياً: `EXP-YYMM-NNNN`.
- `type` (`string`): نمط وتخصيص العملية المالية ويأخذ تصادقاً من ثلاثة جوانب:
  - `'General'`: شؤون إدارية، وقود، إيجار المكاتب، رواتب ومصاريف التخزين والتعبئة.
  - `'Custody'`: عهد مالية مسلمة لمناديب الميل الأخير لتغطية تذاكر السفر والنقل وتسهيل التوزيع.
  - `'FactoryPayment'`: مبالغ وأقساط مرسلة للمصانع الخارجية لتصفية الفواتير المعلقة.
- `amount` (`number`): قيمة السند المالي المصروف من الخزينة.
- `currency` (`string`): عملة معاملة المصروف (`YER` أو `SAR` أو `USD`).
- `recipientId` (`string`): مرجع يخص مندوباً معيناً في حالة تمويل عهد لوجستية.
- `recipientName` (`string`): اسم المندوب المستلم لمراقبة عهدته اللوجستية محاسبياً.
- `factoryName` (`string`): اسم المصنع الخارجي المتلقي للأرصدة في صفقات المواد الخام.
- `notes` (`string`): الشرح الإرشادي وتبرير الصرف المالي.
- `createdAt` (`number`): طابع زمني لحظي لتواريخ الصرف في الدفاتر الإدارية.

---

## 3. محرك المعادلات الحسابية والمالية التفصيلي لأسعار الصرف (Financial Calculation Engine)

يعالج النظام التحويلات المالية متعددة الأطراف والعملات بشكل آلي لمنع الفوارق الحسابية، استناداً إلى اختيار ومؤشرات المصدر ونوع الطلب.

### 3.1 معادلة حساب إجمالي المنتجات والطرود (Total Items Accumulation)
يحتسب النظام آلياً المجموع الفرعي الصافي لجميع القطع المدمجة داخل الطلبة بناءً على السعر الفردي للعملة والأعداد:
$$\text{Products Sum (Original Currency)} = \sum (\text{item.quantity} \times \text{item.productPrice})$$

### 3.2 معامل الفرز حسب نوع المصنع أو التطبيقات الاستهلاكية (Route Pricing Override)
يفصل النظام آلية التسعير وشحن المنشأ اللوجستي إلى منهجين تكنولوجيين:

#### أولاً: شحنات المصانع والشركات الجاهزة (Factory Source Strategy)
لا تعتمد المصانع على النسب المئوية، بل يتم شحن البضائع طبقاً للأوزان والأبعاد والحجم الذي يستغرق مساحات الحاويات والشاحنات:
1. **أوزان الشحن البري / الجوي وعمولتها:** يضرب النظام مجموع الأوزان الفعلية بـ عمولة ثابتة للنقل والخدمات الطردية تعادل `19 SAR` لكل كجم:
   $$\text{Weight Cost SAR} = \text{totalWeight (KG)} \times 19$$
2. **أبعاد الشحن البحري بالمتر المكعب (CBM):** يضرب النظام قيمة الحجم الفعلي للطرود بعمولة شحن بحري ثابتة تعادل `1400 SAR` للمتر المكعب الواحد:
   $$\text{CBM Cost SAR} = \text{totalCBM} \times 1400$$
3. **تحديد القيمة الفلترية للشحن الخارجي (Shipping Fee Global SAR):** يختار النظام تلقائياً القيمة الأكبر بين الوزن العيني والحجم الفعلي (Math.max) كعمولة شحن معتمدة لضمان عدم تعرض الشركة لخسائر المساحات في الحاويات:
   $$\text{Shipping Cost SAR} = \max(\text{Weight Cost SAR}, \text{CBM Cost SAR})$$
4. **احتساب رأس المال للمنتجات (Effective Price SAR):** تكون قيمة السعر الأصلية مساوية للمجموع المباشر لأسعار القطع:
   $$\text{Effective Price SAR} = \text{Products Sum}$$

#### ثانياً: شحنات تطبيقات التسوق العامة والشخصية (App Source Strategy)
تتميز سلال الشراء الفردية في منصات التسوق (مثل Shein) بالأنظمة التالية:
1. **خصومات شي إن باللون الأحمر (SHEIN Red Price Override):** تتيح واجهة النظام إدخال قيمة الخصم المعروض في المنصة مباشرة بالريال السعودي كبديل لمجموع السلة. إذا توفر هذا الحقل وتم إثباته بقيمة أكبر من الصفر، يستبدل محرك الحسابات مجموع المنتجات بديل السعر باللون الأحمر:
   $$\text{Effective Price SAR} = \begin{cases} \text{formData.sheinRedPrice}, & \text{if Shein source and sheinRedPrice} > 0 \\ \text{Products Sum}, & \text{otherwise} \end{cases}$$
2. **عمولة الشحن والنشاط التلقائية للشركة (Auto Shipping Rate):** تفرض الشركة عمولة ربح تشغيلية لشحن الطرود الطبية والعائلية تعادل نسبة مئوية (الافتراضي `12%` عبر متغير `companyProfitRate`) تضاف تلقائياً لقيمة الطلب كعمولة تغليف وشحن ونقل:
   $$\text{Shipping Cost SAR} = \text{Effective Price SAR} \times \left(\frac{\text{companyProfitRate}}{100}\right)$$

### 3.3 الضرائب والرسوم والتحويل البنكي (Taxes & Commissions)
- **رسوم الدفع والتحويل المصرفي:** يفرض البنك والوسيط المالي نسب تحويل تحاسب بالريال السعودي وتعادل الافتراضي `3%` من أصل السعر الأصلي للسلع:
  $$\text{Bank Commission SAR} = \text{Effective Price SAR} \times \left(\frac{\text{bankCommissionRate}}{100}\right)$$
- **إجمالي تكلفة المشتريات بالريال السعودي (Total SAR Record):**
  $$\text{Total Order SAR} = \text{Effective Price SAR} + \text{Shipping Cost SAR} + \text{Bank Commission SAR} + \text{packagingFee}$$

### 3.4 حوسبة سعر صرف الفاتورة بالريال اليمني (Final YER Invoicing)
يتم فحص وتحويل المبلغ الإجمالي إلى العملة المحلية بناءً على عملة المشتريات الرئيسية في مستند المعاملة:
$$\text{Total Cost YER} = \begin{cases} \text{Total Order SAR} \times \text{exchangeRateUSD}, & \text{if currency} = \text{'USD'} \\ \text{Total Order SAR} \times \text{exchangeRateYER}, & \text{if currency} = \text{'SAR'} \end{cases}$$

### 3.5 آلية حساب الربح وتوزيعه محاسبياً (Profit Sharing Formula)
تم برمجت النظام محاسبياً لتقسيم الأرباح التشغيلية الصافية في الشحنات الدولية بحيث يحصل للوسيط والشريك الخارجي في السعودية نسبة `30%` وللشركة في اليمن كأرباح إجمالية نسبة `70%`:
1. **حساب صافي الأرباح (Raw Profit SAR):**
   $$\text{Raw Profit SAR} = \text{Shipping Cost SAR} + \text{packagingFee} - \text{Expenses Margin}$$
   *(حيث Expenses Margin تعادل `0` في طلبات التطبيقات، وتعادل خصم `10 SAR` لكل كجم كأجور مستقطعة للطائرات والشاحنات في طلبات المصانع).*
2. **خصخصة الأرباح للشريك السعودي وتوثيقها:**
   $$\text{Profit Saudi (Region Partner) SAR} = \text{Raw Profit SAR} \times 0.3$$
3. **خصخصة الأرباح للشركة الرئيسية (Final Company Profit):**
   $$\text{Profit Company SAR} = \text{Raw Profit SAR} \times 0.7$$

---

## 4. المسار اللوجستي والميداني للطرود والشحنات (Logistics Status Lifecycle)

يمر الطلب لخدمة سلاسل النقل بخمس مراحل لوجستية متطورة يتم توثيقها لحظياً لدعم أنظمة التتبع:

```
                                      رحلة الشحنات والأثر الميداني للطلب
                                     ───────────────────────────────────
                                                      
                                            [ 1. تم تسجيل الطلب ]
                                             (بناء الفاتورة وحساب الصرف)
                                                      │
                                                      ▼
                                           [ 2. وصل مستودع السعودية ]
                                            (التحقق من الأوزان والأبعاد)
                                                      │
                                                      ▼
                                           [ 3. جاري الشحن لليمن ]
                                            (الترانزيت والناقل الدولي)
                                                      │
                                                      ▼
                                           [ 4. في التخليص الجمركي ]
                                            (المرور عبر المنافذ الجمركية)
                                                      │
                                                      ▼
                                         [ 5. وصل مركز التوزيع في اليمن ]
                                           (الفرز النهائي حسب المحافظات)
                                                      │
                                            ┌─────────┴─────────┐
                                            ▼                   ▼
                                 [ 6. مع المندوب للتوصيل ]  [ 7. ملغي ]
                                   (الميل الأخير وسداد الكاش) (توضيح أسباب الإرجاع)
                                            │
                                            ▼
                                       [ 8. تم التسليم ]
                                   (تصفية العهد وإغلاق الدورة كاملة)
```

1. **تم تسجيل الطلب واستخلاص الفاتورة (Invoice Saved):** تدوين الطلب وعقد المنتجات وحساب المبالغ المترتبة، بانتظار استلام المورد للبضائع.
2. **وصل مستودع السعودية للتعبئة (Arrived Saudi Packaging HUB):** وصول الإمدادات من المواقع الخارجية إلى مقر التجميع الإقليمي للشركة في السعودية للفرز وتثبيت الأوزان الفعلية وتغليف الطرود العائلية وحزم المصانع.
3. **جاري الشحن لليمن براً / جوأً / بحراً (In Transit to Yemen):** تسليم الطرد المجمع إلى وسيلة الشحن الدولية المعينة (بري/جوي/بحري) وانطلاق المسار مع توثيق اسم شركة النقل وتاريخ التحرك والتواريخ المأمولة للوصول.
4. **في التخليص الجمركي والأوراق (Customs Clearance):** توقف الناقلات والشاحنات في المنافذ والجمارك لإجراءات التدقيق الضريبي، وفك الشارات وتسليم التراخيص الجمركية.
5. **وصل مركز التوزيع والفرز النهائي (Arrived Final Depot):** تسلم الطرود في مركز المعالجة والتوزيع الوطني في اليمن (مستودع صنعاء الرئيسي أو ديوان عدن) وتصنيفها حسب موقع المحافظات للتهيئة لوضع التوصيل.
6. **مع المندوب للتوصيل بانتظار التسليم (Out for Final Delivery):** إسناد الطلبات المعتمدة مع العهود المالية وقيمة التوصيل للمندوبين المحليين في قطاع العميل. يتحرك الطلب بعهدة للموزع لتسليم الميل الأخير للمستهلك الفعلي.
7. **تم التسليم وتفصيل العهد الموردة (Fulfillment & Settlement):** إتمام توصيل الطرود واستلام القيمة المالية كاش أو تحويل، وتوريد أموال الفاتورة وأجرة الموزع إلى حظر وصندوق الشركة في اليمن، لتتغير حالتها كـ (مستلمة ومغلقة).
8. **ملغي (Cancelled):** إيقاف المعاملة كلياً نظراً لإلغاء المشتري من العميل أو خلل من المورد الدولي، وتسجل باللون الرمادي في أرشيف الشركة.

---

## 5. واجهات النظام ومكونات الأداء الحية (UI Modules & Component Specifications)

تتفاعل واجهات التطبيق ومودالاته بتناغم ديناميكي عالي وتصميم يعبر عن بيئة العمل المعاصرة:

### 5.1 واجهة لوحة الأداء والإحصائيات الحيوية (Dashboard Portal)
- **بطاقات قياس الأداء المالي اللحظي (Financial KPI Badges):** 4 بطاقات زجاجية تتبدل أرقامها تلقائياً مع الفلترة:
  1. *إجمالي حجم المعاملات المالي:* المبيعات والطلبيات المطلوبة بالريال اليمني لمتابعة الحجم المحقق.
  2. *المبالغ المستلمة والمحصلة دفترياً:* المقبوض الفردي والتراكمي الفعلي في الصناديق.
  3. *الديون العالقة للتحصيل:* الفجوة والمديونية العالقة وتنبيه قسم التحصيل بمقدار العجز الكلي.
  4. *الطلبات المفعّلة لوجستياً:* مجموع الطرود الجاري شحنها وتوزيعها في الميدان حالياً.
- **لوحات الرصد الرسومي والرسوم التفاعلية (Recharts Visualizer):** يظهر رسماً تفاعلياً يعرض نسب النجاح والطلبات المسلمة مقارنة بالشحنات المتوقفة والملغية، لتحليل مدى سرعة التوزيع لكل مندوب وقطاع لوجستي.

### 5.2 واجهة إدارة الطلبيات وإجراءات الفرز اللوجستي (Orders Management Area)
أيقونة العمل والمحطة الأكثر نشاطاً في النظام، وتتكون من:
- **شريط البحث والتصفية المتقدمة (Multipurpose Search & Live Filters):**
  - خيار البحث بالاسم الفعلي للعميل، أو رقم تتبع بوليصة الطيران الخارجي، أو رقم وتساب الزبون، أو رمز الشريحة لشي إن.
  - فرز الطلبات حسب حالتهم اللوجستية العامة (بانتظار مستودع السعودية، قيد الترانزيت البري والجوي، مع المناديب، تم الفرز اللوجستي والتصنيف).
  - تحديد الطلبات بناءً على قنوات التوريد والمصادر، أو مندوبي النقل المحلي.
- **جداول الطلبات القابلة للانسدال والتمدد (Collapsible Order Rows Grid):**
  عند النقر على أي معاملة في الجدول، تنسدل شريحة بيانات ثانية تكشف تفريعات غاية في الأهمية:
  1. *مصفوفة قائمة البضائع والمشتريات:* جدول داخلي يعرض اسم المادة، ورابط شي إن التفاعلي، وسعر الدولار، ووزن الكرتون والحجم.
  2. *لوحة تتبع الرحلات اللوجستية لعدة مسارات (Sub-Tracks Interactive Route Manager):*
     يظهر كل مسار شحن قمنا بتخصيصه، مع مؤشرات ألوان ذكية لكل نمط:
     - ✈️ **الشحن الجوي:** تمثيل بلون سيان مشع (`#06b6d4`) لتعيين رحلات الكارجو الجوية السريعة.
     - 🚛 **الشحن البري:** تمثيل بلون ذهبي عنبري دافئ (`#f59e0b`) لتحديد بواخر الإمداد الترانزيت البري.
     - 🚢 **الشحن البحري:** تمثيل بلون نيلي عميق (`#4338ca`) لتعيين الحاويات الطويلة.
     - *أيقونة نبض الترانزيت اللحظية (Pulsing Dot Indicator):* في حالة وجود مستند ترانزيت لم يسجل بـ تاريخ وصول، تنبض نقطة بلون مشع لتبين أن الشحنة قيد الحركة الميدانية الآن في هذا الطريق.
  3. *لوحة دفعات التحصيل المالي وجدول السداد:* تفصيل بالتواريخ عن الدفعات المستلمة والمقبوضة، تذكرة تذكرة، مع المبلغ المتبقي لكل طلب لتأكيد السداد الكامل.

---

## 6. التفاصيل الكاملة للمودالات والنوافذ التفاعلية (Modal Actions & Workflows)

يقوم النظام باستدعاء نوافذ ومودالات ذكية للتحكم بالعمليات وإجراء الإضافات والتحديثات بدون الحاجة لإعادة توجيه الصفحة:

### 6.1 مودال إنشاء طلب جديد متكامل (Create Order Modal)
- **منطقة اختيار وتعريف العميل (Quick Guest Registration):**
  يتيح النظام اختيار عميل مخزن مسبقاً، وإذا لم يكن الزبون متوفراً، يوفر النظام زراً سريعاً يسمى `➕` بجانب الحقل، عند النقر عليه، ينفتح مودال فرعي بداخل مودال الطلب لإضافة عميل جديد وكتابة اسمه ورقم وتسابه وعنوانه وحفظه ليوضع تلقائياً كملف محدد في استمارة الطلب، مما يلغي التشتت والانتقال المربك بين الفهارس.
- **منطقة تفويض قناة الشراء وسعر الصرف:**
  رسم مدخلات سعر الصرف اللحظي للريال السعودي والدولار، وعائد الخصم الفوري لشي إن، وعمولات الشركة والتحويل البنكي والتامين الخاص بالتغليف.
- **محرر الطرود والمنتجات المكرر (Dynamic Product Rows Grid):**
  القدرة على النقر على كبسولة `أضف صف منتج` لفتح أسطر إدخال تفاعلية غير محدودة لإثراء محتويات الطرد ومجموع القطع دون إغلاق الصفحة.
- **محرر مسار الشحن الدولي البدئي:**
  إدخال تفاصيل بوليصة الشحن الخارجي وبدايات مسارات الترانزيت وتكلفتها فوراً لتكتمل الفاتورة فور الإرسال.

### 6.2 مودال تحويل الحالة وتحديث مسارات الترانزيت (Fulfillment & Segment Track Modal)
شاشه ديناميكية ومقسمة محاسبياً ولوجستياً للتحكم بالكامل ببيانات الطلب المنشأ:
- **معدل الحالة العامة ومكان تواجد الشحنة في اليمن:**
  تغيير شامل لحالات التوصيل وإسنادها للمندوب وتحديد تكلفته لتصنيف العهد على المناديب، وكتابة موقع ومدينة تواجد الحزمة والطرود حالياً والملاحظات والموانع المحاسبية المبرمجة للموزع.
- **محرر مسارات النقل اللوجستي وتحديث شرائح الترانزيت (Logistics Segments Interactive Editor):**
  جدول توسيعي في المودال يسمح بالتحديث الفوري والديناميكي لخطوات الشحن. يتيح إضافة مسار جديد، أو تعديل مسار كـ تاريخ تسلم أو ناقل أو تذكرة شحن، أو حتى حذف مسار، وعند نقر حفظ يعاد محاسبة الفاتورة والربح لحظياً في قاعدة البيانات لإغلاق فوارق الصفقات والحسابات البرية والجوية والبرية.

### 6.3 مودال قبض دفعات مالية فرعية (Collect Installments Modal)
يتيح للمحاسبين فتح نافذة قبض سريعة لتوريد دفعة مالية جديدة من العميل للطلب:
- كتابة القيمة المقبوضة بالريال اليمني وتحديد نمط القبض (كاش، حوالة بنكية، محفظة إلكترونية كـ جوالي أو كريمي).
- كتابة رقم مرجع الإيداع للحوالات لتدقيق البنوك، ليتم آلياً فور الحفظ تقليل الدين المتبقي لزبون، وتحويل الحساب المالي لمدفوع بالكامل إذا استوفيت الرسوم وتوليد إشعار فوري.

### 6.4 مودال عرض تفاصيل التتبع والباركود والخط الزمني (Fulfillment Detail & QR Visualizer)
تحفة تصميمية مميزة لعرض تتبع الطرود بالخطوط الزمنية الأنيقة:
- **توليد رمز الاستجابة السريعة (QR Code Module):** بالاستناد لخطاف التحديد الفني وقراءة رقم التتبع المكتوب في الطلبيات، يستدعي النظام واجهة رسم الخرائط للباركود ليرسم بدقة وسرعة بوليصة تتبع ثنائية الأبعاد على سطح لوحة الخصائص `canvas` بدقة عالية تسمح لموظفي المستودعات والتخزين بتوجيه كاميرات الجوالات التفتيشية والحصول على ملف الطلب الحقيقي في ثانية من المتتبع.
- **تأثير النبض التفاعلي للمسار اللوجستي الحركي (Pulsing Timeline Dot):**
  تمثيل الخط الزمني للتتبع بجدول نقاط زجاجية متتابعة تبيّن للمستورد المحطة المكتملة وتنبض باللون الأصفر الدافئ والمنير للحث اللحظي لموقع وقوف الطرود الحالي الآن مما يعزز الرضا التشغيلي وثقة المتعاملين بالمنصة.

---

## 7. آلية توطين اللغات المتوازنة (Bilingual Adaptability & RTL Setup)

### 7.1 توجيه الشاشه والمحاذاة التلقائية (Dynamic Orientation Alignment)
تم تصميم النظام ليكون مستعداً للتوطين والعمل بالشرق الأوسط والدول الغربية بمرونة فائقة:
- في حالة تغيير وضع اللغة إلى **اللغة العربية (RTL):**
  - تفعيل اتجاه الواجهة ليكون من اليمين إلى اليسار تلقائياً (`dir="rtl"`).
  - ضبط تباعد الأيقونات ومحاذاة القوائم ومواقع الفلتر لتنسجم مع عادات المشاهد العربي.
  - دعم الترجمة الاحترافية لجميع المدخلات اللوجستية والمالية مع تفعيل عائلة الخطوط العربية ذات الحضور الرائع والأنيق المتناسق مع الشاشات الصناعية ومستودعات التوزيع.
- في حالة الانتقال إلى **اللغة الإنجليزية (LTR):**
  - تغيير اتجاه المعاينات والسطور ليكون من اليسار إلى اليمين (`dir="ltr"`).
  - تثبيت عارض وعائلة الخطوط المنظمة (مثل Inter وJetBrains Mono) وعرض الأرقام والبيانات بروعة فائقة للمستخدم الأجنبي ليكون دفتراً مالياً مريحاً للنظر.

---

## 8. دليل حلول وتطوير المشكلات الفنية للشاشات (Developer Operations & Controlled Inputs Guide)

يتفهم النظام متطلبات الريأكت المعيارية لتجاوز الأخطاء البرمجية الشائعة التي تعصف بالواجهات التفاعلية لحظياً:

### 8.1 حماية مدخلات النظام من القيم الفارغة وسلوك Uncontrolled Inputs
لمنع حدوث أخطاء تعارض الريأكت (A component is changing an uncontrolled input to be controlled) والذي ينشأ من تحول حالة الخصائص بمدخلات الواجهات من قيم فارغة (`undefined` أو `null`) لقيمة معرفة ومقروءة فجأة، يضمن النظام حزام أمان قاطع وصارم يتمثل في تخصيص حوافظ وحواضن قيم بديلة في جميع الواجهات:
```tsx
// حزام الأمان الفني في واجهة الطلبات والمسارات لمنع غياب القيم الافتراضية
value={sh.shippingCompany || ''}
value={sh.shippingSource || ''}
value={sh.shippingDestination || ''}
value={sh.shippingDate || ''}
value={sh.shippingDuration || ''}
value={sh.expectedArrival || ''}
value={sh.deliveryDate || ''}
value={sh.shippingCost || 0}
value={sh.packagingFees || 0}
value={item.productName || ''}
value={item.productPrice || 0}
value={item.quantity || 1}
value={item.weight || 0}
value={item.cbm || 0}
```
يضمن هذا السلوك ثبات مظهر الشاشات الفنية وخلو متصفح المشغل والمستودع من التحذيرات أو الانهيامات الكودية، ويحافظ على دقة عمل الخلايا والعمليات لحظة بلحظة.

### 8.2 تصفير وتنظيف النمذجة (Clean Reset Workflows)
عند الحفظ الناجح لأي معاملة، يتم تصفير النماذج لقيمها الافتراضية الصحيحة (تصفير مصفوفة المنتجات وحزمة المسار المبدئي إلى 'بري'، وتصفير المودالز، ومسح الأوزان الإجمالية)، مع توليد الإشعارات السريعة لخدمة سلاسل النقل اللوجستية.

---

**وثيقة التحليل الفني واللوجستي الشريكة للنظام اللوجستي الذكي - 2026.** مخصصة لإرشاد وتطوير وتأسيس النماذج والتوسعات في ديوان حسابات وخدمة سلاسل التوريد.
