# تقرير إكمال إعادة هيكلة الحسابات المالية

## نطاق التنفيذ

تم تنفيذ إعادة الهيكلة مباشرةً داخل المصدر المحلي الرسمي:

`F:\system\swiftship-tracker\swiftshift2\SWIFTSHIP_SYSTEM`

أُنشئت شجرة الحسابات الهرمية وقاعدة الحسابات الافتراضية، مع الحفاظ على الحسابات والقيود والبيانات القائمة. لم يُنشأ أي رصيد افتتاحي أو قيد أو سعر صرف مُصطنع خلال الترحيلات.

| المجال | النتيجة المنفذة |
|---|---|
| الشجرة | جداول `account` و`acc_main` و`acc_sub` و`acc_sub_group`، مع حسابات مالية ورقية في `accounts` فقط. |
| العملات | `cur_no` مرجع إلى `currency.cur_id` في الحسابات والسندات والحركات المالية المدعومة، مع الاحتفاظ المؤقت برمز العملة النصي للتوافق. |
| المعرفات | أصبحت جميع معرفات الحسابات الحالية هي كود الحساب نفسه بصيغة `NNNN-NNNN` بلا بادئة `acc_`. |
| الحسابات الافتراضية | جرى ربط مفاتيح النظام في `default_accounts` بحسابات ورقية نشطة، وأصبح حلها من قاعدة البيانات. |
| الضوابط | قاعدة البيانات تمنع الترحيل إلى حساب معطّل أو عقدة تنظيمية، وتفرض تطابق مرجع العملة وسقف الرصيد الطبيعي. |
| الأرصدة | يُعاد احتساب رصيد الحساب من حركاته، وتُجمع أرصدة طبقات الشجرة بعملة النظام الافتراضية باستخدام آخر أسعار `cur_price`. |
| الواجهة | إضافة إدارة الشجرة والحسابات الافتراضية، وتسلسل اختيار الآباء، وتوليد الأكواد، وحصر القوائم المالية في الحسابات الورقية النشطة. |

## ملفات الترحيل الرئيسية

| الترحيل | الغرض |
|---|---|
| `202608270030_create_accounting_hierarchy_schema.sql` | إنشاء طبقات الشجرة و`default_accounts` وتوسعة `accounts`. |
| `202608270035_add_account_group_entity_bindings.sql` | ربط مجموعات الحسابات ديناميكيًا بأنواع الكيانات وإتاحة الحسابات المباشرة. |
| `202608270040_add_accounting_nature.sql` | إضافة طبيعة الحساب المحاسبية. |
| `202608270045_add_accounts_balance_column.sql` | إضافة الرصيد المباشر للحساب المالي. |
| `202608270050_seed_accounting_hierarchy.sql` | زرع شجرة أولية قابلة للتكرار دون قيود أو أرصدة. |
| `202608270055_classify_existing_accounts.sql` | تصنيف الحسابات الحالية وربطها بالمسار والعملة والرصيد. |
| `202608270060_bind_default_accounts.sql` | ربط الحسابات الافتراضية بحسابات النظام القائمة. |
| `202608270065_normalize_account_natures.sql` | توحيد طبيعة الحسابات الحالية. |
| `202608270070_rekey_accounts_to_account_codes.sql` | ترحيل المعرّفات والمراجع المباشرة وبيانات JSON مع خريطة تدقيق `account_id_migration_map`. |
| `202608270075_enforce_accounting_posting_guards.sql` | إضافة مراجع العملات والمشغلات الذرية وإعادة الاحتساب والتجميع. |

## نتائج التحقق

تم التحقق حيًا من وجود **89** حسابًا مُوحَّد المعرف، وتطابق جميعها مع أكوادها المحاسبية، ووجود **89** صفًا في خريطة الترحيل. لم تظهر مراجع معزولة في الحسابات الافتراضية أو حركات الحسابات أو القيود أو الطلبات أو الأصول. كما تطابقت أرصدة جذور الشجرة الخمسة مع مجموع أرصدة الحسابات الورقية المحولة إلى عملة النظام، بفارق حسابي صفري.

تعمل مشغلات قاعدة البيانات للحركات والحسابات الافتراضية، وجرى ربط جميع حركات الحسابات الموجودة بمرجع العملة. يوجد **سند تاريخي واحد** بلا رمز عملة أو حساب مدين/دائن يمكن الاستدلال منه؛ أبقِي `cur_no` فارغًا لهذا السجل تحديدًا بدل افتراض عملة غير موثقة.

## التحقق البرمجي

نجحت اختبارات Vitest الكاملة: **27** ملف اختبار ناجح و**74** اختبارًا ناجحًا، مع **3** اختبارات متخطاة عمدًا. كما نجح `npx tsc --noEmit --pretty false` وبناء الإنتاج `npm run build:all`، واستجاب التطبيق المحلي على `http://127.0.0.1:3000/` برمز HTTP `200`.

يبقى في بناء الإنتاج تحذيران سابقان غير حاجبين: توافق `import.meta` مع إخراج CJS وحجم بعض الحزم بعد التصغير. لم يمنعا إنتاج حزمة الخادم أو الواجهة.

> أُبقيت إعدادات RLS دون تغيير وفقًا لتوجيه المشروع الصريح. لم تُفعَّل RLS أو تُعدَّل ضمن هذه المهمة.
