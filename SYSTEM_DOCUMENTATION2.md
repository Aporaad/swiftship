# وثيقة التحليل الفني واللوجستي والمالي الشامل (ERP Logistics Platform System Ledger & Core Blueprint)

تُعد هذه الوثيقة الدليل المرجعي الفني واللوجستي والمالي الشامل والمُوثّق لنظام تخطيط وإدارة موارد ومستندات شركة الشحن والوساطة اللوجستية الدولية والمحلية (**ERP Logistics Platform**). تم تفصيل وتحقيق كافة التراكيب والكيانات البرمجية والمالية لتُطابق بدقة تامة الكود المصدري المُنفّذ في النظام دون تزييف أو مبالغة.

---

## 1. البنية التقنية العامة وهندسة النظام (System Tech-Stack & Architecture)

أُسّست المنصة لتعمل كأداة مالية وميدانية حية ذات كفاءة تشغيلية فائقة وسرعة استجابة ملموسة تحت الأحمال وضغوط طرود المستودع اليومية.

*   **محرّك المكونات والأداء (Frontend Core):** تطبيق أحادي الصفحة (SPA) مبني على مكتبة **React 18+** بالاعتماد الكلي على المكونات الوظيفية (**Functional Components**) والخطافات التفاعلية (**Hooks**) وخطاف الـ Context لإدارة الحالة العامة للإعدادات.
*   **عنونة وضمان الكود (Typed Engine):** لغة **TypeScript (TSX)** لإصدار بيئة برمجية صارمة تضمن سلامة صياغة مدخلات النماذج وتطابق كائنات التدفقات الإجرائية.
*   **بوابة البناء والتصدير (Development Flow):** أداة البناء الحديثة **Vite** مدمجة بمصحح الأخطاء السريع.
*   **لغة المظهر والمؤثرات (Theming & Styling):** إطار عمل **Tailwind CSS** يدعم التصميم اللوجستي الداكن والفاخر (**Black Velvet/Crown Gold Dynamic Theme**)، متكاملاً مع أيقونات تفاعلية رشيقة من مكتبة **Lucide React**.
*   **قاعدة البيانات السحابية والمزامنة اللحظية (Cloud Base):** نظام **Google Firebase Firestore**، والذي يتميز بالمطابقة والاستماع اللحظي للبيانات الهيكلية والمالية عبر مستمعين ثابتين (**onSnapshot**) لتحديث الجداول والبطاقات المحاسبية والمناديب لحظةً بلحظة دون الحاجة لطلب تحديث يدوي.
*   **بوابة المصادقة والتأمين (Security & Authentication):** محرك **Firebase Authentication** لضبط دخول الموظفين والشركاء مع التوثيق الموحد والتحقق من حسابات المطورين.
*   **مولد ومحقق المعاملات الثنائي البعد (Interactive QR Barcode):** استخدام حزمة **QRCode** البرمجية لإتمام عملية رسم بوليصة التتبع والطلب على لوحات Canvas حية مدمجة بالواجهة للتسليم والمطابقة السريعة.
*   **محرك الفواتير والمستندات الورقية (Invoicing & Report Documents):** دمج مكتبة **jsPDF** البرمجية لتجميع كشوفات وجداول الفواتير الحالية، وضبط محاذاة البيانات وتوليدها كملف Ledger مالي ومحاسبي للطباعة بصيغة PDF.

---

## 2. الهيكل التفصيلي لكائنات البيانات الصارم (Firestore Collections & Real Database Schemas)

يعتمد محرك البيانات السحابية في النظام على هيكلية وثائقية صلبة ومحددة بدقة تامة في Firestore. نورد أدناه كل مجموعة (Collection) بحقولها وأنواع بياناتها كما هي منفذة بالملف البرمجي:

### 2.1 مجموعة الطلبيات (Collection: `orders`)
تمثل المستند الأكبر داخل النظام؛ حيث يحوي الطلب الواحد جميع التفاصيل اللوجستية، قائمة الطرود اللامتناهية، مسارات النقل والترانزيت المتتابعة، ومخرجات المحرك الحسابي والمالي الصافي:

| اسم الحقل (Firestore Key) | نوع البيانات (Type) | الوصف والدور التقني والميداني داخل النظام |
| :--- | :--- | :--- |
| `id` | `string` | معرف الوثيقة والتوزيع العشوائي التلقائي المولد من Firestore. |
| `orderNumber` | `string` | الرقم التسلسلي التلقائي الذكي للنظام وفق نمط توليد دوري سنوي شهري: `ALX-YYMM-NNNN` (NNNN يبدأ من 1001 ويتزايد بناءً على العمليات السابقة). |
| `customerId` | `string` | معرّف الربط الفلكي الخاص بالعميل في مجموعة العملاء `customers`. |
| `customerName` | `string` | الاسم الكامل للعميل لتسريع قراءته في الجدول وتسهيل الفوترة السريعة. |
| `customerPhone` | `string` | رقم هاتف أو واتساب العميل للمطابقة وعمليات الإشعار التلقائية. |
| `customerAddress` | `string` | عنوان العميل الجغرافي المسجل لإرشاد مناديب التوزيع. |
| `orderSourceType` | `string` | تصنيف احتساب الفاتورة المعتمد؛ يأخذ إحدى القيمتين حصراً: `'Factory'` (شحنات كبار ومصانع بالجملة) أو `'App'` (تطبيقات وسلال تسوق عائلية وشخصية). |
| `orderSourceId` | `string` | معرف قناة الشراء المرتبطة من مجموعة `sources`. |
| `orderSourceName` | `string` | الاسم النصي لقناة الشراء أو المصنع الخارجي لسرعة العرض. |
| `externalOrderNumber` | `string` | الرقم التعريفي الخارجي للطلب بداخل سلة المتجر (مثل سلة Shein أو رقم مرجع المصنع). |
| `trackingNumber` | `string` | البوليصة الدولية العامة للشحنة (الافتراضي هو `orderNumber` إن لم يحدد المورد بوليصة دولية). |
| `shippingCompany` | `string` | شركة وتيار الشحن والتجميع الخارجي الأساسية (مثل Aramex، DHL). |
| `shippingCourierId` | `string` | معرف المندوب اللوجستي الوسيط المسؤول عن النقل الإقليمي الأولي. |
| `deliveryCourierId` | `string` | معرف الموزع الداخلي المكلف بإعادة التوزيع والميل الأخير في محافظات اليمن. |
| `deliveryCourierFee` | `number` | الأجور الممنوحة للموزع المحلي بالريال اليمني لتسليم الطرد (الافتراضي: `4000 YER`). تضاف تلقائياً للفاتورة المحصلة من الزبون، وتوثق كـ عهدة مالية على عاتق الموزع. |
| `currency` | `string` | العملة الأصلية المعتمدة للمشتريات الخارجية؛ وتأخذ حصراً: `'SAR'` أو `'USD'`. |
| `exchangeRateYER` | `number` | أسعار بيع وصرف الريال السعودي مقابل الريال اليمني المعتمدة لحظة حفظ وتجميد الطلب (الافتراضي القائم بالنظام: `390`). |
| `exchangeRateUSD` | `number` | أسعار بيع وصرف الدولار الأمريكي مقابل الريال اليمني المعتمدة لحظة حفظ المعاملة (الافتراضي القائم بالنظام: `535`). |
| `bankCommissionRate` | `number` | نسبة عمولة التحويلات البنكية ومصاريف بوابات الرسوم (الافتراضي: `3%`). |
| `companyProfitRate` | `number` | مصاريف ونسبة عوائد الشركة مقابل شحن طرود التطبيقات (الافتراضي: `12%`). |
| `packagingFee` | `number` | الرسوم الإضافية وتكاليف الصناديق وعزل البضائع المسجلة بالريال السعودي (SAR). |
| `sheinRedPrice` | `number` | الأسعار الخاصة باللون الأحمر والمستحقة لخصومات تطبيق ومنافذ Shein؛ يتم احتسابها اختيارياً ومباشرة لتخطي مجاميع السلال وتحويلها لرأس مال بديل بالريال السعودي. |
| `totalWeight` | `number` | إجمالي الوزن المجمع والفعلي لطرود الطلب بالكيلو جرام (محتسب تراكمياً). |
| `totalCBM` | `number` | إجمالي الأبعاد والحجم المكعب للطرود بالمتر المكعب (محتسب تراكمياً لطلبات الاستيراد البحري الثقيل). |
| `totalCostSAR` | `number` | ناتج تكلفة الطلب وشحنه المحولة والمجمدة بالعملة الإقليمية (SAR). |
| `totalCostYER` | `number` | القيمة المطالب تحصيلها وسدادها بالريال اليمني الصافي (YER) كإجمالي نهائي للفاتورة. |
| `amountPaid` | `number` | المبالغ والتحصيلات المتراكمة والمقبوضة فعلياً بالريال اليمني للتسوية المالية للطلب. |
| `amountRemaining` | `number` | مديونية العميل المتبقية والمستحقة بالريال اليمني للتصفية السريعة (`totalCostYER` - `amountPaid`). |
| `paymentStatus` | `string` | الحالة المحاسبية للتسوية المالية بداخل الدفاتر للطلب: `'Paid'` (تم السداد بالكامل: المديونية المتبقية <= 0)، أو `'Partial Paid'` (تم سداد دفعات نقدية جزئية)، أو `'Unpaid'` (لم يسدد أي دفعات مسبقة للطلب). |
| `orderStatus` | `string` | الحالة اللوجستية والميدانية العامة الراهنة للطلب (ثماني حالات ثابتة؛ انظر قسم خط الحياة اللوجستي). |
| `deliveryStatus` | `string` | حالة حركية التسليم والتوزيع الموضعية لليمن: `'في الانتظار'`، `'جاري النقل'`، `'تم التسليم'`، أو `'مرتجع'`. |
| `locationYemen` | `string` | الموقع والمحطة الجغرافية الدقيقة لوجود البضائع حالياً في اليمن (مثل: مركز التوزيع الرئيسي، مقر الأصبحي، تعز). |
| `internalNotes` | `string` | التوجيهات المالية والتحذيرات اللوجستية ورخص الموزعين المدخلة داخلياً بواسطة الموظفين والمحاسبين. |
| `createdByEmail` | `string` | موثق الأمان؛ البريد الإلكتروني للموظف المدخل لطلب الاستيراد. |
| `createdByName` | `string` | الاسم الكامل للموظف صاحب معاملة التسجيل لدواعي الرقابة الإدارية. |
| `createdAt` | `number` | توثيق التوقيت التاريخي للإنشاء بنظام الميلي ثانية (Epoch ms). |
| `updatedAt` | `number` | تاريخ ووقت التعديل البرمي لمعاملة التدفق الميداني. |
| `items` | `Array<Item>` | مصفوفة المنتجات والمشتريات الفرعية التابعة للطلب (انظر هيكل طرد المنتج أدناه). |
| `shippingDetails` | `Array<ShippingRoute>`| مصفوفة مسارات النقل والترانزيت المتعددة الأجنحة المتوالية للطلب (انظر هيكل مسار الترانزيت أدناه). |

#### 2.1.1 نموذج تفريعات تفاصيل المنتجات (Class / Interface: `Item`)
عبارة عن مصفوفة هيكلية ومضمنة بداخل حقل `items` في مستند الطلب لتجنب الغموض في الفوترة:
*   `productName` (`string`): اسم وبيان وتصنيف السلعة بشكل قاطع (إجباري في النماذج).
*   `productUrl` (`string`): رابط المعاينة الإلكتروني لمتجر المنشأ مخصص لحسم نزاعات اللون والقياس وضمان الجودة بالمستودعات.
*   `quantity` (`number`): عدد القطع المطلوبة المستوردة للطلب (الافتراضي: `1`).
*   `productPrice` (`number`): ثمن السلعة الواحدة بالعملة الأساسية للطلب المحددة (سواء SAR أو USD).
*   `weight` (`number`): الوزن الفردي بالجرام أو الكيلو لحيازة الشحنات (مؤثر رئيسي لعمولات المصانع).
*   `cbm` (`number`): الحجم الفردي المقاس بالمتر المكعب للشحنات الضخمة والمصاعد المصنعية.
*   `trackingNumber` (`string`): كود بوليصة تتبع فرعي ومخصص للطرد الفردي في حال تجزأ التوريد.

#### 2.1.2 نموذج جزيئات التتبع وتحديث خطوات الترانزيت (Class / Interface: `ShippingRoute`)
مصفوفة ثنائية وبنيوية مضمنة بداخل حقل `shippingDetails` تتيح إقرار وإضافة عدة دروب شحن دولية ومحلية متوالية ومستقلة للطلب الفردي (Multi-leg Track Map):
*   `id` (`string`): رمز المعرف العشوائي المولد برمجياً لكل خط للتناغم اللحظي عند الحذف والتعديل الفردي دون التأثير على بقية المسارات.
*   `shippingType` (`string`): الأسلوب والبيئة الفيزيائية للناقل؛ ويأخذ حصراً: `'بري'`، أو `'جوي'`، أو `'بحري'`.
*   `shippingCompany` (`string`): الاسم التجاري للناقل المسؤول عن هذا الجزء من المسار (مثل شركة النجم للترحيل البري).
*   `shippingSource` (`string`): المنفذ أو مركز البداية لهذه المرحلة من الشحن (مثل مستودع الصين، ميناء عدن الدولي).
*   `shippingDestination` (`string`): محطة الوصول ونهاية مرحلة الشحن هذه (مثل مستودع السعودية، مستودع صنعاء الرئيسي).
*   `shippingDate` (`string`): تاريخ موثق لتسليم الطرود إلى الناقل الفرعي.
*   `shippingDuration` (`string`): الفترة الزمنية التقديرية باليوم أو الأسبوع (مثل: "4 أيام").
*   `expectedArrival` (`string`): التاريخ المأمول لوصول الشحنة وتفريغ الكتل في الوجهة المحددة لهذا المسار.
*   `deliveryDate` (`string`): الحيازة الفعلية؛ التاريخ المؤكد لوصول واستلام الشحنة في الوجهة، وتركها خاوية يعني أن الخط قيد الترانزيت.
*   `shippingCost` (`number`): مصاريف النقل والجمارك المنسوبة لهذا الجناح بالعملة الإقليمية للطلب (SAR).
*   `packagingFees` (`number`): الأجور والرسوم الخاصة بالتغليف الخشبي الإضافي أو التبريد للمرحلة الحالية (SAR).

---

### 2.2 مجموعة المشترين والعملاء (Collection: `customers`)
الفهرس المركزي لملفات العملاء وبيانات الاتصال والتقييمات المالية لتسريع ربطهم بالطلبات الجديدة:

*   `id` (`string`): المعرف الفرعي للعميل.
*   `fullName` (`string`): اسم العميل الكامل ثلاثياً والمقيد لدفاتر الاستيراد والتوزيع.
*   `phone` (`string`): رقم هاتف/واتساب الزبون لغرض التواصل والربط الإلكتروني وبث إشعارات القبض والشحنات.
*   `address` (`string`): النطاق الجغرافي السكني والمحافظة لفرز طرود الميل الأخير بالجمهورية اليمنية.
*   `notes` (`string`): مذكرات حول طبيعة حساب الزبون وتنبيهات الاستيراد الهامة.
*   `createdAt` (`number`): الطابع الزمني لتاريخ إضافة الزبون للفهرس المركزي.

---

### 2.3 مجموعة قنوات ومصادر التوريد الشريكة (Collection: `sources`)
تخصيص قنوات التوريد ومراكز الشراء لحساب الجدوى الإحصائية ومسارات الإيرادات والتحليلات البيانية:

*   `id` (`string`): معرف القناة في النظام.
*   `name` (`string`): مسمى قناة التوريد (مثل Shein Outlet Saudi، أو مصنع الألومنيوم للصحون بالصين).
*   `type` (`string`): طريقة العمل والطلب وتأخذ خياراً بديهياً من: `'App'` (متاجر وتطبيقات تسوق عولمية خاضعة للخصم والعمولة النسبية)، أو `'Factory'` (مصنع وشريك صناعي يعامل بحسابات الأوزان والحجم الفعلي).
*   `notes` (`string`): الاتفاق المبرم وأرقام المنسقين بالمعبر الخارجي والتوثيقات اللوجستية.

---

### 2.4 مجموعة شبكة الموزعين الداخليين والعهد (Collection: `couriers`)
تسجل شبكة مناديب التوزيع والترحيل وصناديق المال في المحافظات اليمنية لمراقبة حيازة الأموال وتحصيل الميل الأخير:

*   `id` (`string`): رمز المعرف للمندوب المحلي.
*   `fullName` (`string`): الاسم الكامل للمندوب.
*   `phone` (`string`): رقم هاتف التواصل السريع.
*   `governorate` (`string`): النطاق الجغرافي والمدينة الموكل له تغطيتها وتوزيع طرودها (مثل: صنعاء الدائري، تعز الحوبان، ذمار، عدن).
*   `activeOrdersCount` (`number`): مؤشر حسابي يمثل كمية الطرود والمانيفست المودعة برسم ذمة وعهدة المندوب حالياً قيد التوصيل الفعلي للجمهور.
*   `notes` (`string`): معايير التسليم، عمولاته المقطوعة، وسجلات تدقيق صندوق التوريد.

---

### 2.5 مجموعة المصروفات العامة وتسوية العهد المالي (Collection: `expenses`)
السجل المركزي لبيانات الصندوق والمصروفات والعهد اللوجستية وتوريد حسابات المصانع بالصين لتبيان التدفق المالي:

*   `id` (`string`): معرف وتذكرة وثيقة الصرف.
*   `expenseNumber` (`string`): الرقم التسلسلي المحاسبي المولد للمصروف بنظام أسبوعي/شهري: `EXP-YYMM-NNNN`.
*   `type` (`string`): طبيعة وتوجيه البند المالي ويأخذ تلقائياً إحدى القيم الثلاث حصراً دون سواها:
    *   `'General'`: مصاريف إدارية، وقود الشحن، بدلات تشغيل المكتب، إيجار المستودعات، ورواتب الفرز.
    *   `'Custody'`: عهدة مالية وميزانية ممنوحة للموزعين المحليين لتسيير قوافل النقل بين المدن باليمن.
    *   `'FactoryPayment'`: مبالغ وأقساط مودعة أو مرسلة للمصانع والوسطاء لتصفية الفواتير بالمنشأ.
*   `amount` (`number`): قيمة السند والمال الخارج من الخزينة.
*   `currency` (`string`): عملة معاملة السند المصروف حصراً: `'YER'`، أو `'SAR'`، أو `'USD'`.
*   `recipientId` (`string`): معرف مندوب التوزيع المستلم للمال فرعياً في حالة اختيار النوع كـ عهدة لوجستيكية (`Custody`).
*   `recipientName` (`string`): اسم مستلم المبلغ لتثبيت المسار والمطابقة (اسم المندوب، أو المصنع الخارجي، أو اسم الموظف للمكتب).
*   `factoryName` (`string`): اسم المصنع الشريك في المنشأ في حالة صفقات استيراد المواد الخام وتوريد حساباتها.
*   `status` (`string`): التقييم المالي للسند: `'Pending'` (عهد مالية معلقة برسم توصيل وتحقق الموازنة)، أو `'Completed'` (مصروف نهائي تم تسويته بالكامل وإغلاقه).
*   `createdByUid` | `createdByEmail` | `createdByName` (`string`): حقول هويات الموظف والمحاسب الذي وقع وقيد مستند الصرف لضمان سلامة التدقيق المالي.
*   `createdAt` (`number`): توقيت قيد المصادقة والصرف في النظام بدقة الميلي ثانية.

---

### 2.6 مجموعة الإخطارات السحابية ومصيدة السجلات (Collection: `notifications` & `whatsapp_logs`)
تحتفظ بمستندات التنبيه التلقائية وتراقب نجاح بوابة رسائل الواتس لتوثيق سلامة الإبراق:

*   **مذنب الإخطار الداخلي (`notifications`):** يحمل تفريعات التغييرات المجدولة بالطلبات والفواتير بمصفوفة:
    *   `id` (`string`): معرف الإخطار.
    *   `title` (`string`): عنوان التحذير الموجه للمحاسب أو المنسق.
    *   `message` (`string`): فحوى البيان وتغير الحالات.
    *   `type` (`string`): شكل ومسار الرسالة: `'order'`، أو `'alert'`، أو `'success'`، أو `'default'`.
    *   `read` (`boolean`): تصنيف القراءة والتحول لإطفاء الرمز الأحمر المضيء للتنبيهات.
    *   `createdAt` (`number` / `timestamp`): توقيت قيد وبث تنبيه النظام للواجهات.

*   **سجلات تبريق الواتساب لبيانات العملاء (`whatsapp_logs`):** تتبع سلامة تسلم الرسائل الإلكترونية لـ بوليصات الفرز وتحصيل الكاش:
    *   `id` (`string`): معرف العملية المبرقة.
    *   `phone` (`string`): رقم هاتف العميل الدولي المسلمة له الرسالة.
    *   `message` (`string`): السطور والبيان اللوجستي والمرجعي الذي تم تشكيله وإرساله للعميل بالواتساب.
    *   `status` (`string`): نتيجة الإبراق اللحظية: `'Success'`، أو `'Warning'`، أو `'Failed'`.
    *   `orderId` (`string`): معرف الطلبية المدمجة لتسهيل استرجاع سجل الإرسالات لكل شحنة.
    *   `createdAt` (`timestamp` / `number`): وقت وساعة ضرب البوابة وتوجيه حمولة الإشعار.

---

### 2.7 تخطيط مصفوفة تهيئة وإرسال بوابة الواتس (Collection: `settings` - Document: `whatsapp`)
يتم تخزين البارات والمفاتيح التقنية لشبكات تبريق الواتساب السحابية بداخل وثيقة وحيدة ثابتة مسماة (`whatsapp`) تابعة لجمع الإعدادات المركزي لضمان الأمان والربط الفني العالي:

```json
{
  "enabled": false,
  "provider": "sandbox",
  "config": {
    "token": "",
    "instanceId": "",
    "accountSid": "",
    "sender": "",
    "customUrl": "https://api.example.com/send?phone={phone}&text={message}",
    "customMethod": "POST",
    "customHeaders": "Content-Type: application/json",
    "customBody": "{\n  \"to\": \"{phone}\",\n  \"message\": \"{message}\"\n}"
  },
  "triggers": {
    "onOrderCreated": true,
    "onOrderStatusChanged": true,
    "onPaymentReceived": true
  },
  "templates": {
    "onOrderCreated": "مرحباً {customerName}،\nتم تسجيل طلبك رقم {orderNumber} بنجاح.\nالقيمة الإجمالية: {totalCost} YER\nالمدفوع: {amountPaid} YER\nالمتبقي: {amountRemaining} YER.\nقناة الشحن: {shippingCompany}\nتتبع الرقم الموحد: {trackingNumber}\nشكراً لاختيارك لنا!",
    "onOrderStatusChanged": "تحديث لوجيستي لشحنتك {orderNumber} 🚚:\nالحالة الحالية: {orderStatus}\nالموقع الحالي: {locationYemen}\nقناة الشحن: {shippingCompany}\nالمبلغ المتبقي: {amountRemaining} YER.",
    "onPaymentReceived": "سند قبض مالي إلكتروني 🧾:\nعزيزنا العميل {customerName}،\nتم استلام دفعة مالية بقيمة {amountPaid} YER لطلبك رقم {orderNumber}.\nالمقدار الكلي المدفوع حتى الآن: {totalCostSaved} YER\nالمديونية المتبقية: {amountRemaining} YER.\nشكراً لتعاملكم معنا."
  }
}
```

---

## 3. محرك المعادلات الحسابية والمالية التفصيلي وأسعار الصرف (Core Financial Engine)

تعتمد سلامة الدفاتر الحيوية للنظام ومنع فروق التقريب المصرفية على محرك محاسبي صارم يعمل بمسارين مستقلين تماماً، يفرضان قواعد صارمة عند حساب القيم الإجمالية والربحية.

### 3.1 معادلة حساب القيمة الإجمالية لرأس المال الصافي للطرود (Base Products Accumulator)
يقوم المحرك بجمع قيم كافة السلع والروابط المدرجة بجدول المنتجات فرعياً بناءً على العملة الأساسية المطلوبة والكمية المحددة لكل صف:
$$\text{Products Sum} = \sum_{i=1}^{n} (\text{item.quantity}_i \times \text{item.productPrice}_i)$$

---

### 3.2 هندسة تسعير شحنات المصانع والاستيراد التجاري (Factory Valuation Strategy)
لا تتبع شحنات الاستيراد الكبرى عوائد مئوية، بل يرتكز التسعير على استهلاك المساحات المادية للناقلات والحاويات البرية والبحرية والجوية:

1.  **قاعدة احتساب ثقل الوزن الفعلي للكتل (Weight Leg Fee):** يتم احتساب تكلفة تجميع ونقل الأوزان بناءً على معامل شحن لوجستي ثابت للنظام يعادل `19 SAR` لكل كجم كأجور مستحقة لنقل المستودعات:
    $$\text{Weight Cost SAR} = \text{totalWeight (KG)} \times 19$$
2.  **قاعدة احتساب حجم الحاويات بالبحر (CBM Volume Leg Fee):** يحسب للمنتجات والماكينات العريضة تكلفة المتر المكعب للنظام بمعامل شحن لوجستي بحري ثابت يعادل `1400 SAR` للمتر المكعب:
    $$\text{CBM Cost SAR} = \text{totalCBM} \times 1400$$
3.  **تحديد القيمة العادلة المعتمدة للشحن الخارجي (Shipping Fee Global SAR):** حرصاً على مصالح المؤسسة وعدم ملء الحاويات بسلع خفيفة ذات حيز شاسع، يقوم محرك الحسابات بالمقارنة الذكية للمكاسب واختيار القيمة اللوجستية والمالية الأكبر بين معادلة الوزن الرياضية والحجم الكلي (Math.max) وتقريرها كعمولة الشحن النهائية:
    $$\text{Shipping Cost SAR} = \max(\text{Weight Cost SAR}, \text{CBM Cost SAR})$$
4.  **تثبيت القيمة التقديرية لرأس مال المنتجات (Effective Price SAR):** تعادل مجموع البضائع المدخلة:
    $$\text{Effective Price SAR} = \text{Products Sum}$$

---

### 3.3 هندسة تسعير طرود التطبيقات وسلال التسوق الشخصية (App Invoicing Strategy)
تسير عمليات تسوق الأفراد والعائلات لـ Shein وغيرها وفق النظام التالي:

1.  **مصيدة تخطي ومطابقة سعر خصم اللون الأحمر (SHEIN Red Price Override):** تتيح واجهة الطلبات إثبات حقل خاص بقيمة "السعر باللون الأحمر" المخصوم في سلة مواقع شي إن بالريال السعودي. عند تسجيل المنسق لقيمة في حقل `sheinRedPrice` تزيد عن الصفر، يتوقف المحرك تلقائياً وبشكل كامل عن حوسبة مجموع الأسعار الفرعية للمواد، ويقوم بإسقاط القيمة البديلة للون الأحمر والعمل بها كرأس مال إجمالي للسلع:
    $$\text{Effective Price SAR} = \begin{cases} \text{sheinRedPrice}, & \text{if sheinRedPrice} > 0 \\ \text{Products Sum}, & \text{otherwise} \end{cases}$$
2.  **عمولة الشركة وعوائد التشغيل اللوجستيكية التلقائية (Shipping Cost Rate):** يفرض النظام عوائد شحن وتغليف تشغلية ثابتة تعادل نسبة مئوية مخصصة بالريال السعودي (الافتراضي المتماسك بالنظام: `12%` عبر متغير `companyProfitRate`) تضاف تلقائياً لحساب الفاتورة:
    $$\text{Shipping Cost SAR} = \text{Effective Price SAR} \times \left(\frac{\text{companyProfitRate}}{100}\right)$$

---

### 3.4 حوسبة الرسوم والضرائب والتحويلات البنكية (Financial Taxes & Commissions)
تُستقطع رسوم وعمولات تحويل مصرفي دولية تعادل نسبة مئوية ثابتة (الافتراضي بالنظام: `3%` ممثلة بمتغير `bankCommissionRate`) تضاف لتكلفة الاستيراد وتتحمل على عاتق الفاتورة بالريال السعودي:
$$\text{Bank Commission SAR} = \text{Effective Price SAR} \times \left(\frac{\text{bankCommissionRate}}{100}\right)$$

وتتشكل التكلفة التراكمية الإجمالية للفاتورة بالعملة الإقليمية (SAR) كالتالي:
$$\text{Total Order SAR} = \text{Effective Price SAR} + \text{Shipping Cost SAR} + \text{Bank Commission SAR} + \text{packagingFee}$$

---

### 3.5 تجميد وتسجيل الفواتير بالريال اليمني (YER Final Invoicing Settlement)
يجري تحويل وتثبيت الرقم النهائي المطلوب سداده بالميل الأخير بناءً على عملة تجميع المنتجات المقررة بالطلب:
$$\text{Total Cost YER} = \begin{cases} \text{Total Order SAR} \times \text{exchangeRateUSD}, & \text{if currency} = \text{'USD'} \\ \text{Total Order SAR} \times \text{exchangeRateYER}, & \text{if currency} = \text{'SAR'} \end{cases}$$

والشاهد في عمليات تجميد المعاملة باليمين تترجم بالمعادلات التالية:
*   `amountRemaining` (الديون المعلقة) = $\text{Total Cost YER} - \text{amountPaid}$
*   `paymentStatus` (التصنيف المحاسبي): إذا كان `amountRemaining <= 0` تصبح الحصة مستوفاة بالكامل كمال مدفوع `'Paid'`، عدا ذلك يُصنف الطلب كونه `'Partial Paid'` (سداد جزئي حركي) أو `'Unpaid'` (غير مسدد بتاتاً).

---

### 3.6 آلية تشريح وقسمة الأرباح اللوجستية الصافية (Profit Real-Time Division Mapping)
يقسم النظام محاسبياً الأرباح الدولية في الشحذ الخارجي الصافية للطلب بنسب ثابتة؛ حيث يحصل الشريك الإقليمي والمكتب اللوجستي الشريك في السعودية على نسبة `30%` دفترياً، ويسترجع مقر ومكتب الشركة الرئيسي في اليمن نسبة `70%` المتبقية:

1.  **استخلاص الربح اللوجستي الصافي المباشر (Calculated Raw Profit SAR):**
    $$\text{Raw Profit SAR} = \text{Shipping Cost SAR} + \text{packagingFee} - \text{Expenses Margin}$$
    *(التحقيق الفني لـ Expenses Margin يجرى بنظام تصفية الأجور: يعادل خصم `10 SAR` لكل كجم في طلبات المصانع كأجور تذاكر الشواحن والتخليص بالمنشأ، ويعادل `0` لمبيعات وطرود تطبيقات التسوق).*
2.  **حصة الشريك والمكتب السعودي في السعودية:**
    $$\text{Profit Saudi (Partner Office) SAR} = \text{Raw Profit SAR} \times 0.3$$
3.  **حصة الإدارة والشركة المغذية بصنعاء باليمن:**
    $$\text{Profit Company SAR} = \text{Raw Profit SAR} \times 0.7$$

---

## 4. نظام تتبع الحالات ومسار الرحلة اللوجستية الميدانية للطرود (Logistics Blueprint Lifecycle)

يقيد النظام حركة الطرود ومواقع المستودعات عبر رحلة فيزيائية صارمة من ثمانية محطات رئيسية، يتم تجميدها وتحديثها لحظياً بقوة دمج الواجهات مع Firestore:

```
                            قيد التسجيل (Order Saved)
                                       │
                                       ▼
                       وصل مستودع السعودية للتعبئة
                                       │
                                       ▼
                             جاري الشحن لليمن
                     (بري [عنبري] 🚛 - جوي [سيان] ✈️ - بحري [نيلي] 🚢)
                                       │
                                       ▼
                             في التخليص الجمركي
                                       │
                                       ▼
                        وصل مركز التوزيع والفرز بـاليمن
                                       │
                         ┌─────────────┴─────────────┐
                         ▼                           ▼
            مع المندوب للتسليم (Out for Delivery)       ملغي (Cancelled) [رمادي]
                         │
                         ▼
             تم التسليم وحفظ التحصيل (Settled)
```

1.  **تم تسجيل الطلب وحساب الفاتورة (Order Saved):** حيازة الحسابات؛ تم تشييد الطلب وهيكل كارت الفاتورة بمجموع الطرود والمنتجات وحجم أوزان التقدير، وبانتظار تلقي الشريك بالصين للبضائع.
2.  **وصل مستودع السعودية للتجميع والتعبئة (Arrived Saudi Hub):** حزم السلال؛ إيداع وتفريغ المنتجات والكراتين في ديوان التجميع بالرياض تمهيداً لحصر الأوزان الإجمالية الصافية وعزل الشحنات الهشة بالصناديق الخشبية الواقية.
3.  **جاري الشحن لليمن (In Transit to Yemen):** حركية الترانزيت البرية والجوية والبحرية؛ شحن الطرود وحمولاتها في الشاحنات الدولية، وتحديد تاريخ الترحيل الفني واسم الباخرة أو الطيران مع بث إشعار تتبع البويصلات المكتملة.
4.  **في التخليص الجمركي بالمنفذ (Customs Clearance):** المرور الجمركي؛ وقوف قوافل النقل بجمارك المنافذ البرية (منفذ الوديعة، جمرك البيضاء، جمرك شحن المهرة) لإتمام مراجعة الطرود وتخليصها ضريبياً وسداد الرسوم والجمارك المنسوبة.
5.  **وصل مركز التوزيع والفرز الرئيسي في اليمن (Arrived Final Depot):** فرز المحافظات؛ تسليم البضائع إلى مستودع المعالجة الوطني والفرز الرئيسي بـ (صنعاء أو عدن) لدراسة العنونة، وتصنيف الطرود بحسب رغبات ومديريات العملاء الجغرافية المعتمدة.
6.  **مع المندوب للتوصيل بانتظار التسليم (Out for Final Delivery):** إسناد الميل الأخير؛ تسليم الطرود عيناً وتعهيلها كعهد لوجستيكية في جعبة الموزعين الداخليين المقيدين بمحافظة العميل للتسليم بالمنزل، وتثبيت عمولة الموزع المقررة دفترياً (الافتراضي: `4000 YER`).
7.  **تم التسليم وتفريغ حيازة الكاش (Settled / Completed):** إغلاق الدورة؛ الموزع يجمع المبالغ والديون من العميل يداً بيد وتوقيع مستند المانيفست بالقبول وتوليد سند القبض وإتجاه المال الكاش فورياً لصندوق خزينة الشركة بفرع اليمن.
8.  **ملغي (Cancelled):** الإيقاف الكامل للمعاملة والطلب لعدم جدية الفرز أو بطلان المشتري بطلب العميل، وتأخذ شكلاً مستقرّاً ورمادياً ببطاقات الأرشيف لتوثيق التسوية الإدارية.

---

## 5. نظام إدارة المستخدمين والموظفين والتحكم بالصلاحيات (Staff Access Control & Security Matrix)

يتضمن بقوة بناءً تشريعيًا للحد من كسر المعلومات أو تسريب الميزانيات المالية، ويقسم المهام برخص دقيقة:

### 5.1 نظام حظر واستبعاد المناديب الاستباقي (Carrier Isolation)
*شكل متطور من عزل أدوار العمل اللوجستي:* يفرض النظام رقابة صارمة في واجهة الدخول والأمان؛ حيث يُحظر الموزعون الخارجيون المسجلون بـ (مجموعة المناديب `couriers`) من محاولة الدخول وتصفح الواجهات اللوحية عبر كود الاستبعاد التلقائي المبرمج بمتطوع الأمان:
```typescript
// كود عزل المناديب والمناديب في useRole لمنع الدخول وحفظ السرية
if (userData.role === 'Courier' || userData.roleId === 'courier' || userData.role === 'courier') {
  setRole(null);
  setPermissions([]);
  setProfile(null);
  setLoading(false);
  auth.signOut().catch(console.error);
  return;
}
```
هذا الأسلوب يقضي على تسريبات قوائم أرقام الهاتف للزبائن، أو الإطلاع على مكاسب الحاويات وخسائر الخطوط.

### 5.2 قائمة رخص الصلاحيات التقنية بالنظام (Comprehensive Permission Directory)
تم تقييد الواجهات والأزرار والتعديل بوثيقة رخص ثابتة ومعرفة كالتالي:

| رمز رخصة الصلاحية (Permission ID) | الاسم البرمجي واللوحي باللغة العربية | الوصف والصلاحية الممنوحة بمفاصل النظام |
| :--- | :--- | :--- |
| `view_dashboard` | عرض لوحة التحكم | فتح شاشات التحليلات والرسوم الكشفية للإيرادات. |
| `view_orders` | عرض الطلبات | استكشاف جداول الطلبات وأرشيف الشحنات وتتبع المسار. |
| `manage_orders` | إدارة الطلبات (إضافة/تعديل) | إنشاء الفواتير وإضافة الطرود وتحديث المبالغ ورأس المال. |
| `update_order_status` | تحديث حالة الطلب فقط | متاح فقط لتعديل خطوات الترانزيت اللوجستي ومواقعه باليمن. |
| `delete_orders` | حذف الطلبات | محو وتدمير مستند الطلب من قاعدة البيانات (صلاحية خطيرة). |
| `view_customers` | عرض العملاء | استعراض كشوفات المستوردين وأرقام تواصل الواتساب. |
| `manage_customers` | إدارة العملاء | إنشاء كروت المشترين وتصحيح العناوين والمنتمات. |
| `delete_customers` | حذف العملاء | محو سجل عميل من الفهرس السحابي للشركة. |
| `manage_sources` | إدارة مصادر الطلبات | إضافة وحفظ منافذ شي إن وسعر صرفها أو مصانع الصين. |
| `delete_sources` | حذف مصادر الطلبات | التخلي عن قنوات معينة وحرق معرّفاتها. |
| `manage_couriers` | إدارة المناديب | تسجيل شبكات التوصيل وتوزيع عهد الطرود جغرافياً. |
| `delete_couriers` | حذف المناديب | حذف وتسهيل شطب مناديب وموزعي الميل الأخير. |
| `manage_users` | إدارة الموظفين والأدوار | تفعيل رخص جديدة للعمل وتعيين الوظائف بالمكتب المالي. |
| `delete_users` | حذف الموظفين | فصل وإلغاء حساب موظف بالمكتب أو قسم الدعم. |
| `view_finance` | عرض البيانات المالية | مراقبة إجمالي الأرباح المشتركة والعهد الخارجة وميزانية الصندوق. |
| `manage_finance` | إدارة المالية والمدفوعات | صعود دفعات قبض الحسابات وتسجيل المصروفات العامة وتوريد العهد. |
| `settings` | إعدادات النظام | تحكم المطورين بالنوى وتنزيل النسخ الكاملة والدمج السحابي. |

---

### 5.3 تصفية وتصنيف الأدوار والوظائف البرمجية الافتراضية (Default Role Profiles)
تتثبت المجلدات والأدوار من وثائق هجينة تدعم فرض القواعد المعيارية للمصنعين والمناديب والموظفين الماليين:

1.  **حساب المسير الأكبر (Root SuperAdmin - `Admin`):**
    *   **الأشخاص الممنوحين:** أصحاب الملفات البرمجية الحتمية المقررة بالنواة (`alsrhyarslan5@gmail.com` ، `arslan.alshamari@gmail.com` ، `admin@swiftship.system`).
    *   **مصفوفة الصلاحيات:** يمنح رخصة شاملة تمثل بـ الرمز البديل (`['*']`) للولوج الشامل وتخطي الفلترة. لا يمكن شطبه أو تعطيله بواسطة لوحة التحكم.
2.  **أمين السر والتشغيل (Employee):**
    *   **مصفوفة الصلاحيات:** تقتصر رخص الموظفين على إدخال ومقايسة الحركات المباشرة: `['view_dashboard', 'view_orders', 'manage_orders', 'view_customers', 'manage_customers', 'delete_orders', 'delete_customers', 'manage_couriers', 'delete_couriers']`. يُعزل بالكامل عن الإطلاع على توازنات أرباح الشركة ومصروفات الصناديق لتلافي الاطلاع المالي العام.
3.  **المحاسب المالي للشركة (Accountant):**
    *   **مصفوفة الصلاحيات:** تحكم شامل بقضايا تدقيق النقد والمدفوعات: `['view_dashboard', 'view_orders', 'view_finance', 'manage_finance', 'manage_sources', 'delete_sources']`. يُعزل عن التلاعب بجداول الموظفين أو مسارات الأدوار الخاصة بالنواة.

---

## 6. نظام الإدارة المالية المحاسبية والتحصيلات (Accounting & Double-Leg Valuation System)

يحتوي النظام على بوابات مالية منظمة قادرة على تتبع عهد من عُملات متباينة، وتوريد النقد عبر نماذج مبرمجة:

### 6.1 واجهة المصروفات العامة والعهد وقسمتها اللوجستيكية (General Expenses Log)
تخضع شاشة المصاريف لتنظيم حاد يقيد التدفق النقدي:
*   **إنشاء سند الصرف المتتابع:** يضمن توليد رقم مالي معتمد تلو الآخر باسم `expenseNumber` بصيغة `EXP-YYMM-NNNN` للمطابقة الدفترية.
*   **تصديق وتوريد العهد للمحافظات:** عند تسجيل سند المصروف من نوع عهدة لموزع (`Custody`) واختيار مندوب معين من حقل المناديب في الفهرس السحابي، يتم تمويل حصالة الموزع بالمال لمساعدته في ترحيل الطرود وقطع السفر والرحلات.
*   **تصفية العمد وتحديث الأرصدة التلقائي:** يتمكن المحاسب من فحص مستندات الصرف والتحول بها من قيد المراجعة والمستمرة كعهد معلقة لتصبح مصروف مستوفى ومنتهي بعد مراجعة فواتير البنزين والنقل الميداني.

---

### 6.2 واجهة قبض الأقساط وتسوية ديون الطلب (Installments & Debt Settlement)
*آلية تصفية العميل يدوياً على مرحلتين:* يتاح للزبائن قبض وحوسبة ودفع الفواتير بأقساط جزئية حية عبر نافذة الاستلام السريع (`handleAddPayment`):
1.  **حساب المديونيات الجوية والبرية بالتحديث التلقائي:** يقرأ النظام رصيد الدفعة المدخلة ويشرح آلياً تراجع مديونية العميل المتبقية `amountRemaining` ويرفع مجموع ما سدده الزبون فعلياً `amountPaid`.
2.  **سند التحصيل الرقمي:** توثيق التاريخ وهويات المستقبل، والتصنيف الديناميكي التلقائي لحالة الفاتورة إلى `'Partial Paid'` (في انتظار التسوية ومتبقي عليه رصيد) أو تحويلها فوراً إلى اللون الأخضر الزاهر كمستند مالي مقفل ومسدد كلياً `'Paid'`.
3.  **توليد الـ WhatsApp المالي:** إثارة حدث الإرسال التلقائي للزبون بسند قبض رقمي يحمل تفاصيل الرصيد المستلم والمتبقي وتوقيع تذكرة المعاملة.

---

## 7. نظام الإشعارات وبوابة تبريق الواتساب الديناميكية (Dynamic Notifications & WhatsApp Dispatch Gateway)

تم دمج نظام الإبراق بأسلوبين فنيين لضمان عدم غياب التوجيهات والأحداث اللوجستية:

### 7.1 نظام التحذير والإخطارات الفورية للداشبورد (System Alerts Center)
مخزن مركزي يسجل تلميحات ترحيل الفواتير واستقبال النقد ويغذي بطاقة التنبيهات المضيئة بقمة الواجهة:
*   **التشخيص بالألوان والرموز:** يتم فرز بطاقة الرمز حسب نوع المعاملة لسرعة الاستدلال البصري:
    *   ✈️ **أيقونة شحنة طردية (`order`):** تظهر رمز علبة برتقالية/زرقاء زاهية للأحداث الاستيرادية.
    *   ⚠️ **تحذير بنوك ومصاريف الصناديق (`alert`):** تظهر رمز مثلث عنبري دافئ دال على خلل بعمليات الصندوق أو ديون متلتلتة.
    *   💚 **نجاح السداد والدفع الكلي (`success`):** تظهر بكرة دائرة خضراء زاهية ترمز لعمليات التحصيل.
*   **التصفية المتكاملة وتصفير القراءة:** يمكن للمنسق النقر على كبسة "تحديد الكل كمقروء" ليعمل بأسلوب Batch مالي على Firestore لتدمير وإخماد نقاط التنبيه الحمراء فوراً بنقرة وحيدة.

---

### 7.2 بوابة تبريق الواتساب ذات الهياكل القابلة للتخصيص (Dynamic WhatsApp Engine)
يتيح النظام تحويل رسائل سلاسل الإمداد لتبريق فوري ومباشر على حسابات الزبائن بالواتساب لرفع الثقة وضمان تأكيد الشحنات:
*   **تعدد موفري ومنافذ الخدمة (Gateway Providers):** يدعم محرك الإرسال المتكامل العمل تحت أربعة خوارزميات ربط قابلة للتحديد كلياً من الشاشة:
    1.  `Sandbox`: بوابة الأمان التجريبية لاختبار الفروع وقنوات التحقق الفني بدون رسوم تشغيلية.
    2.  `UltraMsg`: بروتوكولات ومنافذ التبريق السريع عبر الـ API الفاخرة للواتس.
    3.  `Twilio`: الاتصال مع القنوات الموثقة الرسمية للشركات الكبرى.
    4.  `Custom Webhooks`: تتيح للمطورين الربط الكامل مع أي سكريبتات إرسال خاصة عبر تمرير قيم وبلوكات ديناميكية بـ (الرابط المخصص، نوع الطلب POST/GET، الهيدرز والترتيب التمويلي للبودي بصيغة JSON).
*   **محرر القوالب الذكية وتلقيم المتغيرات (Variable Chips Injector):** يمنح النظام قسم إدارة تبريق الواتس لوحة تفاعلية ممسوكة بـ Chips وعلامات كودية قابلة للحقن واللصق بمكان مؤشر المحرر يدوياً:
    *   `{customerName}`: يتم تبديله آلياً لاسم المستورد المستفيد بالرسائل.
    *   `{orderNumber}`: يُلقم كود الفاتورة أو رقم الطلب لشركات التتبع.
    *   `{trackingNumber}`: يحقن رقم بوليصة الشحن الخارجي.
    *   `{totalCost}`: قيمة الطلب الصافية بالريال اليمني.
    *   `{amountRemaining}`: رصيد المديونية المتبقي على العميل للتحصيل باليمن.
*   **مراقبة الممانيفست المبعوث السحابي (Live logs):** شاشة تتبع تاريخية تعرض نجاح أو فشل وفترات محاولات الإرسال السابقة مع مرشحات ذكية لفحص أرسالات بوليصة محددة.

---

## 8. الهوية البصرية وخيارات الإعدادات الاستراتيجية والتصدير (Cosmic Theme & Settings Suite)

ينفرد النظام بلغة بصرية فائقة وذات حضور برمي فاخر، مع أدوات تحافظ على ديمومة وتوطين البيانات تحت أي ظروف:

### 8.1 الهوية المعمارية والسمات البصرية (Cosmic Velvet & Gold Aesthetic Theme)
*   **لوحة الخلفيات والتجانس الداكن:** الاعتماد على تدرجات لونية شديدة السواد والنعومة (`#0a0a0c` إلى `#121215`) مع تمثيل أسلوب المظهر الزجاجي التفاعلي (**Glassmorphism Component**) باستخدام فلاتر الضبابية المتقدمة للخلفية (`backdrop-blur-md`) وتخفيف الحدود إلى تباعد رائع ومريح للعين لتلافي التشتت المكتبي بالمستودع.
*   **الذهب الملكي المتألق (Royal Amber Gold - `#d4af37`):** اعتمد النظام الذهب العتيق واللامع كعنوان للأزرار المهمة، والتقطيرات والنقاط الزمنية المضيئة للتتبع، مما يعزز الفخامة الإدارية وجدية تداول الوثائق والأرقام المالية.
*   **النبض اللوجستي والخطوط الزمنية المتحركة:** إبراز خطوات وحالة الشحنة الحالية عبر تأثير نبض متدفق ومضيء بصرياً بلون فوسفوري واعد يجدب نظر المشغل فوراً لمكان ومدينة وقوف الطرود بشكل فوري.

---

### 8.2 شاشة التحكم الاستراتيجية والنسخ المالي (Central Settings Console)
أداة إدارة هجينة مبرمجة لأمناء السر والمديرين لحماية النوى:
1.  **لوحة التوطين المتزن وعولمة الواجهات (Bilingual Alignment):** تمكين التحول الفلكي بكامل صفحات وعناصر النظام للعمل إما باللغة العربية مع إجبار كامل العناصر اللوحية والتصميم على سلوك اليمين لليسار (`dir="rtl"`)، أو التحول الكامل للغة الإنجليزية وسلوك اليسار لليمين (`dir="ltr"`) بتبديل دقيق لمواقع الأزرار وأوزان الخطوط والمصطلحات.
2.  **أداة النسخ السحابي العازلة لإنقاذ وفك الفهارس (JSON Vault Ledger Backup):** إتاحة تنزيل نسخة رقمية معززة كاملة وذات تشفير هيكلي بصيغة ملف بياني `.json` يحتوي على خلاصة كافة قواعد وفهارس النظام السحابية مجتمعة بالكامل (`customers`, `couriers`, `sources`, `orders`, `users`, `roles`).
3.  **أداة إعادة التكتل والدمج المحمي (Rebase Ledger Import):** تمكن مديري النظام من استدعاء ملف الاحتياط ورفعه وإعادة قرصنة ودمج السحابة ومحو أي هياكل معطوبة لتوليد تهيئة تشغيلية موثوقة ونظيفة بالكامل وبضغطة زر.
4.  **تطهير كاش النواة المتصفح الموضعي:** تمكن المطورين من تنظيف ومحو ركام ذاكرة المتصفح الموضعية وفك الملفات المؤقتة لتصفير الاستدعاءات الحركية وحسم تعارز الهوست المطبخ الفني للرياكت.

---

**مستند وتوثيق النواة والتحليل اللوجستي المحاسبي لشبكة الاستيراد الذكية - إصدار كود 2026.** معتمد وموقع كلياً لإرشاد المطورين وتوسيع مفاصل وتطبيقات الفئات وقنوات التحصيل والطرود.
